# 测试入门指南

> 这份文档写给**完全没接触过自动化测试**的人。
> 不需要你会写代码。读完你能做到三件事：
> 1. 知道「自动化测试」到底在干什么、为什么值得花两天做；
> 2. 看懂测试报告里的那些数字（379 个用例、61% 覆盖率…）；
> 3. 明白本项目里那 442 个用例分别在守着什么。
>
> 想直接看结论 → 跳到 [第七章](#七三个测试抓-bug-的真实复盘重点)（三个真实 Bug 复盘）。

---

## 〇、先讲个故事：没有测试的日子

假设你要上线一个「登录」功能。手动验证是这样的：

1. 打开浏览器，输入账号密码，点登录 → 成功 ✅
2. 输错密码，点登录 → 提示错误 ✅
3. 好，功能没问题，上线。

**三天后**，你改了一个不相干的地方——比如「密码输入框加了显示密码的小眼睛」。改完你只测了小眼睛，觉得没问题，上线了。

结果：**用户输错密码时页面白屏**。因为那个改动不小心碰到了一段校验逻辑。

问题出在哪？

| 手工测试的痛点 | 说明 |
| --- | --- |
| **只能测你想到的场景** | 你脑子里只有 3 条路径，实际有 20 条（空密码、超长用户名、网络超时、token 过期…） |
| **改完要全部重测一遍** | 这叫「回归测试」。功能一多，每次改一点就要点几十遍，人会烦、会偷懒 |
| **不敢动老代码** | 你不知道改这里会不会弄坏那里，于是代码越来越烂，谁也不敢碰 |
| **凌晨三点才发现** | 用户先发现的 Bug，代价最大 |

**自动化测试解决的就是这件事**：把「手动点击验证」变成「一行命令，几秒钟，几百个场景全跑一遍」。

> 一句话：**测试就是给代码雇了一个不知疲倦、不会偷懒、每次改动都从头到尾重查一遍的质检员。**

---

## 一、测试的本质：一个测试长什么样

剥掉所有术语，一个测试就三件事：

```
① 准备（给定输入） → ② 执行（跑一下代码） → ③ 断言（结果等于我期望的吗？）
```

拿本项目一个真实的测试来看（`backend/tests/unit/test_weather_helpers.py`）：

```python
def test_空值返回None(self):
    # ① 准备：输入是 None
    # ② 执行：调用风向换算函数
    result = wc._wind_dir(None)

    # ③ 断言：结果应该是 None
    assert result is None
```

```python
@pytest.mark.parametrize(
    ("deg", "expect"),
    [(0, "北"), (45, "东北"), (90, "东"), (135, "东南"),
     (180, "南"), (225, "西南"), (270, "西"), (315, "西北")],
)
def test_八方位换算(self, deg, expect):
    assert wc._wind_dir(deg) == expect
```

第二段只写了 1 个函数，但会跑出 **8 个测试**（0°→北、45°→东北…）。
这种写法叫**参数化**——同一套逻辑喂不同数据，省得复制粘贴 8 遍。

> ⚠️ 顺便解释一个你可能疑惑的点：本项目后端说「379 个用例」，
> 但代码里 `def test_` 只有 314 个。差的那 65 个就是参数化**展开**出来的。
> 报告里的数字是「实际执行的检查次数」，比函数个数更有意义。

**如果断言不成立会怎样？** 测试**失败**（报红），并告诉你：

```
E  AssertionError: assert '小雨' == '中雨'
```

左边是实际结果，右边是你期望的。这行信息就是排查 Bug 的起点。

---

## 二、测试分三层：为什么不是「一种测试搞定所有」

这是最容易被新手搞混的地方。其实测试有**三个层次**，各有分工：

```
                     ┌─────────────────────────────┐
   少而精 ↑           │  E2E 端到端测试（22 个）      │  打开真浏览器，
          │           │  模拟真人操作整个网站         │  最真实、最慢、最贵
          │           ├─────────────────────────────┤
          │           │  接口测试（78 个）            │  直接调后端 API，
          │           │  验证"前端拿到的数据对不对"    │  不渲染界面
          │           ├─────────────────────────────┤
   多而快 ↓           │  单元测试（301 个）           │  只测一个函数/一小块逻辑，
                      │  毫秒级、跑得飞快             │  最便宜、最多
                     └─────────────────────────────┘
```

**为什么是金字塔形状（底下多、上面少）？**

| 层次 | 跑一次耗时 | 能发现什么 | 缺点 |
| --- | --- | --- | --- |
| 单元测试 | 毫秒 | 某段逻辑算错了、边界没处理 | 发现不了「接口接错线」 |
| 接口测试 | 几十毫秒 | 接口参数、鉴权、数据库读写 | 发现不了「页面按钮点不动」 |
| E2E | 几秒～几十秒 | 用户流程整条链路 | 慢、脆（外部依赖多）、维护贵 |

底层要**多**（每个函数都该有几个），上层要**少而关键**（只测最重要的几条用户路径）。
如果反着来——写 500 个 E2E、0 个单测——你会发现改一个字就要等 20 分钟，而且经常误报。

**本项目怎么分的？**

| 层次 | 工具 | 数量 | 覆盖什么 |
| --- | --- | --- | --- |
| 单元测试 | pytest | 301 | 天气数据解析、穿衣规则、路线评分、正文抽取、意图识别… |
| 接口测试 | pytest + httpx | 78 | 登录注册、行程、笔记、反馈、首页、鉴权边界（401/403/409） |
| 前端单测 | Vitest | 41 | 天气映射函数、axios 拦截器、时间轴组件 |
| E2E | Playwright | 22 | 注册登录 → 看天气 → AI 对话 → 管行程 → 提反馈 |
| **合计** | | **442** | |

---

## 三、黑话翻译表

**新手看不懂测试，90% 是因为术语。** 先扫一眼这张表，后面看代码就不懵了。

| 黑话 | 人话 |
| --- | --- |
| **用例（Test Case）** | 一条检查。比如「输对密码能登录」就是一条用例 |
| **断言（Assert）** | 判断「结果是不是我期望的」。`assert 结果 == 期望`，不成立就报错 |
| **测试通过 / 失败** | 俗称「绿了 / 红了」。全绿=所有检查都成立 |
| **Fixture（夹具）** | **测试用的预备工作**。比如"先造一个已登录用户"，写好一次，所有用例都能用。类比：做菜前切好备好的料 |
| **Mock（打桩 / 模拟）** | **假装某个东西存在**。比如测试时需要"和风天气返回 30℃"，但不想真去调 API，就造一个假回应。类比：拍戏用的道具枪 |
| **参数化** | 一套逻辑喂多组数据，自动展开成多个用例（见第一章例子） |
| **覆盖率（Coverage）** | 「我的代码有多少行被测试跑过」。61% = 六成代码被执行过 |
| **回归（Regression）** | 改完新功能后，把老功能**全部重测一遍**，防止"改好一个坏三个" |
| **E2E（端到端）** | 用真浏览器模拟真人，从点按钮到看结果整条链路都真实执行 |
| **flaky（抖动）** | 同一份代码，时好时坏的测试。E2E 的天敌，必须消灭 |
| **CI** | 持续集成。每次提交代码，机器自动跑一遍测试，红了就不让合并 |
| **红线（fail_under）** | 覆盖率低于某个值就直接失败。本项目设了 60%，防止越写越低 |

---

## 四、Day 29：后端测试（379 个用例）

### 4.1 装了哪些工具，各管什么

文件：`backend/requirements-dev.txt`

| 工具 | 干什么的 | 类比 |
| --- | --- | --- |
| `pytest` | 测试框架本体，负责"发现测试、执行、报结果" | 考场 + 监考老师 |
| `pytest-asyncio` | 本项目的接口都是 async 的，它让测试也能 await | 让考试支持"异步答题" |
| `pytest-cov` | 统计覆盖率，出报告 | 阅卷后统计"哪些知识点没考到" |
| `respx` | 拦截 httpx 请求（专门对付外部 API） | 门卫，把"打给外网"的电话转接到假号码 |

**注意这里**：这些依赖放在 `requirements-dev.txt`，**生产镜像不装**。
测试工具不该出现在线上环境——既没必要，也是安全风险。

### 4.2 测试文件怎么组织的

```
backend/tests/
├── conftest.py          ← 全局"预备工作"（fixture 都在这）
├── unit/                ← 单元测试：只测一段逻辑
│   ├── test_weather_helpers.py     (14) 天气数据解析、容错
│   ├── test_outfit_engine.py       (12) 穿衣规则
│   ├── test_route_planner.py       (13) 路线评分
│   ├── test_article_extractor.py   (17) 新闻正文抽取
│   ├── test_scoring_and_helpers.py (33) 评分与工具函数
│   └── ... 共 14 个文件
└── api/                 ← 接口测试：调后端 API 看返回
    ├── test_auth.py                (13) 注册/登录/鉴权
    ├── test_notes.py               (24) 行程笔记增删改查
    ├── test_itinerary.py           (17) 行程
    ├── test_feedback.py            (13) 反馈
    └── test_home.py                (11) 首页聚合
```

### 4.3 三个关键设计（为什么测试能"跑得干净"）

这些是 `conftest.py` 里花了心思的地方，也是最容易踩坑的地方：

**① 用独立的测试数据库**

测试会**真的往数据库写数据**（造行程、造用户）。如果和开发库共用，你的演示数据会被冲掉。

做法：把数据库连接指向 `weather_db_test` 这个独立库，和开发库物理隔离。
测试怎么折腾都行，测完随手清空。

**② 每个用例跑完自动清表**

```python
@pytest_asyncio.fixture(autouse=True)
async def _clean_tables():
    yield                                    # 先让用例执行
    await conn.execute(text(f"TRUNCATE TABLE {names} RESTART IDENTITY CASCADE"))
    #                          ↑ 清空所有表        ↑ 自增 ID 也归零
```

`autouse=True` 意思是"每个用例都自动套用"，不用手动写。
**为什么必须清？** 否则「用例 A 造了一条行程」会影响「用例 B 的断言」，
测试就变成"看运气"——这正是新手写测试最常见的翻车点。

**③ 外部依赖全部打桩（Mock）**

本项目的天气数据来自和风、路线来自高德、新闻搜索来自 Tavily、AI 回复来自大模型。
测试时**一个都不真调**：

| 好处 | 说明 |
| --- | --- |
| 断网也能跑 | 飞机上、CI 服务器上都能跑，不依赖网络 |
| 不烧钱 | 不消耗 API 额度（尤其大模型） |
| 结果稳定 | 真实天气每天都变，没法断言"今天一定是 30℃"；假数据永远是 30℃ |
| 能测异常 | 可以故意让"和风返回错误"，验证代码的容错逻辑——真实环境很难复现 |

`conftest.py` 里还有一句很妙的设计说明：

> `respx_mock` 未匹配到的外部请求会直接报错——这本身就是一道防线，
> 能暴露出"某个用例偷偷访问了真实网络"。

翻译成人话：**如果有测试没打招呼就往外网打电话，测试会当场抓住它。**

### 4.4 结果

```
379 个用例全部通过
覆盖率 61%（红线 60%）
断网执行依然全绿   ← 证明外部依赖确实全被拦住了
```

---

## 五、Day 30：前端测试（41 个单测 + 22 个 E2E）

前端比后端多一层概念，先看图：

```
┌──────────────────────────────────────────────────────────┐
│  E2E 测试（Playwright，22 个）                            │
│  真的开一个浏览器 → 真的点按钮 → 真的看页面出现什么       │
│  「代替人把整个网站用一遍」                                │
└──────────────────────────────────────────────────────────┘
┌──────────────────────────────────────────────────────────┐
│  组件 / 单元测试（Vitest，41 个）                         │
│  不开浏览器，在内存里假装有个网页 → 渲染一个组件 → 查文本 │
│  「不开浏览器也能验证界面逻辑」                            │
└──────────────────────────────────────────────────────────┘
```

### 5.1 为什么两种都要？

| | Vitest 单测 | Playwright E2E |
| --- | --- | --- |
| 速度 | 5 秒跑完 41 个 | 2 分钟跑完 22 个 |
| 依赖 | 不需要起服务 | 需要后端+数据库+前端全跑起来 |
| 能发现 | 函数算错、组件渲染错、状态没更新 | 接口接错、按钮点不动、登录态丢失 |
| 测不了 | 真实的路由跳转、跨页面流程 | —（什么都能测，就是慢） |

**策略：能用单测覆盖的就别用 E2E。** E2E 只留给"最重要的几条用户路径"。

### 5.2 单测测了什么

| 文件 | 用例 | 测试对象 | 覆盖率 |
| --- | --- | --- | --- |
| `utils/weather.test.ts` | 9 | 天气图标映射、星期格式化 | **100%** |
| `api/http.test.ts` | 14 | 请求自动带登录凭证、响应解包、401 自动登出、超时提示 | **100%** |
| `components/ForecastList.test.tsx` | 13 | 时间轴 7天/14天/30天切换、缺数据显示占位、接口挂了不白屏 | 91% |
| `components/WeatherHero.test.tsx` | 5 | 天气主卡渲染、空态、字段缺失显示 `--` | — |

**为什么 `api/http.ts` 值得 100% 覆盖？**
它是**所有请求的必经之路**——每个接口调用都会过它。这里的逻辑（自动加 token、
统一解包、401 处理）一旦出错，是**全站性故障**，而不是某个页面出问题。
测试投入产出比最高的就是这种"枢纽代码"。

### 5.3 E2E 测了哪五条路径

| 文件 | 用例 | 路径 |
| --- | --- | --- |
| `auth.spec.ts` | 4 | 注册 → 自动登录 → 退出；密码错误提示；表单校验 |
| `home.spec.ts` | 6 | 打开首页自动出天气；提醒/穿搭区块；时间轴切换；导航跳转 |
| `itinerary.spec.ts` | 5 | 新增 → 编辑 → 删除行程；表单校验；笔记面板 |
| `feedback.spec.ts` | 4 | 匿名提交成功；空内容拦截；类型标签 |
| `chat.spec.ts` | 3 | 提问收到回复；空输入不发送；对话进历史侧栏 |

**E2E 的关键设计**：

- **打真实服务**，不做任何模拟——E2E 的价值就在"真实"，模拟了就失去意义
- **每个用例注册一个全新账号**（用户名带时间戳），互不干扰，想跑几遍跑几遍
- **失败自动截图 + 录像**，不用重现现场就知道当时页面长什么样

---

## 六、覆盖率是什么？61% 算高还是低？

**一句话**：代码有 100 行，测试跑完只执行了 61 行 → 覆盖率 61%。

```
   if 温度 > 30:              ← 测试跑过
       建议短袖                ← 测试跑过
   else:
       建议外套                ← 测试跑过
```

**但覆盖率会骗人**，这是新手最容易误解的地方：

```python
def test_除法(self):
    calc(10, 2)        # 执行了代码 → 覆盖率 +1
                       # 但一行断言都没写！代码写得对不对，压根没验证
```

上面这个测试能让覆盖率变高，却什么都没检查。这叫「假绿」。

**所以怎么看覆盖率？**

| 数字 | 含义 |
| --- | --- |
| 60%+ | 及格。核心逻辑基本有保障 |
| 80%+ | 良好。一般是核心模块单独拉高 |
| 100% | **不一定好**。可能是写了一堆没断言的测试凑数 |
| 30%- | 危险。改了代码不知道会坏哪 |

**本项目的口径**（`pytest.ini`）：

```ini
omit =
    */app/scripts/*     # 命令行工具脚本（手动跑一次的那种）
    */app/tasks/*       # Celery 任务定义，只是对 service 的包装

[coverage:report]
fail_under = 60         # 低于 60% 直接判定失败，CI 会拦住
```

**为什么排除那两类文件？** 它们是"胶水代码"——只是调用别处的逻辑，
真正业务在 `services/` 层（已覆盖）。排除它们，覆盖率才能真实反映"业务逻辑测够了没"。

> ⚠️ 这个排除是**有正当理由的收窄**，不是为了好看而删。
> 区别在于：`app/tasks/xx.py` 里只有 `collect_alerts.delay()` 这一行调用，
> 而它调用的 `collect_alerts()` 函数本身是被测过的。

---

## 七、三个"测试抓 Bug"的真实复盘（重点）

**为什么这段最重要？** 因为「测试能抓 Bug」听起来像推销话术。
下面三个是**这两天真实发生**的事——如果不写测试，它们会一直悄悄躺在代码里。

### Bug 1：雷阵雨拿不到"防雷"提醒

**现象**：天气是「雷阵雨」时，系统只提示"记得带伞"，没提示"注意防雷"。

**代码长这样**：

```python
WEATHER_RULES = {
    "雨":   ["记得带伞", "路面湿滑"],
    "雷":   ["注意防雷", "远离大树"],     # ← 写了，但永远轮不到它
    "雷阵雨": ["注意防雷", "尽量别在户外"],
}

for keyword, tips in WEATHER_RULES.items():
    if keyword in weather_desc:
        result = tips
        break                      # ← 找到就停
```

**为什么没人发现**：字典里「雨」写在「雷」前面。「雷阵雨」包含「雨」，
所以第一轮就命中「雨」并 `break` 跳出了循环——**「雷阵雨」那条规则是死代码**。

平时测试"下雨天"完全正常，只有精确输入"雷阵雨"才暴露。

**测试怎么发现的**：
```python
def test_雷阵雨应包含防雷提示(self):
    tips = get_tips("雷阵雨")
    assert any("防雷" in t for t in tips)   # ❌ 失败 → 抓到！
```

**修复**：调整词表顺序（危险现象在前）+ 去掉 `break`，让规则可以叠加生效。

---

### Bug 2：「中雨转小雨」显示成「小雨」

**现象**：天气预报写"中雨转小雨"，界面上显示的图标和文字却是"小雨"。

**修复前的代码**：

```typescript
const WEATHER_MAP = { 晴:…, 多云:…, 阴:…, 小雨:…, 中雨:…, 雷阵雨:…, 雷阵雨伴冰雹:… }

for (const key of Object.keys(WEATHER_MAP)) {
  if (desc.includes(key)) return WEATHER_MAP[key]   // 谁先声明用谁
}
```

**为什么没人发现**：这是"按声明顺序匹配"的经典陷阱——

| 输入 | 期望 | 实际 | 原因 |
| --- | --- | --- | --- |
| 中雨转小雨 | 中雨 | **小雨** | 字典里「小雨」声明在「中雨」前面 |
| 雷阵雨伴冰雹 | 雷阵雨伴冰雹 | **雷阵雨** | 「雷阵雨」先命中，更具体的那条废了 |

> 注意这两个 bug 是**同一个病根**（宽泛规则抢先命中具体规则），
> 一个在后端词表、一个在前端字典。**这就是"同一类错误会反复出现"的典型**——
> 所以测试要覆盖的不只是"这个 bug"，而是"这类 bug"。

**修复**：不再看声明顺序，改成**「出现位置最靠前的优先；位置相同时取更长的关键词」**：

```typescript
for (const key of Object.keys(WEATHER_MAP)) {
  const idx = desc.indexOf(key)
  if (idx < bestIndex || (idx === bestIndex && key.length > bestKey.length)) {
    bestKey = key                       // 「雷阵雨伴冰雹」更长 → 优先于「雷阵雨」
  }
}
```

---

### Bug 3：登录失败，用户看到一行英文技术黑话

**现象**：密码输错，页面上跳出：

> Request failed with status code 401

用户一脸问号。而后端明明返回了友好的中文：

```json
{ "detail": "用户名或密码错误" }
```

**为什么没人发现**：这个 Bug **只有真的输错密码才会出现**。开发时天天用正确的密码
登录，测试时也是"点一下登录就进去了"——错误的路径天然被跳过。

**测试怎么发现的**：E2E 特意走一遍错误密码：

```typescript
await page.getByPlaceholder('至少 6 位').fill('WrongPassword123')
await page.getByRole('button', { name: /登\s*录/ }).click()
await expect(page.getByText('用户名或密码错误')).toBeVisible()   // ❌ 失败
// 实际显示的是 "Request failed with status code 401"
```

**根因**：请求封装层把原始错误对象直接抛给页面，页面上取了 `e.message`，
拿到的是 axios（网络库）自己拼的技术信息，而不是后端给的中文提示。

**修复**：登录页专门从响应体里提取 `detail` 字段。

---

### 三个 Bug 的共同点

| | Bug 1 | Bug 2 | Bug 3 |
| --- | --- | --- | --- |
| 谁会发现 | 用户（天气是雷阵雨那天） | 用户 | 用户 |
| 手工能测出来吗 | 能，但要恰好输入"雷阵雨" | 能，但要恰好输入"中雨转小雨" | 能，但要主动输错密码 |
| 写测试后 | 永久守住 | 永久守住 | 永久守住 |

**核心价值在这**：修一次 Bug 不难，难的是**保证它不会在三个月后被人改回来**。
测试用例会永远站在那里：谁再改坏，机器立刻报红。

---

## 八、测试写起来有哪些坑（为什么它也是门手艺）

新手常以为"写测试 = 多写几个 assert"。实际上真正的难点在**让测试稳定、可信**。
这两天踩的坑：

| 坑 | 现象 | 解决思路 |
| --- | --- | --- |
| **用例互相污染** | 单独跑全绿，一起跑一片红 | 独立测试库 + 每个用例后自动清表 |
| **连接池跨事件循环** | `RuntimeError: Event loop is closed` | 测试环境改用 NullPool（不缓存连接） |
| **外键约束** | 测试直接写 `user_id=1` 报错 | 先造用户、拿真实 ID，再写关联数据 |
| **测试断言写错** | 以为代码有 Bug，其实是期望写错了 | 区分「代码错了」和「我以为错了」——这次真有 3 处是后者 |
| **antd 按钮点不动** | 找不到名为「登录」的按钮 | antd 给两个字的按钮**自动插空格**（「登录」→「登 录」），要用正则匹配 |
| **隐藏元素点不了** | 点击一直超时 | 组件渲染了一个视觉隐藏的单选框，必须点它旁边可见的标签 |
| **同名文字撞车** | 报 strict mode violation | 页面标题和导航菜单项文字相同，要限定查找范围 |
| **测试产物清理失败** | 启动就报错 | 本机环境拦截了文件删除操作，改用带时间戳的全新目录绕开 |
| **Node 版本不够** | Playwright 起不来 | 它要求 Node 20+，本机默认 18，需显式指定版本 |

**几个反 flaky（抗抖动）的设计原则**（E2E 尤其重要）：

1. **不猜上一条测试留下的状态** → 每个用例用全新账号
2. **不断言会变的东西** → 只断言"温度卡片出现了"，不断言"温度一定是 29.9℃"（天气真的会变）
3. **日期动态计算** → 不写死 `2026-09-25`，否则过几天测试就全红
4. **等状态而不是等时间** → 等"元素出现"，而不是"睡 3 秒"

---

## 九、这些测试对项目的实际价值

| 价值 | 具体体现 |
| --- | --- |
| **敢改代码** | 改完跑一遍 `pytest`，2 秒内知道有没有弄坏别的地方 |
| **重构有底气** | 换天气数据源（和风 ← Open-Meteo）时，解析层测试立刻验证新旧行为一致 |
| **回归自动化** | 442 个检查 2 分钟跑完，替代人工点几十遍 |
| **文档作用** | 看 `test_notes.py` 就知道笔记接口支持哪些操作、边界怎么处理 |
| **面试硬通货** | 能讲清"为什么这么设计、踩了哪些坑、测试抓出过什么 Bug"，比说"我写过测试"强太多 |
| **CI 的基础** | Day 31 的流水线就是「每次提交自动跑这 442 个用例」，红了禁止合并 |

**面试怎么讲（30 秒版）**：

> 后端用 pytest 写了 379 个用例，分单元和接口两层，外部依赖（和风、高德、Tavily、LLM）
> 全部用 respx 打桩，所以断网也能跑、不消耗额度；数据层用独立测试库 +
> 每个用例后自动清表保证隔离。前端用 Vitest 写组件和工具函数单测（41 个），
> 用 Playwright 覆盖 5 条核心用户路径（22 个）。这些测试抓出过 3 个真实缺陷，
> 比如「雷阵雨」因为字典顺序问题永远命中「雨」的规则，拿不到防雷提示。

---

## 十、你自己怎么跑

### 后端测试

```bash
cd backend

# 跑全部（含覆盖率报告）
.venv/Scripts/python.exe -m pytest

# 只跑快的单元测试
.venv/Scripts/python.exe -m pytest tests/unit -q

# 只跑接口测试
.venv/Scripts/python.exe -m pytest tests/api -q

# 只跑一个文件 / 一个用例
.venv/Scripts/python.exe -m pytest tests/api/test_notes.py
.venv/Scripts/python.exe -m pytest "tests/api/test_notes.py::TestCrud::test_创建笔记"

# 覆盖率 HTML 报告（生成到 htmlcov/index.html，能逐行看到哪行没被测试跑到）
.venv/Scripts/python.exe -m pytest --cov-report=html
```

### 前端测试

```bash
cd frontend-user

npm test            # 单元测试（41 个，约 5 秒）
npm run test:cov    # 单测 + 覆盖率（HTML 报告在 coverage/index.html）
npm run test:e2e    # E2E（22 个，需要先 docker compose up -d 起服务）
npm run test:e2e:ui # E2E 可视化调试模式（能看到浏览器实时操作）
```

**E2E 跑失败了怎么看？** 项目配置了「失败自动留痕」：

```
frontend-user/e2e-results/<时间戳>/<用例名>/
├── test-failed-1.png   ← 失败瞬间的截图
├── video.webm          ← 完整操作录像
└── trace.zip           ← 可回放的详细轨迹
```

用 `npx playwright show-report` 能看到一个网页版的报告，点进去像看录像回放。

---

## 附录 A：442 个用例全清单

### 后端单元测试（301 个，14 个文件）

| 文件 | 数量 | 测什么 |
| --- | --- | --- |
| `test_scoring_and_helpers.py` | 33 | 评分算法、通用工具函数 |
| `test_qweather_client.py` | 22 | 和风天气数据解析与容错 |
| `test_chat_history_service.py` | 21 | 会话分组、消息落库 |
| `test_home_service.py` | 19 | 首页聚合、提醒生成 |
| `test_amap_routing.py` | 18 | 高德路线规划 |
| `test_article_extractor.py` | 17 | 新闻正文抽取与质量判定 |
| `test_clean_engine.py` | 17 | 数据清洗引擎 |
| `test_weather_helpers.py` | 14 | 风向换算、数值容错、时间解析 |
| `test_agent_intent.py` | 13 | AI 意图识别 |
| `test_itinerary_reminder.py` | 13 | 行程 + 天气提醒联动 |
| `test_route_planner.py` | 13 | 出行方案编排 |
| `test_amap_client.py` | 12 | 高德地理编码、地点联想 |
| `test_outfit_engine.py` | 12 | 穿衣规则（含上面说的 Bug 1） |
| `test_web_search_service.py` | 12 | 联网搜索封装 |

### 后端接口测试（78 个，5 个文件）

| 文件 | 数量 | 测什么 |
| --- | --- | --- |
| `test_notes.py` | 24 | 行程笔记增删改查、导入导出、越权访问 |
| `test_itinerary.py` | 17 | 行程增删改查、鉴权边界 |
| `test_auth.py` | 13 | 注册、登录、token 校验、权限 |
| `test_feedback.py` | 13 | 提交反馈、匿名提交、管理员回复 |
| `test_home.py` | 11 | 首页数据聚合 |

### 前端测试（63 个）

| 文件 | 数量 | 层次 |
| --- | --- | --- |
| `utils/weather.test.ts` | 9 | 单测 |
| `api/http.test.ts` | 14 | 单测 |
| `components/ForecastList.test.tsx` | 13 | 组件 |
| `components/WeatherHero.test.tsx` | 5 | 组件 |
| `e2e/auth.spec.ts` | 4 | E2E |
| `e2e/home.spec.ts` | 6 | E2E |
| `e2e/itinerary.spec.ts` | 5 | E2E |
| `e2e/feedback.spec.ts` | 4 | E2E |
| `e2e/chat.spec.ts` | 3 | E2E |

---

## 附录 B：一分钟速查

```
测试是什么      → 给代码雇的质检员：给定输入 → 跑代码 → 断言结果
为什么要写      → 改完不用手工重测、不敢碰的老代码变敢改、用户先发现问题变成机器先发现
三层金字塔      → 单元(多/快/便宜) → 接口 → E2E(少/慢/真实)
Fixture         → 测试前的预备工作（如"先造个已登录用户"）
Mock            → 假装外部服务存在（不真调 API，不烧钱，结果稳定）
覆盖率          → 代码被测试执行过的百分比；61% 及格；100% 不一定好
本项目的测试    → 后端 pytest 379 + 前端 Vitest 41 + Playwright 22 = 442 个
抓到过的真 Bug  → 雷阵雨拿不到防雷提示 / 中雨转小雨显示成小雨 / 登录报错显示英文
```

---

**相关文档**：

- 详细技术实现与踩坑全过程 → `工作日志.md` 的 Day 29 / Day 30
- 测试相关的深度知识点（测试金字塔原理、Playwright 选择器策略等）→ `知识点剖析.md` 第 29 / 30 节
- 后续计划：Day 31 会把测试接入 CI，每次提交自动跑这 442 个用例
