# 基于气象大数据的出行推荐系统 · 28 天逐日开发计划

> 修订版说明：对话模型采用「多提供商抽象层」，默认 DeepSeek，可通过配置切换通义千问 / 智谱 GLM / OpenAI / Moonshot / Ollama 等 OpenAI 兼容模型；向量维度固定 `Vector(1024)`；Docker 仅编排 PostgreSQL + Redis。
> 推进原则：**每天只完成对应天数任务，禁止超前开发未排期模块**；每阶段结束需输出 ✅ 检查清单，全部打勾才算完成。
> 使用方式：每天开发前打开对应 `Day N` 章节，完成后勾选 `- [x]`。

---

## 项目总览

| 维度 | 内容 |
| --- | --- |
| 落地场景 | 广州市气象大数据 + 3D 地理可视化 + AI 智能体 |
| 前端 | C 端 React19 + Vite + TS；B 端 Vue3 + Vite + TS |
| 后端 | Python FastAPI + SQLAlchemy 2.0 异步 ORM |
| 数据库 | PostgreSQL 17 + TimescaleDB + pgvector(1024) + PostGIS + Redis |
| AI | LangGraph + MCP + Skill + RAG；多模型对话（DeepSeek 等 OpenAI 兼容模型，默认 deepseek）+ 智谱 embedding-3 |
| 任务队列 | Celery + Redis |
| 交付目标 | 28 天 MVP 可运行版本，可写简历 + 面试演示 |

### 关键约束
- 各模型 API Key **仅存后端 `.env`**，禁止硬编码 / 暴露前端 / 提交 git。
- 对话模型仅接入「OpenAI 兼容格式」提供商（DeepSeek / Qwen / GLM / OpenAI / Moonshot / Ollama），不引入 Anthropic Claude 等不兼容适配器。
- 知识库向量字段固定 `Vector(1024)`，模型 `智谱 embedding-3`（dimensions=1024）。
- 所有 LLM/Embedding 调用必须 `try-except` 捕获网络异常、限流、超时，返回友好降级提示。
- 控制知识库文档数量，避免产生过多 API 费用。

---

## 第一周：项目基建与数据底座（Day 1‑7）
**里程碑**：完成全栈脚手架、数据库设计、气象数据接入与清洗能力，跑通「数据导入 → 清洗 → 入库 → 接口查询」全链路。

### Day 1 · 环境与脚手架
**模块**：环境与脚手架
**目标**：前后端脚手架全部可运行；数据库容器正常；三大扩展启用成功；`.env` 配置完成。

#### 具体任务
- [ ] 搭建总工程目录，git 初始化，编写 `.gitignore`
- [ ] 创建 React19 + Vite 用户端、Vue3 + Vite 管理端脚手架，保证可独立启动
- [ ] 搭建 FastAPI 后端基础目录骨架，最小 demo 可访问 `/docs`
- [ ] 使用修订版 `docker-compose.yml`，启动 postgres、redis 容器
- [ ] psql 启用 `timescaledb`、`vector`、`postgis` 三个扩展
- [ ] 后端新建 `.env`，填入 DeepSeek API Key、数据库、Redis 环境变量

#### 每日检查清单
- [ ] 前后端脚手架可独立运行
- [ ] `/docs` 可访问
- [ ] postgres / redis 容器正常启动
- [ ] 三大扩展 `CREATE EXTENSION` 成功
- [ ] `.env` 完成且已加入 `.gitignore`

---

### Day 2 · 数据库设计
**模块**：数据库设计
**目标**：所有数据表在 PostgreSQL 创建成功；向量字段维度为 1024；超表创建完成。

#### 具体任务
- [x] 编写 SQLAlchemy 异步会话、数据库连接配置
- [x] 编写全部数据模型：用户、资讯、反馈、气象时序、3D 地标
- [x] 编写知识库向量表（embedding = `Vector(1024)`）
- [x] 完成表迁移
- [x] 将 `weather_history` 时序表转为 TimescaleDB 超表

#### 每日检查清单
- [x] 所有数据表创建成功
- [x] 向量字段维度确认为 1024
- [x] TimescaleDB 超表创建完成

---

### Day 3 · 基础业务接口
**模块**：基础业务接口
**目标**：Swagger 可访问；鉴权生效；数据可正常读写入库。

#### 具体任务
- [x] 后端封装统一响应体、全局异常处理、JWT 鉴权中间件
- [x] 完成用户、资讯公告、用户反馈三大模块 CRUD 接口
- [x] 通过 Swagger 调试全部基础接口

#### 每日检查清单
- [x] Swagger 可访问且接口列表完整
- [x] JWT 鉴权生效（未携带 token 被拦截）
- [x] 用户 / 资讯 / 反馈数据可正常读写

---

### Day 4 · 气象数据源接入
**模块**：气象数据源接入
**目标**：可拉取广州天气；定时任务正常执行，时序表有数据写入。

#### 具体任务
- [x] 对接 Open-Meteo / 和风天气，封装广州实时、7 天预报、预警拉取工具
- [x] 数据写入气象时序表
- [x] 配置 Celery + Redis，实现每小时自动同步气象数据定时任务

#### 每日检查清单
- [x] 可拉取广州实时天气 + 7 天预报 + 预警
- [x] 气象时序表有数据写入
- [x] Celery 定时任务正常执行

---

### Day 5 · 数据清洗模块
**模块**：数据清洗模块
**目标**：上传测试 CSV，可异步完成清洗入库；可查看任务日志。

#### 具体任务
- [x] 基于 Pandas 实现去重、缺失填充、异常过滤、单位标准化
- [x] 开发 CSV 原始文件上传接口，封装 Celery 异步清洗任务
- [x] 管理端清洗任务触发、日志查询接口

#### 每日检查清单
- [x] 上传 CSV 可异步完成清洗并入库
- [x] 可查看清洗任务日志
- [x] 清洗规则（去重 / 填充 / 异常过滤 / 单位标准化）生效

---

### Day 6 · RAG 知识库底座（调用 DeepSeek Embedding）
**模块**：RAG 知识库底座
**目标**：输入问题，可返回 Top5 相关知识库片段；向量入库无维度报错。

#### 具体任务
- [ ] 整理 30‑50 篇知识库文档（气象、广州出行、穿搭）
- [ ] 封装文档加载、重叠分块；调用 `智谱 embedding-3`（dimensions=1024）生成 1024 维向量，存入 pgvector
- [ ] 开发向量语义检索接口，实现余弦相似度检索

#### 每日检查清单
- [ ] 知识库文档已向量化入库
- [ ] 向量维度为 1024，无报错
- [ ] 输入问题可返回 Top5 相关片段

---

### Day 7 · 第一周联调收尾
**模块**：第一周联调收尾
**目标**：数据链路无阻塞；底座稳定，可进入 AI 模块开发。

#### 具体任务
- [x] 全链路验证：原始数据 → 清洗 → 入库 → 接口查询
- [x] 修复环境、数据库、接口 bug
- [x] 记录 DeepSeek API 调用异常捕获、超时配置

#### 每日检查清单
- [x] 数据链路全贯通无阻塞
- [x] 第一周遗留 bug 已修复
- [x] DeepSeek API 异常 / 超时处理已落地

---

## 第二周：AI 核心引擎开发（Day 8‑14）
**里程碑**：跑通 LangGraph Agent 全链路，实现出行规划、穿搭推荐两大核心 Skill，支持 SSE 流式对话。

### Day 8 · LangGraph Agent 框架
**模块**：LangGraph Agent 框架
**目标**：可正常调用对话模型 API；状态机节点流转正常；可拿到大模型返回结果；支持多模型提供商切换。

#### 具体任务
- [x] 封装 LLM 客户端，基于 `langchain-openai` 适配器实现「多提供商抽象层」（DeepSeek / Qwen / GLM / OpenAI / Moonshot / Ollama），默认 DeepSeek，`settings.LLM_PROVIDER` 一键切换
- [x] 搭建 LangGraph 状态机，定义 AgentState 会话状态
- [x] 实现意图识别、LLM 生成节点，完成基础单轮对话调用

#### 每日检查清单
- [x] 对话模型 API 调用成功
- [x] 状态机节点流转正常
- [x] 单轮对话可拿到模型返回
- [x] 多模型提供商切换可用（`get_llm(provider)` 动态实例化 + 缓存）

---

### Day 9 · MCP 工具层封装
**模块**：MCP 工具层封装
**目标**：提问"广州今天天气"，Agent 自动调用 MCP 天气工具并返回结果。

#### 具体任务
- [x] 基于 MCP Python SDK 搭建 MCP Server；封装天气查询、地点查询、资讯检索工具
- [x] LangGraph 接入 MCP 客户端，实现工具自动识别、参数提取、调用执行
- [x] 测试 Agent 自动选择工具执行

#### 每日检查清单
- [x] MCP Server 启动正常
- [x] Agent 能自动识别并调用工具
- [x] 天气类问题返回工具真实结果

---

### Day 10 · RAG 接入 + 自动决策逻辑
**模块**：RAG 接入 + 自动决策逻辑
**目标**：完整链路：用户提问 → 意图识别 → RAG 检索 → MCP 工具调用 → LLM 生成答案。

#### 具体任务
- [x] 把 RAG 向量检索封装为 Agent 节点，问答前执行知识库检索增强
- [x] 完善 ReAct 推理逻辑：判断是否调用工具、是否反问用户、是否直接回答
- [x] 增加 API 调用失败、超时降级策略

#### 每日检查清单
- [x] RAG 检索节点接入 Agent
- [x] ReAct 决策逻辑完整（工具 / 反问 / 直答）
- [x] 异常降级策略生效

---

### Day 10 追加 · 联网搜索 + 用户私有知识库（需求变更）
**模块**：联网搜索 + 多租户 RAG
**目标**：Agent 支持联网搜索实时信息；用户可上传个人出行计划，实现个性化知识库检索。
> 说明：用户追加需求，原 Day 11 出行规划 Skill 顺延。

#### 具体任务
- [x] 封装联网搜索工具（Tavily 优先 + DuckDuckGo 兜底），注册为 web_search MCP 工具
- [x] 新建 user_knowledge 表（user_id 隔离），实现上传/检索/列表/删除接口
- [x] 私有检索封装为 Agent 本地工具 search_my_plans（contextvar 透传 user_id）
- [x] 端到端测试：登录用户检索个人计划、匿名用户隔离、公共问答不受影响

#### 每日检查清单
- [x] web_search 联网搜索工具可用（待配 Tavily Key）
- [x] 用户私有知识库上传 + 检索跑通
- [x] 多租户数据隔离生效（匿名用户无法访问他人数据）

---

### Day 11 · 出行规划 Skill 开发
**模块**：出行规划 Skill 开发
**目标**：提问"明天上午从广州南站去广州塔怎么出行"返回多套带天气提示方案。

#### 具体任务
- [x] 接入高德地图开放 API，封装路线规划工具（地理编码 + 路线规划 + 无 Key 兜底）
- [x] 封装出行规划 Skill，融合气象 + 路线做多维度评分
- [x] Agent 调用 Skill 输出带天气提示的多套出行方案

#### 每日检查清单
- [x] 地图路线规划工具可用
- [x] 出行 Skill 融合气象 + 路线评分
- [x] 返回多套带天气提示方案

---

### Day 12 · 穿搭推荐 Skill 开发
**模块**：穿搭推荐 Skill 开发
**目标**：提问"明天户外爬山适合穿什么"返回贴合场景穿搭建议。

#### 具体任务
- [x] 完善穿搭知识库规则
- [x] 封装穿搭推荐 Skill，读取气象参数结合知识库生成建议
- [x] 支持用户补充偏好，动态调整推荐

#### 每日检查清单
- [x] 穿搭知识库规则完善
- [x] 穿搭 Skill 结合气象参数生成建议
- [x] 支持用户偏好动态调整

---

### Day 13 · SSE 流式 + 多轮对话
**模块**：SSE 流式 + 多轮对话
**目标**：前端对话页打字机流式输出；多轮对话上下文连贯；API 报错有友好提示。

#### 具体任务
- [x] 基于 sse-starlette 实现 DeepSeek token 流式推送
- [x] 对话历史持久化，多轮上下文记忆
- [x] 网络 / API 异常兜底降级

#### 每日检查清单
- [x] SSE 流式 token 推送正常
- [x] 多轮对话上下文连贯
- [x] API 异常有友好降级提示

---

### Day 14 · AI Demo 验证 + 优化
**模块**：AI Demo 验证 + 优化
**目标**：核心场景回答准确流畅；流式正常；AI 模块可独立演示。

#### 具体任务
- [x] 完整测试核心场景："明天下午去广州塔玩，适合穿什么，怎么去最方便"
- [x] 调优提示词，控制 API 超时、重试逻辑
- [x] 输出可独立演示 AI Demo

#### 每日检查清单
- [x] 核心场景回答准确流畅
- [x] 流式输出正常
- [x] AI 模块可独立演示

---

## 第三周：前端开发与全链路联调（Day 15‑21）
**里程碑**：完成用户端、管理端核心页面，前后端全链路打通，产出可运行 MVP 版本。

### Day 15 · 用户端框架 + AI 对话页
**模块**：用户端框架 + AI 对话页
**目标**：首页展示天气；对话页可以和 AI 完成流式问答。

#### 具体任务
- [x] React 用户端布局、导航、路由、全局样式
- [x] 首页天气卡片，对接气象预报接口
- [x] AI 对话页面，EventSource 对接 SSE 流式接口

#### 每日检查清单
- [x] 首页天气卡片正常展示
- [x] 路由 / 导航可用
- [x] AI 对话页流式问答正常

---

### Day 16 · 3D 天气可视化基础
**模块**：3D 天气可视化基础
**目标**：页面加载 3D 场景；天气切换自动切换粒子特效。

#### 具体任务
- [ ] @react-three/fiber 搭建 3D 简易广州场景底板
- [ ] 实现晴天、雨天粒子特效
- [ ] 3D 场景与实时天气数据联动

#### 每日检查清单
- [ ] 3D 场景加载成功
- [ ] 晴 / 雨粒子特效可用
- [ ] 特效随实时天气联动

---

### Day 17 · 3D 特效深化 + 交互
**模块**：3D 特效深化 + 交互
**目标**：5 类天气特效可用；时间轴交互正常。

#### 具体任务
- [ ] 补充多云、雾、高温天气特效
- [ ] 3D 空间数据标注（温度、湿度、风力）
- [ ] 7 天预报时间轴，切换日期同步更新场景

#### 每日检查清单
- [ ] 5 类天气特效可用
- [ ] 空间数据标注正常
- [ ] 时间轴切换同步场景

---

### Day 18 · 用户端业务页面
**模块**：用户端业务页面
**目标**：用户端全部业务页面完成，接口联调正常。

#### 具体任务
- [ ] 气象资讯列表 + 详情页，分类筛选
- [ ] 用户反馈提交表单页面
- [ ] 出行、穿搭推荐结果展示页面

#### 每日检查清单
- [ ] 资讯列表 / 详情 / 筛选可用
- [ ] 反馈表单可提交
- [ ] 推荐结果页正常展示

---

### Day 19 · 管理端核心功能（上）
**模块**：管理端核心功能（上）
**目标**：管理端登录正常；两大模块功能完整。

#### 具体任务
- [ ] Vue3 管理端登录、布局、侧边导航、权限
- [ ] 气象大数据管理页面：列表筛选、批量操作、导出
- [ ] 资讯公告管理增删改查

#### 每日检查清单
- [ ] 登录 + 权限控制正常
- [ ] 气象数据管理（筛选 / 批量 / 导出）可用
- [ ] 资讯公告 CRUD 完整

---

### Day 20 · 管理端核心功能（下）
**模块**：管理端核心功能（下）
**目标**：管理端核心业务全部可用，统计图表渲染正常。

#### 具体任务
- [ ] 用户反馈管理，状态流转、回复功能
- [ ] 气象时序统计分析，ECharts 时序图表
- [ ] 原始数据清洗管理页面，任务提交、日志查看

#### 每日检查清单
- [ ] 反馈状态流转 + 回复可用
- [ ] ECharts 时序图表渲染正常
- [ ] 清洗任务提交 / 日志查看可用

---

### Day 21 · 全链路联调 + MVP 产出
**模块**：全链路联调 + MVP 产出
**目标**：产出可运行 MVP；全部核心功能可演示，可写简历。

#### 具体任务
- [ ] 用户端完整流程：浏览天气 ‑ AI 对话 ‑ 看资讯 ‑ 提交反馈
- [ ] 管理端完整流程：数据管理 ‑ 清洗 ‑ 统计 ‑ 反馈处理
- [ ] 修复核心 bug

#### 每日检查清单
- [ ] 用户端全流程跑通
- [ ] 管理端全流程跑通
- [ ] 核心 bug 已修复，MVP 可运行

---

## 第四周：高级功能 + 优化 + 简历包装（Day 22‑28）
**里程碑**：补全 3D 地图配置，性能优化，容器化，输出简历与演示素材。

### Day 22 · 管理端 3D 地图配置（上）
**模块**：管理端 3D 地图配置（上）
**目标**：地图正常加载；可增删拖拽地标，数据存入数据库。

#### 具体任务
- [ ] 管理端接入 CesiumJS，加载广州 3D 地图底座
- [ ] 地标点添加、拖拽编辑；对接后端 landmark 地标接口

#### 每日检查清单
- [ ] CesiumJS 广州 3D 地图加载成功
- [ ] 地标可增删 / 拖拽，数据入库

---

### Day 23 · 管理端 3D 地图配置（下）
**模块**：管理端 3D 地图配置（下）
**目标**：3D 地图配置模块完整可用；用户端同步展示地标。

#### 具体任务
- [ ] 模型上传、图层显隐、视角保存
- [ ] 用户端 3D 场景读取管理端配置地标数据展示

#### 每日检查清单
- [ ] 模型上传 / 图层显隐 / 视角保存可用
- [ ] 用户端同步展示配置地标

---

### Day 24 · 性能与体验优化
**模块**：性能与体验优化
**目标**：首屏、接口响应明显改善；AI-API 网络异常有友好降级。

#### 具体任务
- [ ] 前端：骨架屏、懒加载、按需引入，首屏优化
- [ ] 后端：Redis 缓存高频气象预报、热点资讯；增加 DeepSeek API 请求重试、超时控制
- [ ] 全局异常、加载、错误提示完善

#### 每日检查清单
- [ ] 首屏 + 接口响应明显改善
- [ ] Redis 缓存生效
- [ ] API 重试 / 超时 / 降级完善

---

### Day 25 · 容器化部署
**模块**：容器化部署
**目标**：`docker-compose up` 一键拉起整套业务系统，功能正常。

#### 具体任务
- [ ] 编写 React / Vue 前端 Nginx Dockerfile；FastAPI 后端 Dockerfile；Celery worker 容器配置
- [ ] 完善 docker-compose 编排所有业务容器（不含 ollama）
- [ ] 本地完整部署验证

#### 每日检查清单
- [ ] 前端 / 后端 / Celery 镜像构建成功
- [ ] docker-compose 编排完整
- [ ] 一键部署功能正常

---

### Day 26 · 简历素材输出
**模块**：简历素材输出
**目标**：简历项目定稿；截图、演示视频齐全。

#### 具体任务
- [ ] 按 STAR 法则写简历项目描述（重点：DeepSeek API、LangGraph、MCP、RAG-pgvector、多模 PostgreSQL）
- [ ] 截取系统截图；录制 1‑2 分钟演示视频

#### 每日检查清单
- [ ] 简历项目描述定稿
- [ ] 系统截图齐全
- [ ] 演示视频完成

---

### Day 27 · 技术文档 & 面试准备
**模块**：技术文档 & 面试准备
**目标**：文档完整；准备好面试口述素材。

#### 具体任务
- [ ] 编写 README：项目介绍、技术栈、部署启动步骤、架构说明
- [ ] 整理面试高频问题：API 调用、Agent 流程、MCP、pgvector+TimescaleDB+PostGIS 多模架构、RAG 流程、异常降级策略
- [ ] 整理后续迭代功能清单

#### 每日检查清单
- [ ] README 完整
- [ ] 面试高频问题清单完成
- [ ] 迭代功能清单整理完成

---

### Day 28 · 最终收尾
**模块**：最终收尾
**目标**：MVP 最终版本交付，随时可演示、投递。

#### 具体任务
- [ ] 全流程回归测试，修复剩余 bug
- [ ] 整理代码、补充注释
- [ ] 项目归档，制定秋招后迭代计划

#### 每日检查清单
- [ ] 全流程回归通过
- [ ] 代码整理 + 注释补充完成
- [ ] 项目归档，迭代计划制定

---

## 进度记录
> 在每天完成时，将对应 Day 的检查清单勾选 `- [x]`，并在下方记录日期与备注。

| Day | 日期 | 状态 | 备注 |
| --- | --- | --- | --- |
| Day 1 | | ⬜ | |
| Day 2 | 2026-08-23 | ✅ | 完成：6张业务表、Vector(1024)、weather_history 超表 |
| Day 3 | 2026-08-24 | ✅ | 完成：统一响应/全局异常/JWT/用户·资讯·反馈 CRUD |
| Day 4 | 2026-08-24 | ✅ | 完成：Open-Meteo数据源/时序表写入/Celery每小时定时同步 |
| Day 5 | 2026-08-24 | ✅ | 完成：Pandas清洗引擎/CSV上传Celery异步/任务日志接口 |
| Day 6 | 2026-08-24 | ✅ | 完成：RAG知识库底座（智谱embedding-3真实向量/pgvector入库/余弦检索Top5），16篇文档33块 |
| Day 7 | 2026-08-24 | ✅ | 完成：全链路联调10项通过/清洗链路7项通过/补默认admin账号/记录API异常超时配置 |
| Day 8 | 2026-08-25 | ✅ | 完成：LangGraph Agent 框架（LLM客户端/状态机/意图识别+生成节点），真实DeepSeek单轮对话跑通 |
| Day 9 | | ⬜ | |
| Day 10 | | ⬜ | |
| Day 11 | | ⬜ | |
| Day 12 | | ⬜ | |
| Day 13 | 2026-08-27 | ✅ | 完成：SSE流式(sse-starlette逐token推送+意图先行)、chat_messages表多轮历史持久化、多轮上下文连贯（省略主语仍理解）、异常兜底 |
| Day 14 | 2026-08-27 | ✅ | 完成：复合场景多工具协同（天气+穿搭+路线三工具联动）、提示词优化（复合问题逐一调工具+默认出发地兜底）、AI演示脚本 |
| Day 15 | 2026-08-27 | ✅ | 完成：Ant Design5+React Router用户端框架（导航/路由/预留入口）、首页天气卡片+7天预报、AI对话页SSE流式（fetch+ReadableStream） |
| Day 16 | | ⬜ | |
| Day 17 | | ⬜ | |
| Day 18 | | ⬜ | |
| Day 19 | | ⬜ | |
| Day 20 | | ⬜ | |
| Day 21 | | ⬜ | |
| Day 22 | | ⬜ | |
| Day 23 | | ⬜ | |
| Day 24 | | ⬜ | |
| Day 25 | | ⬜ | |
| Day 26 | | ⬜ | |
| Day 27 | | ⬜ | |
| Day 28 | | ⬜ | |

---

## 遗留问题跟踪

### Day 7 遗留问题收尾（2026-08-24 解决）
| 遗留项 | 状态 | 解决方案 |
| --- | --- | --- |
| Celery 无自启机制 | ✅ 已解决 | 新增 `backend/start_celery.ps1`（Windows 一键启动）；`docker-compose.yml` 新增 `celery` service（全 docker 部署用） |
| 数据库 17 个垃圾测试账号 | ✅ 已解决 | 删除 `alice*/wtest*/ctest*` 等，保留 `admin`/`admin280517` |
| 前端未联调 | ⚠️ 部分解决 | 前端仍为脚手架（无业务页面，属 Day15+ 排期）；已配 `vite.config.ts` 的 `/api` 代理到后端 8000，两前端均 `build` 成功 |
