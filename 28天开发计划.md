# 基于气象大数据的出行推荐系统 · 28 天逐日开发计划

> 修订版说明：对话模型采用「多提供商抽象层」，默认 DeepSeek，可通过配置切换通义千问 / 智谱 GLM / OpenAI / Moonshot / Ollama 等 OpenAI 兼容模型；向量维度固定 `Vector(1024)`；Docker 仅编排 PostgreSQL + Redis。
> 推进原则：**每天只完成对应天数任务，禁止超前开发未排期模块**；每阶段结束需输出 ✅ 检查清单，全部打勾才算完成。
> 使用方式：每天开发前打开对应 `Day N` 章节，完成后勾选 `- [x]`。

---

## 项目总览

| 维度 | 内容 |
| --- | --- |
| 落地场景 | 广州市气象大数据 + 3D 地理可视化 + AI 智能体 |
| 前端 | C 端 React19 + Vite + TS；B 端 Vue3 + Vite + TS |
| 后端 | Python FastAPI + SQLAlchemy 2.0 异步 ORM |
| 数据库 | PostgreSQL 17 + TimescaleDB + pgvector(1024) + PostGIS + Redis |
| AI | LangGraph + MCP + Skill + RAG；多模型对话（DeepSeek 等 OpenAI 兼容模型，默认 deepseek）+ 智谱 embedding-3 |
| 任务队列 | Celery + Redis |
| 交付目标 | 28 天 MVP 可运行版本，可写简历 + 面试演示 |

### 关键约束
- 各模型 API Key **仅存后端 `.env`**，禁止硬编码 / 暴露前端 / 提交 git。
- 对话模型仅接入「OpenAI 兼容格式」提供商（DeepSeek / Qwen / GLM / OpenAI / Moonshot / Ollama），不引入 Anthropic Claude 等不兼容适配器。
- 知识库向量字段固定 `Vector(1024)`，模型 `智谱 embedding-3`（dimensions=1024）。
- 所有 LLM/Embedding 调用必须 `try-except` 捕获网络异常、限流、超时，返回友好降级提示。
- 控制知识库文档数量，避免产生过多 API 费用。

---

## 第一周：项目基建与数据底座（Day 1‑7）
**里程碑**：完成全栈脚手架、数据库设计、气象数据接入与清洗能力，跑通「数据导入 → 清洗 → 入库 → 接口查询」全链路。

### Day 1 · 环境与脚手架
**模块**：环境与脚手架
**目标**：前后端脚手架全部可运行；数据库容器正常；三大扩展启用成功；`.env` 配置完成。

#### 具体任务
- [ ] 搭建总工程目录，git 初始化，编写 `.gitignore`
- [ ] 创建 React19 + Vite 用户端、Vue3 + Vite 管理端脚手架，保证可独立启动
- [ ] 搭建 FastAPI 后端基础目录骨架，最小 demo 可访问 `/docs`
- [ ] 使用修订版 `docker-compose.yml`，启动 postgres、redis 容器
- [ ] psql 启用 `timescaledb`、`vector`、`postgis` 三个扩展
- [ ] 后端新建 `.env`，填入 DeepSeek API Key、数据库、Redis 环境变量

#### 每日检查清单
- [ ] 前后端脚手架可独立运行
- [ ] `/docs` 可访问
- [ ] postgres / redis 容器正常启动
- [ ] 三大扩展 `CREATE EXTENSION` 成功
- [ ] `.env` 完成且已加入 `.gitignore`

---

### Day 2 · 数据库设计
**模块**：数据库设计
**目标**：所有数据表在 PostgreSQL 创建成功；向量字段维度为 1024；超表创建完成。

#### 具体任务
- [x] 编写 SQLAlchemy 异步会话、数据库连接配置
- [x] 编写全部数据模型：用户、资讯、反馈、气象时序、3D 地标
- [x] 编写知识库向量表（embedding = `Vector(1024)`）
- [x] 完成表迁移
- [x] 将 `weather_history` 时序表转为 TimescaleDB 超表

#### 每日检查清单
- [x] 所有数据表创建成功
- [x] 向量字段维度确认为 1024
- [x] TimescaleDB 超表创建完成

---

### Day 3 · 基础业务接口
**模块**：基础业务接口
**目标**：Swagger 可访问；鉴权生效；数据可正常读写入库。

#### 具体任务
- [x] 后端封装统一响应体、全局异常处理、JWT 鉴权中间件
- [x] 完成用户、资讯公告、用户反馈三大模块 CRUD 接口
- [x] 通过 Swagger 调试全部基础接口

#### 每日检查清单
- [x] Swagger 可访问且接口列表完整
- [x] JWT 鉴权生效（未携带 token 被拦截）
- [x] 用户 / 资讯 / 反馈数据可正常读写

---

### Day 4 · 气象数据源接入
**模块**：气象数据源接入
**目标**：可拉取广州天气；定时任务正常执行，时序表有数据写入。

#### 具体任务
- [x] 对接 Open-Meteo / 和风天气，封装广州实时、7 天预报、预警拉取工具
- [x] 数据写入气象时序表
- [x] 配置 Celery + Redis，实现每小时自动同步气象数据定时任务

#### 每日检查清单
- [x] 可拉取广州实时天气 + 7 天预报 + 预警
- [x] 气象时序表有数据写入
- [x] Celery 定时任务正常执行

---

### Day 5 · 数据清洗模块
**模块**：数据清洗模块
**目标**：上传测试 CSV，可异步完成清洗入库；可查看任务日志。

#### 具体任务
- [x] 基于 Pandas 实现去重、缺失填充、异常过滤、单位标准化
- [x] 开发 CSV 原始文件上传接口，封装 Celery 异步清洗任务
- [x] 管理端清洗任务触发、日志查询接口

#### 每日检查清单
- [x] 上传 CSV 可异步完成清洗并入库
- [x] 可查看清洗任务日志
- [x] 清洗规则（去重 / 填充 / 异常过滤 / 单位标准化）生效

---

### Day 6 · RAG 知识库底座（调用 DeepSeek Embedding）
**模块**：RAG 知识库底座
**目标**：输入问题，可返回 Top5 相关知识库片段；向量入库无维度报错。

#### 具体任务
- [ ] 整理 30‑50 篇知识库文档（气象、广州出行、穿搭）
- [ ] 封装文档加载、重叠分块；调用 `智谱 embedding-3`（dimensions=1024）生成 1024 维向量，存入 pgvector
- [ ] 开发向量语义检索接口，实现余弦相似度检索

#### 每日检查清单
- [ ] 知识库文档已向量化入库
- [ ] 向量维度为 1024，无报错
- [ ] 输入问题可返回 Top5 相关片段

---

### Day 7 · 第一周联调收尾
**模块**：第一周联调收尾
**目标**：数据链路无阻塞；底座稳定，可进入 AI 模块开发。

#### 具体任务
- [x] 全链路验证：原始数据 → 清洗 → 入库 → 接口查询
- [x] 修复环境、数据库、接口 bug
- [x] 记录 DeepSeek API 调用异常捕获、超时配置

#### 每日检查清单
- [x] 数据链路全贯通无阻塞
- [x] 第一周遗留 bug 已修复
- [x] DeepSeek API 异常 / 超时处理已落地

---

## 第二周：AI 核心引擎开发（Day 8‑14）
**里程碑**：跑通 LangGraph Agent 全链路，实现出行规划、穿搭推荐两大核心 Skill，支持 SSE 流式对话。

### Day 8 · LangGraph Agent 框架
**模块**：LangGraph Agent 框架
**目标**：可正常调用对话模型 API；状态机节点流转正常；可拿到大模型返回结果；支持多模型提供商切换。

#### 具体任务
- [x] 封装 LLM 客户端，基于 `langchain-openai` 适配器实现「多提供商抽象层」（DeepSeek / Qwen / GLM / OpenAI / Moonshot / Ollama），默认 DeepSeek，`settings.LLM_PROVIDER` 一键切换
- [x] 搭建 LangGraph 状态机，定义 AgentState 会话状态
- [x] 实现意图识别、LLM 生成节点，完成基础单轮对话调用

#### 每日检查清单
- [x] 对话模型 API 调用成功
- [x] 状态机节点流转正常
- [x] 单轮对话可拿到模型返回
- [x] 多模型提供商切换可用（`get_llm(provider)` 动态实例化 + 缓存）

---

### Day 9 · MCP 工具层封装
**模块**：MCP 工具层封装
**目标**：提问"广州今天天气"，Agent 自动调用 MCP 天气工具并返回结果。

#### 具体任务
- [x] 基于 MCP Python SDK 搭建 MCP Server；封装天气查询、地点查询、资讯检索工具
- [x] LangGraph 接入 MCP 客户端，实现工具自动识别、参数提取、调用执行
- [x] 测试 Agent 自动选择工具执行

#### 每日检查清单
- [x] MCP Server 启动正常
- [x] Agent 能自动识别并调用工具
- [x] 天气类问题返回工具真实结果

---

### Day 10 · RAG 接入 + 自动决策逻辑
**模块**：RAG 接入 + 自动决策逻辑
**目标**：完整链路：用户提问 → 意图识别 → RAG 检索 → MCP 工具调用 → LLM 生成答案。

#### 具体任务
- [x] 把 RAG 向量检索封装为 Agent 节点，问答前执行知识库检索增强
- [x] 完善 ReAct 推理逻辑：判断是否调用工具、是否反问用户、是否直接回答
- [x] 增加 API 调用失败、超时降级策略

#### 每日检查清单
- [x] RAG 检索节点接入 Agent
- [x] ReAct 决策逻辑完整（工具 / 反问 / 直答）
- [x] 异常降级策略生效

---

### Day 10 追加 · 联网搜索 + 用户私有知识库（需求变更）
**模块**：联网搜索 + 多租户 RAG
**目标**：Agent 支持联网搜索实时信息；用户可上传个人出行计划，实现个性化知识库检索。
> 说明：用户追加需求，原 Day 11 出行规划 Skill 顺延。

#### 具体任务
- [x] 封装联网搜索工具（Tavily 优先 + DuckDuckGo 兜底），注册为 web_search MCP 工具
- [x] 新建 user_knowledge 表（user_id 隔离），实现上传/检索/列表/删除接口
- [x] 私有检索封装为 Agent 本地工具 search_my_plans（contextvar 透传 user_id）
- [x] 端到端测试：登录用户检索个人计划、匿名用户隔离、公共问答不受影响

#### 每日检查清单
- [x] web_search 联网搜索工具可用（待配 Tavily Key）
- [x] 用户私有知识库上传 + 检索跑通
- [x] 多租户数据隔离生效（匿名用户无法访问他人数据）

---

### Day 11 · 出行规划 Skill 开发
**模块**：出行规划 Skill 开发
**目标**：提问"明天上午从广州南站去广州塔怎么出行"返回多套带天气提示方案。

#### 具体任务
- [x] 接入高德地图开放 API，封装路线规划工具（地理编码 + 路线规划 + 无 Key 兜底）
- [x] 封装出行规划 Skill，融合气象 + 路线做多维度评分
- [x] Agent 调用 Skill 输出带天气提示的多套出行方案

#### 每日检查清单
- [x] 地图路线规划工具可用
- [x] 出行 Skill 融合气象 + 路线评分
- [x] 返回多套带天气提示方案

---

### Day 12 · 穿搭推荐 Skill 开发
**模块**：穿搭推荐 Skill 开发
**目标**：提问"明天户外爬山适合穿什么"返回贴合场景穿搭建议。

#### 具体任务
- [x] 完善穿搭知识库规则
- [x] 封装穿搭推荐 Skill，读取气象参数结合知识库生成建议
- [x] 支持用户补充偏好，动态调整推荐

#### 每日检查清单
- [x] 穿搭知识库规则完善
- [x] 穿搭 Skill 结合气象参数生成建议
- [x] 支持用户偏好动态调整

---

### Day 13 · SSE 流式 + 多轮对话
**模块**：SSE 流式 + 多轮对话
**目标**：前端对话页打字机流式输出；多轮对话上下文连贯；API 报错有友好提示。

#### 具体任务
- [x] 基于 sse-starlette 实现 DeepSeek token 流式推送
- [x] 对话历史持久化，多轮上下文记忆
- [x] 网络 / API 异常兜底降级

#### 每日检查清单
- [x] SSE 流式 token 推送正常
- [x] 多轮对话上下文连贯
- [x] API 异常有友好降级提示

---

### Day 14 · AI Demo 验证 + 优化
**模块**：AI Demo 验证 + 优化
**目标**：核心场景回答准确流畅；流式正常；AI 模块可独立演示。

#### 具体任务
- [x] 完整测试核心场景："明天下午去广州塔玩，适合穿什么，怎么去最方便"
- [x] 调优提示词，控制 API 超时、重试逻辑
- [x] 输出可独立演示 AI Demo

#### 每日检查清单
- [x] 核心场景回答准确流畅
- [x] 流式输出正常
- [x] AI 模块可独立演示

---

## 第三周：前端开发与全链路联调（Day 15‑21）
**里程碑**：完成用户端、管理端核心页面，前后端全链路打通，产出可运行 MVP 版本。

### Day 15 · 用户端框架 + AI 对话页
**模块**：用户端框架 + AI 对话页
**目标**：首页展示天气；对话页可以和 AI 完成流式问答。

#### 具体任务
- [x] React 用户端布局、导航、路由、全局样式
- [x] 首页天气卡片，对接气象预报接口
- [x] AI 对话页面，EventSource 对接 SSE 流式接口

#### 每日检查清单
- [x] 首页天气卡片正常展示
- [x] 路由 / 导航可用
- [x] AI 对话页流式问答正常

---

### Day 16 · 3D 天气可视化基础
**模块**：3D 天气可视化基础
**目标**：页面加载 3D 场景；天气切换自动切换粒子特效。

#### 具体任务
- [x] Three.js 搭建 3D 简易广州场景底板
- [x] 实现晴天、雨天粒子特效
- [x] 3D 场景与实时天气数据联动

#### 每日检查清单
- [x] 3D 场景加载成功
- [x] 晴 / 雨粒子特效可用
- [x] 特效随实时天气联动

---

### Day 17 · 3D 特效深化 + 交互
**模块**：3D 特效深化 + 交互
**目标**：5 类天气特效可用；时间轴交互正常。

#### 具体任务
- [ ] 补充多云、雾、高温天气特效
- [ ] 3D 空间数据标注（温度、湿度、风力）
- [ ] 7 天预报时间轴，切换日期同步更新场景

#### 每日检查清单
- [ ] 5 类天气特效可用
- [ ] 空间数据标注正常
- [ ] 时间轴切换同步场景

---

### Day 18 · 用户端业务页面
**模块**：用户端业务页面
**目标**：用户端全部业务页面完成，接口联调正常。

#### 具体任务
- [x] 气象资讯列表 + 详情页，分类筛选（News / NewsDetail，分类 Tab + 搜索 + 置顶 + 分页）
- [x] 用户反馈提交表单页面（Feedback 匿名可提交 + 类型标签）
- [x] 出行、穿搭推荐结果展示页面（Recommend 双 Tab，接 /recommend 结构化接口）
- [x] 附：用户端登录注册（Login + Profile + MainLayout 登录态 + token 自动携带）

#### 每日检查清单
- [x] 资讯列表 / 详情 / 筛选可用
- [x] 反馈表单可提交
- [x] 推荐结果页正常展示

---

### Day 19 · 管理端核心功能（上）
**模块**：管理端核心功能（上）
**目标**：管理端登录正常；两大模块功能完整。

#### 具体任务
- [x] Vue3 管理端登录、布局、侧边导航、权限（Vue3 + Element Plus + Pinia，路由守卫强制 admin）
- [x] 气象大数据管理页面：列表筛选、批量操作、导出（/weather/admin-list 分页筛选 + 多选批量导出 CSV）
- [x] 资讯公告管理增删改查（NewsManage 分类/搜索 + 新增编辑删除 + 置顶发布开关）

#### 每日检查清单
- [x] 登录 + 权限控制正常
- [x] 气象数据管理（筛选 / 批量 / 导出）可用
- [x] 资讯公告 CRUD 完整

---

### Day 20 · 管理端核心功能（下）
**模块**：管理端核心功能（下）
**目标**：管理端核心业务全部可用，统计图表渲染正常。

#### 具体任务
- [x] 用户反馈管理，状态流转、回复功能（feedback 表加 reply/reply_at + FeedbackManage 状态流转 + 回复）
- [x] 气象时序统计分析，ECharts 时序图表（/weather/stats 按天聚合 + WeatherStat 双轴折线图）
- [x] 原始数据清洗管理页面，任务提交、日志查看（CleanManage 上传 + 日志终端样式 + 下载）

#### 每日检查清单
- [x] 反馈状态流转 + 回复可用
- [x] ECharts 时序图表渲染正常
- [x] 清洗任务提交 / 日志查看可用

---

### Day 21 · 全链路联调 + MVP 产出
**模块**：全链路联调 + MVP 产出
**目标**：产出可运行 MVP；全部核心功能可演示，可写简历。

#### 具体任务
- [ ] 用户端完整流程：浏览天气 ‑ AI 对话 ‑ 看资讯 ‑ 提交反馈
- [ ] 管理端完整流程：数据管理 ‑ 清洗 ‑ 统计 ‑ 反馈处理
- [ ] 修复核心 bug

#### 每日检查清单
- [ ] 用户端全流程跑通
- [ ] 管理端全流程跑通
- [ ] 核心 bug 已修复，MVP 可运行

---

## 第四周：高级功能 + 优化 + 简历包装（Day 22‑28）
**里程碑**：补全 3D 地图配置，性能优化，容器化，输出简历与演示素材。

### Day 22 · 管理端 3D 地图配置（上）
**模块**：管理端 3D 地图配置（上）
**目标**：地图正常加载；可增删拖拽地标，数据存入数据库。

#### 具体任务
- [ ] 管理端接入 CesiumJS，加载广州 3D 地图底座
- [ ] 地标点添加、拖拽编辑；对接后端 landmark 地标接口

#### 每日检查清单
- [ ] CesiumJS 广州 3D 地图加载成功
- [ ] 地标可增删 / 拖拽，数据入库

---

### Day 23 · 管理端 3D 地图配置（下）
**模块**：管理端 3D 地图配置（下）
**目标**：3D 地图配置模块完整可用；用户端同步展示地标。

#### 具体任务
- [ ] 模型上传、图层显隐、视角保存
- [ ] 用户端 3D 场景读取管理端配置地标数据展示

#### 每日检查清单
- [ ] 模型上传 / 图层显隐 / 视角保存可用
- [ ] 用户端同步展示配置地标

---

### Day 24 · 性能与体验优化
**模块**：性能与体验优化
**目标**：首屏、接口响应明显改善；AI-API 网络异常有友好降级。

#### 具体任务
- [x] 前端：骨架屏、懒加载、按需引入，首屏优化
- [x] 后端：Redis 缓存高频气象预报、热点资讯；增加 DeepSeek API 请求重试、超时控制
- [x] 全局异常、加载、错误提示完善

#### 每日检查清单
- [x] 首屏 + 接口响应明显改善（用户端路由懒加载，页面按需拆 chunk；管理端 ECharts 按需引入）
- [x] Redis 缓存生效（`core/cache.py`，气象/资讯已接入，Redis 不可用时静默降级已验证）
- [x] API 重试 / 超时 / 降级完善（llm_client timeout=60s + max_retries=2，全链路降级）

---

### Day 25 · 容器化部署
**模块**：容器化部署
**目标**：`docker-compose up` 一键拉起整套业务系统，功能正常。

#### 具体任务
- [x] 编写 React / Vue 前端 Nginx Dockerfile；FastAPI 后端 Dockerfile；Celery worker 容器配置
- [x] 完善 docker-compose 编排所有业务容器（不含 ollama）
- [x] 本地完整部署验证

#### 每日检查清单
- [x] 前端 / 后端 / Celery 镜像构建成功
- [x] docker-compose 编排完整（postgres/redis/backend/celery/frontend-user/frontend-admin 六服务）
- [x] 一键部署功能正常

---

### Day 26 · 简历素材输出
**模块**：简历素材输出
**目标**：简历项目定稿；截图、演示视频齐全。

#### 具体任务
- [ ] 按 STAR 法则写简历项目描述（重点：DeepSeek API、LangGraph、MCP、RAG-pgvector、多模 PostgreSQL）
- [ ] 截取系统截图；录制 1‑2 分钟演示视频

#### 每日检查清单
- [ ] 简历项目描述定稿
- [ ] 系统截图齐全
- [ ] 演示视频完成

---

### Day 27 · 技术文档 & 面试准备
**模块**：技术文档 & 面试准备
**目标**：文档完整；准备好面试口述素材。

#### 具体任务
- [ ] 编写 README：项目介绍、技术栈、部署启动步骤、架构说明
- [ ] 整理面试高频问题：API 调用、Agent 流程、MCP、pgvector+TimescaleDB+PostGIS 多模架构、RAG 流程、异常降级策略
- [ ] 整理后续迭代功能清单

#### 每日检查清单
- [ ] README 完整
- [ ] 面试高频问题清单完成
- [ ] 迭代功能清单整理完成

---

### Day 28 · 最终收尾
**模块**：最终收尾
**目标**：MVP 最终版本交付，随时可演示、投递。

#### 具体任务
- [ ] 全流程回归测试，修复剩余 bug
- [ ] 整理代码、补充注释
- [ ] 项目归档，制定秋招后迭代计划

#### 每日检查清单
- [ ] 全流程回归通过
- [ ] 代码整理 + 注释补充完成
- [ ] 项目归档，迭代计划制定

---

## 第二阶段：增强与工程化（Day 29 起）

> **背景**：Day 1‑28 已完成 MVP——13 个路由模块 / **65 个接口** / 13 张表 /
> 1 个 LangGraph Agent + **7 个 MCP 工具** + 3 个 Skill / 6 容器一键部署。
>
> 第二阶段聚焦三件事：
> 1. **工程化补齐**——自动化测试、CI/CD、可观测性（当前 **0 个测试文件**，是最大短板）
> 2. **从「被动查询」到「主动服务」**——推送能力（天气类产品的命脉）
> 3. **AI 与技术深度**——多智能体协作、时序数据分析、混合检索

**状态标记**：⬜ 未开始 / 🔶 进行中 / ✅ 完成 / ⏸️ 暂缓

---

### 第五周：工程化基建（Day 29‑33）

#### Day 29 · 后端自动化测试体系
**模块**：测试基建
**目标**：核心链路具备回归保护，整体覆盖率 ≥ 60%。

##### 具体任务
- [x] 引入 `pytest` + `pytest-asyncio` + `pytest-cov` + `respx`（Mock HTTP）
- [x] 测试库隔离：独立测试库 `weather_db_test`（**不污染开发数据**）
- [x] **单元测试**：`amap_client`（坐标直传 / 地点联想）、`outfit_engine`（规则匹配）、
      `home_service`（提醒生成）、`chat_history_service`（会话分组）、`article_extractor`（正文质量判定）
- [x] **接口测试**：users / itinerary / notes / feedback / home 主流程 + 鉴权边界（401/403/409）
- [x] **外部依赖全 Mock**：和风、高德、Tavily、LLM，测试不依赖网络与 API Key
- [x] 覆盖率报告 + `pytest.ini` 配置（含 60% 红线）

##### 每日检查清单
- [x] `pytest` 全绿（379 用例通过）
- [x] 核心 Service 与 Router 均有对应用例（含异常分支）
- [x] 断网执行测试仍全绿（外部依赖全部打桩）

#### Day 30 · 前端测试 + E2E
**模块**：测试基建
**目标**：关键交互有测试保护，核心用户流程可自动回归。

##### 具体任务
- [x] `Vitest` + `@testing-library/react`：工具函数（100%）、API 拦截器（100%）、关键组件（ForecastList / WeatherHero），41 用例全绿
- [x] `Playwright` E2E：登录注册 / 首页天气 / AI 对话 / 行程增删改 / 反馈提交，22 用例覆盖 5 条核心路径
- [x] 测试数据隔离：每用例独立注册时间戳账号，可重复执行；行程用例收尾清理
- [x] 失败自动截图 + 录像 + trace（`only-on-failure`）；CI 无头模式配置就绪（E2E_BASE_URL 可指向测试环境）

> 附带修复 3 个真实缺陷：复合天气匹配错误（中雨转小雨→小雨）、
> 登录失败显示 axios 技术信息而非后端中文提示、登录页体验账号密码写错。
> 踩坑记录见工作日志 Day 30（antd 两字按钮空格 / Segmented 隐藏 radio / strict violation / IDE 删除劫持）

##### 每日检查清单
- [ ] 单测 + E2E 全部通过
- [ ] E2E 覆盖 5 条核心用户路径
- [ ] 失败截图与视频产物可查

#### Day 31 · CI 持续集成流水线
**模块**：自动化
**目标**：每次 push 自动检查，坏代码进不了主干。

##### 具体任务
- [x] GitHub Actions 工作流：`lint`（ruff + eslint）→ `test`（pytest 379 + vitest 41）→ `build`（4 个镜像 matrix 并行）
- [x] 后端缓存 pip、前端缓存 npm（按 lock 文件命中），缩短流水线耗时
- [x] PR 校验配置就绪 + 状态徽章写进 README
- [x] 提交前本地钩子（`pre-commit`：ruff / ruff-format / eslint / tsc 类型检查 / 大文件与冲突标记）

##### 每日检查清单
- [x] push 后流水线自动触发并全绿（总耗时 6 分 13 秒）
- [x] 徽章展示在 README
- [x] 错误代码能拦住（实测：pytest 失败时 job 红、need 其通过的镜像构建不再执行）

> 附带完成代码质量基线：ruff 204 个问题清零（自动修 197 + 手工 10）、
> 格式化 69 个文件；前端 eslint 从 12 error 降到 0。
> 排查记录见工作日志 Day 31：CI 日志需登录才能看，于是让 CI 把失败信息
> 输出成注解（未登录可读），据此定位到 `pytest` 与 `python -m pytest`
> 的 sys.path 差异（表现为退出码 4，极易误判为参数错误）。

#### Day 32 · CD 自动化部署
**模块**：自动化
**目标**：一条命令 / 一次合并即可完成线上更新，且可回滚。

##### 具体任务
- [x] 镜像推送至 GHCR（`ghcr.io/ticnix/weather-*`，内置 GITHUB_TOKEN 推送，零 Secret 配置）
- [x] 部署方式：`deploy.sh`（pull → up -d，无 Watchtower 的自动升级不可控风险）；
      标签策略 `latest` + `sha-<hash>`，回滚 = 换旧 tag 重跑同一条命令
- [x] 部署后健康检查：后端 `/health` + 用户端 + 管理端三入口，失败自动回滚上一版本
- [x] 环境区分：`ENV_FILE` 变量 + `backend/.env.prod.example` 模板
      （`.env.*` 已入 gitignore；本项目 CI/CD 无需任何 GitHub Secret，测试全打桩 + 内置 token）
- [x] 数据安全：部署前自动 `pg_dump`（停应用后取一致性快照）+ 完整性校验 + 保留 7 份

##### 每日检查清单
- [x] 自动部署全流程跑通（push → CI → GHCR → deploy.sh → 服务健康，
      全部容器运行在 ghcr.io/ticnix/* 镜像上）
- [x] 健康检查失败能触发回滚（`FORCE_HEALTHCHECK_FAIL=1` 演练：自动回滚 + 服务恢复，退出码 1）
- [x] 数据库备份文件生成且可恢复（恢复到临时库核对：users 35 行 / news 11 行无 SQL 错误）

#### Day 33 · 前端健壮性与可观测性
**模块**：稳定性
**目标**：异常不白屏，线上问题可定位。

##### 具体任务
- [x] 全局 `ErrorBoundary`（含重试按钮）+ 布局内页面级兜底（单页崩溃导航仍可用）
- [x] 统一请求失败兜底：http.ts 拦截器此前已按错误类型统一提示（401/超时/断网/业务错误），
      各页面已有 Skeleton——本次未重复建设，仅在文档中固化为规范
- [x] 后端结构化日志：JSON 单行 + request_id 贯穿（contextvars）+ method/path/route/status/耗时，
      关闭 uvicorn.access 重复日志
- [x] 健康检查增强：DB / Redis 独立检查（2s 超时），失败返回 503（与 deploy.sh 回滚语义联动）；
      外部 API **刻意不查**（第三方抖动不应误触发回滚）
- [x] 简易监控：`/metrics` 按路由模板聚合请求数/错误数/平均耗时/错误率
      （进程内计数，未引入 Prometheus/Sentry——当前需求几十行代码可满足）
- [x] Celery：任务失败不再吞异常（此前返回 ok:False 被 Celery 记为 SUCCESS）
      + `autoretry_for` 指数退避重试 ×3 + `task_failure` 信号全局告警（结构化日志）

##### 每日检查清单
- [x] 人为抛错不导致白屏（ErrorBoundary 测试 5 个用例覆盖兜底/重试/自愈）
- [x] 日志可按请求 ID 串起完整调用链（实测：JSON 日志含 request_id/method/path/status/duration_ms）
- [x] 健康检查能反映各依赖状态（/health 返回 db/redis 分项；DB 故障 → 503 有测试覆盖）

---

### 第六周：主动服务能力（Day 34‑39）

#### Day 34 · 通知基础设施
**模块**：推送
**目标**：具备向用户主动发消息的通道与订阅管理。

##### 具体任务
- [x] 通道落地：**Web Push（VAPID，pywebpush）** 为主 + 邮件 SMTP（配置驱动，未配置自动禁用）
- [x] 表设计：`push_subscriptions`（每设备一条，endpoint 唯一，失效自动清理）
      + `notification_logs`（全通道发送记录）；推送偏好留给 Day 39 订阅管理页
- [x] 前端订阅授权流程：用户手势触发 requestPermission → SW ready → subscribe →
      保存；denied 友好降级（引导改浏览器权限，不反复弹）；sw.js 处理 push 与点击跳转
- [x] 统一发送服务 `notification_service.notify_user()`：顺序走所有可用通道
      （AsyncSession 不支持 gather 并发——实测挂起），各通道互不影响
- [x] 失败处理：推送 404/410 自动清理失效订阅；瞬时失败重试一次；skipped≠failed 语义分离

##### 每日检查清单
- [x] 能向已订阅用户推送测试通知（`POST /notifications/test`，走全部可用通道并返回各通道结果）
- [x] 未授权/拒绝友好降级（权限被拒 → 引导改浏览器设置；不支持 → 明确告知环境限制）
- [x] 发送记录可查询（notification_logs 落库 + `GET /notifications/logs` + Profile 页最近记录）

#### Day 35 · 每日智能早报
**模块**：推送
**目标**：把首页仪表盘的能力「主动送达」用户。

##### 具体任务
- [x] Celery beat 每小时整点分发（按用户各自设定的 hour 过滤），复用 `home_service.build_dashboard`
- [x] 内容：今日天气 + 提醒（预警 danger 优先，最多 2 条）+ 穿搭建议 + **今日行程与天气冲突提示**
- [x] 无行程时给通用提示，避免空推送；天气源全挂时退化为纯提醒文本
- [x] 免打扰：`notification_prefs` 表（开关 + 推送小时 5~22 点），Profile 页可视化设置

##### 每日检查清单
- [x] 定时任务能生成并推送早报（`dispatch_morning_reports` + beat `crontab(minute="0")`）
- [x] 内容包含行程冲突提示（复用行程的 `weather_hint`，如「建议带雨具或考虑改期」）
- [x] 关闭开关后不再推送（`morning_enabled=False` 的用户分发时被过滤，有测试覆盖）

#### Day 36 · 天气预警实时推送
**模块**：推送
**目标**：预警一发布，相关用户立刻收到。

##### 具体任务
- [x] Celery 每 10 分钟轮询预警源；新增 `weather_alerts` 表 + `fingerprint` 去重键识别**新增**预警
- [x] 按城市匹配受影响用户：广州面向所有订阅者，异地只推给未来 7 天行程涉及该城市的用户
- [x] 站内实时通道：**SSE**（选它而非 WebSocket——纯服务端单向推送）+ Redis Pub/Sub 跨进程中转
- [x] 预警去重与冷却：fingerprint 命中的预警只刷新 `last_seen_at`，不再推送
- [x] 额外修复：和风预警接口属付费能力（免费 Key 403），加 Open-Meteo 阈值兜底，
      否则该功能在默认配置下永远不会触发

##### 每日检查清单
- [x] 新增预警能触发推送且不重复（单测覆盖「第二次轮询 new_alerts=0」；
      全链路集成验证：入库 → 实时收到 → 通知记录落库）
- [x] 前端能实时收到并弹出提醒（`AlertStreamListener`：站内通知 + 系统级通知双路；
      真实链路实测「Redis 发布 → SSE 秒级收到」）

#### Day 37 · 生活指数接入
**模块**：功能增强
**目标**：把已打通但闲置的和风 indices 接口用起来。

##### 具体任务
- [x] 接入和风 `/indices/1d`（实测免费 Key 有权限，一次返回 16 类指数）
- [x] 首页与仪表盘接口返回指数摘要（`dashboard.indices` + `/weather/indices` 三个接口）
- [x] 结合用户画像做个性化：新增 `users.body_preference`（怕冷/怕热）+ 近期行程活动，
      按相关度**排序**（不是筛选）；怕冷先看穿衣与感冒，有爬山行程先看运动与紫外线
- [x] 指数入库时序表 `life_indices`（唯一键 城市+日期+类型），`/indices/history` 支持历史对比

##### 每日检查清单
- [x] 首页展示 3 个以上生活指数（实测返回 4 项：舒适度 / 防晒 / 穿衣 / 紫外线）
- [x] 指数随天气变化正确更新（同日重复拉取走更新而非新插；
      实测当日广州小雨 → 洗车「不宜」、晾晒「不宜」、穿衣「炎热」，与天气一致）

> 安全修复：接入过程中发现 `PUT /api/v1/users/{user_id}` 无权限校验，
> 普通用户可把自己改成 admin；已修复并补 6 个回归用例。

#### Day 38 · PWA 能力
**模块**：体验
**目标**：可安装到手机桌面，弱网可用，补齐"没有 App"的短板。

##### 具体任务
- [x] `vite-plugin-pwa`：manifest + Service Worker，图标用纯 Python 手写 PNG 生成
      （含 maskable 安全区版本），无新增依赖
- [x] 静态资源预缓存（60 项）+ 接口 stale-while-revalidate（天气/首页），
      其余 GET 走 NetworkFirst，写操作一律不缓存
- [x] 添加到主屏幕引导（Chromium 用 `beforeinstallprompt`，iOS 给文字指引，可"暂不"）
- [x] 离线状态提示：做成应用内常驻横条而非静态离线页——
      断网时应用外壳本就能打开，用户需要的是"看到的是缓存"这个解释
- [x] 关键整合：推送脚本改为 `push-sw.js` 由生成的 SW `importScripts` 注入
      （一个 scope 只能有一个 SW），**并因此修好了一个悬着的问题——
      Day 34 的推送 SW 其实从未被注册过**

##### 每日检查清单
- [x] 浏览器可「安装应用」（manifest 字段齐全 + 192/512 图标可访问 + maskable + SW 接管）
- [x] 断网后仍能打开并看到缓存天气（E2E 实测：断网 reload 后导航与天气均在）
- [x] PWA 审计项通过（未跑 Lighthouse，改用浏览器 API 逐条核对可安装性条件，
      并以 E2E 固化，见 `e2e/pwa.spec.ts`）

#### Day 39 · 推送偏好与订阅管理页
**模块**：推送
**目标**：用户可自主管理推送，避免打扰。

##### 具体任务
- [x] 「我的」页通知设置：推送总开关 + 三类类型开关（早报 / 预警 / 行程提醒）+ 早报时间
- [x] 订阅状态展示与一键退订：**多设备列表**（每台设备独立退订），
      而不是只给一个「关闭推送」——那样用户不知道关的是哪台
- [x] 推送历史记录：带**类型标签**与被拦下原因（"用户已关闭「天气预警」"）
- [x] 偏好校验收口在唯一发送入口（`notify_user(category=...)`），
      被拦下也留 skipped 记录，让"为什么没收到"有答案
- [x] 补上真实发送方：**出发前 30 分钟行程提醒**——
      否则「行程提醒」开关背后没有发送方，是个假开关

##### 每日检查清单
- [x] 偏好设置生效并持久化（E2E 实测：关掉某类后刷新仍是关闭状态）
- [x] 退订后不再收到对应推送（订阅删除 + 类型开关双重拦截，各有用例覆盖）

---

### 第七周：AI 与技术深度（Day 40‑45）

#### Day 40 · AI 一键排行程
**模块**：AI
**目标**：自然语言描述需求，自动生成可执行的行程安排。

##### 具体任务
- [ ] 新增 `itinerary_planner` Skill：输入「想去哪 / 几天 / 偏好」→ 输出结构化行程
- [ ] 编排链：查天气（规避雨天户外）→ 景点检索（RAG + 地标库）→ 路线耗时 → 生成行程
- [ ] 支持"一键保存到我的行程"（调用现有 itinerary 接口）
- [ ] 前端：对话中给出可点击的行程卡片

##### 每日检查清单
- [ ] 「周末想去广州玩两天」能生成含天气规避的行程
- [ ] 生成结果可一键写入行程表
- [ ] 行程与真实天气不冲突（雨天不排户外）

#### Day 41 · 多智能体协作（Supervisor 架构）
**模块**：AI 架构
**目标**：把单 Agent 升级为多 Agent 协作，提升复杂问题处理质量。

##### 具体任务
- [ ] 拆分领域 Agent：天气 / 行程 / 穿搭 / 路线
- [ ] Supervisor 节点负责意图路由与结果汇总（LangGraph 条件边）
- [ ] 各 Agent 复用现有 MCP 工具，避免重复实现
- [ ] 对比单 Agent 的效果与延迟，量化收益

##### 每日检查清单
- [ ] 复合问题路由到正确 Agent
- [ ] 多 Agent 结果汇总连贯、无重复
- [ ] 输出架构图与对比数据

#### Day 42 · TimescaleDB 时序分析
**模块**：数据深度
**目标**：真正发挥时序库能力（当前仅当普通表用）。

##### 具体任务
- [ ] 建**连续聚合视图**（continuous aggregate）：日均温 / 极值 / 降水累计 / 湿度均值
- [ ] 压缩策略与保留策略配置
- [ ] 同比环比接口：「今年 9 月比去年同期 +1.2°C」
- [ ] 首页/管理端展示趋势图（ECharts）

##### 每日检查清单
- [ ] 连续聚合自动刷新
- [ ] 同比环比数据正确
- [ ] 图表展示趋势与异常

#### Day 43 · RAG 检索升级
**模块**：AI
**目标**：提升知识库召回质量。

##### 具体任务
- [ ] **混合检索**：pgvector 向量召回 + 全文检索（`tsvector`/BM25）双路合并
- [ ] **重排**：接入 Rerank 模型（或轻量交叉编码器）对候选精排
- [ ] 分块策略优化（语义分块 + 重叠窗口）
- [ ] 建评测集：20 条问题，对比升级前后命中率

##### 每日检查清单
- [ ] 评测集命中率有可量化提升
- [ ] 检索耗时无明显劣化

#### Day 44 · 行程分享
**模块**：功能
**目标**：行程可分享，形成传播点。

##### 具体任务
- [ ] 生成只读分享链接（带过期时间）
- [ ] 服务端渲染行程海报图（含天气/穿搭摘要）
- [ ] 分享落地页：无需登录可查看

##### 每日检查清单
- [ ] 链接可在未登录状态查看行程
- [ ] 海报图生成正确、风格统一

#### Day 45 · 用户画像与偏好学习
**模块**：个性化
**目标**：建议随使用越来越贴合个人。

##### 具体任务
- [ ] 记录用户行为（查看/采纳的穿搭、常用地点、出行方式偏好）
- [ ] 偏好画像表 + 更新策略（衰减权重）
- [ ] 推荐排序融入画像（如怕冷用户降温提醒更前）
- [ ] 「我的」页可查看与重置画像

##### 每日检查清单
- [ ] 行为能影响后续推荐排序
- [ ] 用户可查看并重置画像

---

### 第八周：可视化与收官（Day 46‑50）

#### Day 46 · 地图可视化
**模块**：可视化
**目标**：以地图替代此前暂缓的 3D，直观展示区域天气。

##### 具体任务
- [ ] 广州各区天气热力图（高德 JS API / ECharts 地图）
- [ ] 出行路线轨迹在地图上绘制
- [ ] 行程地点标注与天气气泡

##### 每日检查清单
- [ ] 地图可切换区域查看天气
- [ ] 路线与地点标注正确

#### Day 47 · 管理端数据大屏
**模块**：可视化
**目标**：运营数据一屏掌握。

##### 具体任务
- [ ] 核心指标卡片：DAU / 接口调用量 / AI 对话数 / 推送到达率
- [ ] 时序图表：天气同步成功率、任务执行情况
- [ ] 实时刷新（轮询或 SSE）

##### 每日检查清单
- [ ] 大屏数据准确、可自动刷新
- [ ] 异常指标有视觉突出

#### Day 48 · 性能压测与优化
**模块**：性能
**目标**：明确容量边界，优化瓶颈。

##### 具体任务
- [ ] `locust` / `k6` 压测核心接口（首页仪表盘、天气、AI 对话）
- [ ] 定位瓶颈（DB 查询 / 外部 API / LLM 延迟）并优化
- [ ] 缓存命中率统计与优化
- [ ] 输出压测报告（QPS / P95 延迟 / 瓶颈分析）

##### 每日检查清单
- [ ] 压测报告产出
- [ ] 关键接口 P95 达标
- [ ] 瓶颈项有优化前后对比

#### Day 49 · 文档与简历增量更新
**模块**：交付
**目标**：把第二阶段成果转化为可展示材料。

##### 具体任务
- [ ] README 更新（新增测试/CI/推送/多智能体等能力）
- [ ] 架构图更新（增加通知链路、多 Agent 拓扑）
- [ ] 简历与面试宝典增量：测试策略、CI/CD、多智能体验证、时序分析收益
- [ ] 补充技术难点与踩坑记录（工作日志）

##### 每日检查清单
- [ ] 文档与代码一致
- [ ] 简历可讲的新增亮点 ≥ 4 条

#### Day 50 · 最终回归与归档
**模块**：收官
**目标**：可长期维护、随时演示。

##### 具体任务
- [ ] 全流程回归（含推送、AI 排行程、PWA）
- [ ] 清理临时代码与脚本，统一代码风格
- [ ] 打 tag 归档，输出迭代总结
- [ ] 制定下一阶段方向（小程序 / 多城市 / 商业化探索）

##### 每日检查清单
- [ ] 回归全部通过
- [ ] 版本打 tag 并归档
- [ ] 迭代总结输出

---

### 优先级说明

若时间有限，按以下顺序推进（**性价比由高到低**）：

| 顺序 | 任务 | 理由 |
| --- | --- | --- |
| 1 | Day 29‑33 工程化基建 | 面试硬伤（0 测试、无 CI/CD），且是后续所有改动的地基 |
| 2 | Day 34‑35 通知 + 早报 | 补齐产品命脉，从"被动查询"变"主动服务" |
| 3 | Day 40 AI 一键排行程 | 演示效果最强，面试加分明显 |
| 4 | Day 42 TimescaleDB 时序分析 | 让技术选型"名副其实"，面试可深挖 |
| 5 | Day 41 多智能体协作 | 架构亮点，但耗时较长，可放后 |
| 6 | Day 36‑39 / 43‑50 | 体验与深度增强，按需选做 |

---

## 进度记录
> 在每天完成时，将对应 Day 的检查清单勾选 `- [x]`，并在下方记录日期与备注。

| Day | 日期 | 状态 | 备注 |
| --- | --- | --- | --- |
| Day 1 | | ⬜ | |
| Day 2 | 2026-08-23 | ✅ | 完成：6张业务表、Vector(1024)、weather_history 超表 |
| Day 3 | 2026-08-24 | ✅ | 完成：统一响应/全局异常/JWT/用户·资讯·反馈 CRUD |
| Day 4 | 2026-08-24 | ✅ | 完成：Open-Meteo数据源/时序表写入/Celery每小时定时同步 |
| Day 5 | 2026-08-24 | ✅ | 完成：Pandas清洗引擎/CSV上传Celery异步/任务日志接口 |
| Day 6 | 2026-08-24 | ✅ | 完成：RAG知识库底座（智谱embedding-3真实向量/pgvector入库/余弦检索Top5），16篇文档33块 |
| Day 7 | 2026-08-24 | ✅ | 完成：全链路联调10项通过/清洗链路7项通过/补默认admin账号/记录API异常超时配置 |
| Day 8 | 2026-08-25 | ✅ | 完成：LangGraph Agent 框架（LLM客户端/状态机/意图识别+生成节点），真实DeepSeek单轮对话跑通 |
| Day 9 | | ⬜ | |
| Day 10 | | ⬜ | |
| Day 11 | | ⬜ | |
| Day 12 | | ⬜ | |
| Day 13 | 2026-08-27 | ✅ | 完成：SSE流式(sse-starlette逐token推送+意图先行)、chat_messages表多轮历史持久化、多轮上下文连贯（省略主语仍理解）、异常兜底 |
| Day 14 | 2026-08-27 | ✅ | 完成：复合场景多工具协同（天气+穿搭+路线三工具联动）、提示词优化（复合问题逐一调工具+默认出发地兜底）、AI演示脚本 |
| Day 15 | 2026-08-27 | ✅ | 完成：Ant Design5+React Router用户端框架（导航/路由/预留入口）、首页天气卡片+7天预报、AI对话页SSE流式（fetch+ReadableStream） |
| Day 16 | 2026-08-27 | ✅ | 完成：原生Three.js搭建广州3D场景（地面+广州塔小蛮腰+建筑群）、5类天气粒子特效（晴/雨/云/雾/高温）、实时天气驱动联动 |
| Day 17 | - | ⏸️ | 暂缓：3D 板块已隐藏（前端 3D 代码/入口已移除），后续开发时再开启 |
| Day 18 | 2026-09-08 | ✅ | 完成：资讯列表/详情/筛选、反馈提交、出行/穿搭推荐展示页(Recommend双Tab)、附用户端登录注册；「我的行程」页此前遗漏，2026-09-14 补齐；资讯板块同日改造为真实数据自动采集（中央气象台预警 + 本地天气简报，Celery 每日 7:30/17:30） |
| Day 19 | 2026-09-08 | ✅ | 完成：Vue3管理端登录+布局+权限守卫、气象大数据管理(筛选/批量导出)、资讯公告CRUD、附天气同步页 |
| Day 20 | 2026-09-08 | ✅ | 完成：反馈状态流转+回复(feedback加reply列)、ECharts时序统计(/weather/stats)、清洗管理(上传/日志/下载) |
| Day 21 | 2026-09-09 | 🔶 | chat HTTP 层 intent=other bug 已修复（强关键词预判，12/12 用例通过）；用户端/管理端浏览器点检 + 清洗全流程实测待 Docker 环境 |
| Day 22 | - | ⏸️ | 暂缓：管理端 3D 地图板块已隐藏，后续开发时再开启 |
| Day 23 | - | ⏸️ | 暂缓：管理端 3D 地图板块已隐藏，后续开发时再开启 |
| Day 24 | 2026-09-11 | ✅ | 完成：后端 Redis 缓存+失败静默降级；用户端路由懒加载+全局错误提示；管理端 ECharts 按需引入 |
| Day 25 | 2026-09-14 | ✅ | 完成：六个镜像构建成功(后端1.34GB/前端75MB+76.5MB)、docker compose 一键启动 6 容器全 Up，Nginx 反代与 Celery 均验证通过 |
| Day 26 | | ⬜ | |
| Day 27 | 2026-09-11 | 🔶 | README 已完成（介绍/技术栈/目录/启动/环境变量/架构/降级/FAQ）；面试高频问题见面试宝典.md，迭代清单待补 |
| Day 28 | | ⬜ | |

### 第二阶段（增强与工程化，Day 29 起）

| Day | 日期 | 状态 | 备注 |
| --- | --- | --- | --- |
| Day 29 | 2026-09-14 | ✅ | 完成：19 个测试文件 379 用例全绿、覆盖率 61%（红线 60%）、断网可跑；踩坑 NullPool 跨事件循环 + 外键约束；测试还抓出「天气规则 break 短路」缺陷（雷阵雨拿不到防雷提示）并修复 |
| Day 30 | 2026-09-14 | ✅ | 完成：vitest 41 用例（utils/http 100%）+ Playwright 22 用例（5 条核心路径）全绿；测试驱动修出 3 个真实缺陷（复合天气匹配 / 登录错误提示显示技术信息 / 体验账号密码写错）；覆盖率口径收窄至单测该守住的代码（84%） |
| Day 31 | 2026-09-15 | ✅ | 完成：GitHub Actions 三关流水线（ruff 10s / pytest 1m52s / 前端 38s / 4 镜像并行构建）首次全绿，总耗时 6m13s；ruff 204 问题清零、eslint 12 error 降 0、锁定测试依赖版本；排查出 `pytest` vs `python -m pytest` 的 sys.path 差异（CI 退出码 4 的真凶），并建立「失败日志转注解」机制（无需登录即可读） |
| Day 32 | 2026-09-15 | ✅ | 完成：CI 的 release job 推送 4 镜像到 GHCR（latest + sha 标签，零 Secret）；compose 支持 IMAGE_TAG/ENV_FILE 变量；deploy.sh 一键部署（备份/健康检查/自动回滚）；实测部署 GHCR 镜像上线、回滚演练通过、备份恢复到临时库验证数据完整 |
| Day 33 | 2026-09-15 | ✅ | 完成：前端两级 ErrorBoundary（重试按钮）；后端 JSON 结构化日志 + request_id 贯穿 + /metrics 路由级指标；/health 增强（DB/Redis 分项，失败 503 与回滚联动）；Celery 修复「吞异常伪装 SUCCESS」深层问题 + autoretry×3 + 信号告警。测试 399(后端)+46(前端)，新增 25 个 |
| Day 34 | 2026-09-15 | ✅ | 完成：Web Push（VAPID/pywebpush）+ 邮件 SMTP 双通道、订阅/退订/发送记录/测试推送接口、前端订阅授权流程（sw.js + Profile 通知设置区块、拒绝友好降级）；修出 AsyncSession 不支持 gather 并发导致挂起的坑；测试 406(后端)+46(前端) |
| Day 35 | 2026-09-15 | ✅ | 完成：早报组装复用首页看板（天气+提醒+穿搭+今日行程冲突）；免打扰偏好表（开关+推送小时），beat 每小时分发按用户设定过滤；前端通知设置加早报开关与时间；测试 419(后端)+46(前端)，新增 13 个 |
| Day 36 | 2026-09-16 | ✅ | 完成：`weather_alerts` 表 + fingerprint 去重识别新增预警；按城市/行程精准匹配受影响用户；Web Push/邮件 + SSE 双通道（Redis Pub/Sub 跨进程）；beat 每 10 分钟轮询。真实链路实测通过（入库→实时收到→通知记录落库），并修出 2 个真实问题：心跳被当作预警弹出、和风预警接口 403 需免费源兜底。测试 439(后端)+46(前端)，新增 22 个 |
| Day 37 | 2026-09-16 | ✅ | 完成：和风 `/indices/1d` 接入（实测免费 Key 可用，一次返回 16 类指数）；`life_indices` 时序表支持历史对比；首页展示 4 项并按「体质偏好 + 行程活动 + 指数结论」排序（新增 `users.body_preference`，Profile 页可视化设置）。修出 3 个问题：和风文档编码与接口实际不一致（10/11 反了，导致排序莫名错位）、提权漏洞（普通用户可改任意用户 role）、测试库 `create_all` 不补新列。测试 472(后端)+46(前端)，新增 29 个 |
| Day 38 | 2026-09-16 | ✅ | 完成：`vite-plugin-pwa`（manifest + SW，图标纯 Python 手写 PNG 含 maskable）；预缓存 60 项 + 天气/首页 StaleWhileRevalidate、其余 GET NetworkFirst、写操作不缓存；安装引导区分 Chromium/iOS；离线提示做成应用内横条而非静态页。关键整合：推送脚本改 `push-sw.js` 由生成的 SW 注入（一个 scope 只能一个 SW），**并因此修好一个悬着的问题——Day 34 的推送 SW 其实从未被注册过**。E2E 28 全绿（新增 PWA 6），前端单测 56（新增 10） |
| Day 39 | 2026-09-16 | ✅ | 完成：三类类型开关（早报/预警/行程提醒）+ 多设备订阅列表与单个退订 + 历史记录带类型与被拦原因；偏好校验收口在唯一发送入口，被拦下也留 skipped 记录。**补上真实发送方**：出发前 30 分钟行程提醒（用 10 分钟时间窗口保证幂等，不记状态）。修出 2 个真问题：`pywebpush` 从未进 `requirements.txt`（本地能跑、容器起不来，且暴露后端容器自 Day 34 起未重建）、前端单测在负载下偶发超时。测试 489(后端)+62(前端)+E2E 32 |
| Day 40 | | ⬜ | AI 一键排行程（天气规避 + 可写入行程表） |
| Day 41 | | ⬜ | 多智能体协作（Supervisor 架构 + 效果/延迟对比） |
| Day 42 | | ⬜ | TimescaleDB 连续聚合 + 同比环比分析 |
| Day 43 | | ⬜ | RAG 混合检索 + Rerank（含评测集对比） |
| Day 44 | | ⬜ | 行程分享（只读链接 + 海报图） |
| Day 45 | | ⬜ | 用户画像与偏好学习 |
| Day 46 | | ⬜ | 地图可视化（区域天气热力图 + 路线轨迹） |
| Day 47 | | ⬜ | 管理端数据大屏 |
| Day 48 | | ⬜ | 性能压测与优化（locust/k6 + 压测报告） |
| Day 49 | | ⬜ | 文档与简历增量更新 |
| Day 50 | | ⬜ | 最终回归与归档 |

---

## 遗留问题跟踪

### Day 7 遗留问题收尾（2026-08-24 解决）
| 遗留项 | 状态 | 解决方案 |
| --- | --- | --- |
| Celery 无自启机制 | ✅ 已解决 | 新增 `backend/start_celery.ps1`（Windows 一键启动）；`docker-compose.yml` 新增 `celery` service（全 docker 部署用） |
| 数据库 17 个垃圾测试账号 | ✅ 已解决 | 删除 `alice*/wtest*/ctest*` 等，保留 `admin`/`admin280517` |
| 前端未联调 | ⚠️ 部分解决 | 前端仍为脚手架（无业务页面，属 Day15+ 排期）；已配 `vite.config.ts` 的 `/api` 代理到后端 8000，两前端均 `build` 成功 |
