# 知识点剖析（面试向）

> 这份文档记录项目开发过程中**真正踩过的坑**和背后的原理。
> 每个主题都按「是什么 → 为什么 → 本项目怎么做 → 面试怎么答」组织，
> 目的是让你能不背稿地讲清楚自己的项目。
>
> 📖 **没接触过自动化测试、觉得「测试到底在干嘛」？**
> 先读 [`测试入门指南.md`](./测试入门指南.md)——那份是零基础向的，
> 用大白话 + 本项目真实 Bug 案例讲清测试是什么、442 个用例分别在守什么。
> 本文档则偏技术深度，讲「为什么这么设计」。

---

## 目录

| 主题 | 来源 | 一句话价值 |
| --- | --- | --- |
| [一、自动化测试体系](#一自动化测试体系) | Day 29 | 项目从"没测试"到 379 个用例、61% 覆盖率 |
| [二、异步测试的事件循环陷阱](#二异步测试的事件循环陷阱) | Day 29 | 最难查的一类 bug，面试能讲出深度 |
| [三、测试数据隔离两种方案](#三测试数据隔离两种方案) | Day 29 | 为什么选独立库而不是事务回滚 |
| [四、Mock 外部依赖](#四mock-外部依赖) | Day 29 | 断网也能跑全量测试 |
| [五、覆盖率的正确用法](#五覆盖率的正确用法) | Day 29 | 别拿它当 KPI，也别为了数字造假 |
| [六、前端测试体系](#30--前端测试体系vitest-单测--playwright-e2e) | Day 30 | Vitest 与 Playwright 的分工、选择器稳定性排序 |
| [七、CI 持续集成](#31--ci-持续集成) | Day 31 | `pytest` 与 `python -m pytest` 的路径差异、lint 规则的取舍 |
| [八、CD 自动化部署](#32--cd-自动化部署) | Day 32 | 镜像标签即回滚能力、备份的一致性时机、部署脚本的失败处理 |

---

## 一、自动化测试体系

### 是什么

**测试 = 一段替你做检查的代码**。改完功能后跑一遍，它会告诉你"原来能用的功能还被没被改坏"。

### 为什么需要

想象一个场景：项目有 65 个接口，你改了一个"天气规则"文件。怎么知道没影响别的地方？

- **没测试**：手动点一遍全部页面，半小时过去了，还不一定点全
- **有测试**：`pytest` 跑一下，2 分钟告诉你"379 个检查点全部通过"或"第 47 个坏了"

### 测试金字塔

```
        /\        端到端测试（E2E）   少而慢，验证完整流程
       /  \       —— 如 Playwright 点页面
      /____\
     /      \     集成/接口测试        中等，验证多个模块协作
    /        \    —— 如「调接口 → 落库 → 查回来」
   /__________\
  /            \  单元测试              多而快，验证单个函数
 /______________\ —— 如「23.7℃ 应命中凉爽档」
```

比例建议：**单元 70% / 集成 20% / E2E 10%**。
本项目 Day 29 完成的是下面两层（单元 + 接口），E2E 安排在 Day 30。

### 本项目怎么做

```
backend/tests/
├── conftest.py                  # 全局 fixture（测试库、客户端、鉴权头）
├── unit/                        # 14 个文件：纯逻辑与解析层
│   ├── test_amap_client.py
│   ├── test_outfit_engine.py
│   └── ...
└── api/                         # 5 个文件：接口 + 鉴权 + 数据隔离
    ├── test_auth.py
    ├── test_itinerary.py
    └── ...
```

### 面试怎么答

> "项目原本没有测试，我补了一套：19 个文件、379 个用例，覆盖率 61%。
> 单元测试覆盖规则引擎、解析层、评分逻辑；接口测试覆盖鉴权边界和多租户隔离。
> 外部依赖（天气、地图、LLM）全部用 respx 打桩，所以**断网也能跑全量**。
> 测试还帮我抓出一个真 bug：天气规则遍历时 break 了，导致『雷阵雨』拿不到防雷提示。"

---

## 二、异步测试的事件循环陷阱

> ⭐ 这是本项目 Day 29 最难查的问题，**面试讲这个很加分**。

### 现象

单独跑某个测试文件 → **全绿**。
和别的文件一起跑 → **大面积 ERROR**：

```
RuntimeError: Event loop is closed
self = <ProactorEventLoop running=False closed=True debug=False>
```

### 原理：什么是事件循环

异步代码（`async/await`）需要一个"调度员"来安排任务，这个调度员就是**事件循环（event loop）**。

打比方：事件循环是**餐厅里的一个服务员**。
- 你点菜（发起 await）→ 服务员去后厨下单 → 顺便服务别的桌 → 菜好了端给你
- 这个服务员只服务"自己负责的区域"

### 坑在哪

`pytest-asyncio` 的默认行为是：**每个测试用例开一个新的服务员（新 event loop）**。

而数据库连接池（asyncpg）的连接是**绑定在具体服务员身上的**：

```
用例 1：服务员 A 建了连接 ──→ 用完放回池子（连接仍记着"我归 A 管"）
用例 1 结束：服务员 A 下班了 ❌
用例 2：服务员 B 从池子里拿连接 ──→ 连接说"我归 A 管" ──→ 💥 报错
```

### 为什么难查

1. **单独跑不复现**——只有一个用例时，连接和 loop 是一对一的，没问题
2. **错误信息误导**——报的是 `Event loop is closed`，看起来像"代码里手动关了 loop"，实际是连接池的锅
3. **修一半还会剩一半**——我第一反应是给"测试自己的 engine"加 NullPool，但接口测试走的是**应用自己的 engine**（`app.db.session.engine`），它仍是默认连接池

### 解决方案

```python
# app/db/session.py
_use_null_pool = os.getenv("DB_USE_NULLPOOL", "").lower() in ("1", "true", "yes")

engine = create_async_engine(
    settings.DB_URL,
    pool_pre_ping=True,
    **({"poolclass": NullPool} if _use_null_pool else {}),
)
```

**NullPool = 不用连接池**：每次用完就关掉，下次重新建。
这样连接永远属于"当前这个服务员"，不会跨 loop 复用。

```
生产环境：用连接池（建连接有开销，复用更划算）
测试环境：用 NullPool（用例频繁切换 loop，复用反而会出事）
```

### 同源问题

项目在 Celery 侧**早就踩过同一个坑**——Celery 任务里反复 `asyncio.run()` 也会创建新 loop，
当时的解法也是"模块级复用一个 loop + NullPool 独立引擎"。

> **这就是"同一个原理在不同场景反复出现"的典型**，所以我说它是同类坑。

### 面试怎么答

> "多文件跑测试时遇到 `Event loop is closed`。根因是 pytest-asyncio 每个用例新建事件循环，
> 而 asyncpg 的连接池把连接绑定在创建它的 loop 上，跨 loop 复用就报错。
> 有意思的是，单独跑不复现——因为只有一个 loop。
> 解法是让 engine 用 NullPool，每用完即关、不跨 loop 复用。
> 这个坑项目在 Celery 里也踩过，本质是同一个问题。"

---

## 三、测试数据隔离两种方案

### 问题

测试要往库里写数据，怎么保证**不污染开发数据**、又**不互相干扰**？

### 方案对比

| 方案 | 做法 | 优点 | 缺点 |
| --- | --- | --- | --- |
| A. 同库 + 事务回滚 | 每个用例在事务里跑，结束回滚 | 快、无额外库 | **挡不住"自建 session 并 commit"的代码** |
| B. 独立测试库 | 建单独的库，用例结束 TRUNCATE | 彻底隔离、真实 | 需建库、稍慢 |

### 为什么本项目选 B

项目里有些 Service 在未传 `db` 时会**自己建 session**：

```python
# chat_history_service.add_message
owns_db = db is None
session = db or AsyncSessionLocal()      # ← 自建 session
...
await s.commit()                          # ← 真实提交，事务回滚拦不住
```

方案 A 依赖"所有写操作都走同一个被回滚的事务"，
但只要有一处自建 session 并 commit，数据就**真写进开发库了**。

所以选 B：`weather_db_test` 独立库 + 每用例 `TRUNCATE ... RESTART IDENTITY CASCADE`。

```python
@pytest_asyncio.fixture(autouse=True)
async def _clean_tables():
    yield
    async with _engine.begin() as conn:
        names = ", ".join(f'"{t.name}"' for t in reversed(Base.metadata.sorted_tables))
        await conn.execute(text(f"TRUNCATE TABLE {names} RESTART IDENTITY CASCADE"))
```

> 注意 `RESTART IDENTITY`：让自增 ID 归零，避免用例之间产生"ID 顺序"的隐性依赖。

### 面试怎么答

> "我用独立测试库 + 每用例清表。本来想用事务回滚（更轻量），
> 但项目里有 Service 会自建 session 并 commit，回滚挡不住这类写入，
> 会真写进开发库，所以换成独立库物理隔离。"

---

## 四、Mock 外部依赖

### 是什么

**Mock = 假的替身**。测试时把"真实的外部服务"换成"你控制的假响应"。

打比方：演员试镜时不会真的去拍外景——用一个绿幕代替。
**绿幕 = Mock**，试镜 = 测试。

### 为什么必须 Mock

| 如果不 Mock | 后果 |
| --- | --- |
| 依赖网络 | 断网/服务抖动 → 测试随机失败（"flaky test"） |
| 依赖 API Key | CI 上没配 Key → 全挂 |
| 依赖真实数据 | 天气天天变 → 断言写不了 |
| 消耗额度 | 和风/高德/Tavily 都有限额，跑测试就把额度烧光 |

### 本项目怎么做

用 `respx` 拦截 httpx 请求：

```python
async def test_解析搜索结果(self, respx_mock):
    respx_mock.post("https://api.tavily.com/search").mock(
        return_value=httpx.Response(200, json={"results": [{"title": "广州天气", ...}]})
    )
    results = await ws._search_tavily("广州天气", 5)
    assert results[0]["title"] == "广州天气"
```

对 Service 层的函数依赖，用 `monkeypatch`：

```python
monkeypatch.setattr(home_service, "fetch_weather", fake_fetch_weather)
```

### 一个巧妙的设计

`respx` 有个默认行为：**没被 mock 的外部请求会直接报错**。

这等于免费获得一道防线——如果某个用例"偷偷"访问了真实网络，
测试会立刻失败并告诉你"有个请求没打桩"。
所以我没加"兜底 mock"，反而**故意保留这个报错行为**。

### 面试怎么答

> "外部依赖全部打桩，保证测试不联网、不需要 Key、不烧额度。
> 而且我特意依赖 respx『未匹配就报错』的特性——
> 这样一旦有用例偷偷发真实请求，测试会立刻失败，等于多一道防线。"

---

## 五、覆盖率的正确用法

### 是什么

**覆盖率 = 被测试执行到的代码行数 ÷ 总代码行数**。

### 怎么理解它

覆盖率**只能说明"没测什么"，不能说明"测得好不好"**。

```python
def test_什么也没验证(self):
    await client.get("/api/v1/notes")     # 执行了，覆盖率 +1
    # 但没有任何 assert —— 接口返回 500 也照样通过
```

这个用例让覆盖率上涨，却毫无价值。**高覆盖率 ≠ 高质量**。

### 本项目的做法

**1. 设置红线但不迷信**

```ini
[coverage:report]
fail_under = 60      # 低于 60% 直接让 CI 失败
```

**2. 排除口径要诚实、并写明理由**

```ini
# 注意用 */ 前缀匹配：Windows 下实际路径是 app\tasks\xxx.py（反斜杠），
# 只写 app/tasks/* 会因为分隔符差异匹配不上
omit =
    */app/scripts/*     # 一次性入口脚本
    */app/tasks/*       # Celery 调度胶水，业务逻辑在 services 层（已覆盖）
    */__init__.py
```

> ⚠️ **不要为了数字好看去排除业务代码**——那是自欺欺人。

**3. 关注"哪些关键路径没覆盖"而不是"百分比"**

看这份清单比看总分有用得多：

```
app/services/agent.py        24%   ← Agent 编排未覆盖（需 mock LLM）
app/services/weather_news_service.py  16%  ← 资讯采集流程未覆盖
```

### 面试怎么答

> "我设了 60% 的红线，但我不会拿它当 KPI。
> 覆盖率只说明『没测什么』，不说明『测得对不对』——
> 一个没有断言的用例也能刷高覆盖率。
> 我更关注的是：核心业务路径有没有覆盖、异常分支有没有覆盖。
> 比如排除项我明确写了理由：Celery 任务只是调度包装，逻辑在 service 层已覆盖。"

---

## 附：本项目测试相关速查

```bash
# 跑全部测试 + 覆盖率
cd backend && python -m pytest

# 只跑单元测试（快）
python -m pytest tests/unit -q

# 只跑接口测试
python -m pytest tests/api -q

# 跑单个文件 / 单个用例
python -m pytest tests/unit/test_amap_client.py
python -m pytest "tests/api/test_notes.py::TestCrud::test_创建笔记"

# 失败时看详细堆栈（默认已开 -v）
python -m pytest -x --tb=long      # -x 遇到第一个失败即停

# 覆盖率 HTML 报告（生成到 htmlcov/index.html，可点进去看哪行没覆盖）
python -m pytest --cov-report=html
```

| 配置项 | 作用 |
| --- | --- |
| `asyncio_mode = auto` | async 测试函数不用手写 `@pytest.mark.asyncio` |
| `DB_USE_NULLPOOL=1` | 测试环境启用 NullPool，避免跨事件循环报错 |
| `respx_mock` fixture | 拦截 httpx 请求（respx 提供） |
| `client` fixture | ASGI 直连客户端，不启动真实服务器 |
| `auth_headers` / `admin_headers` | 已登录的请求头（普通用户 / 管理员） |

---

# 31 · CI 持续集成

## 一、CI 到底解决什么问题

**是什么**：每次 push 代码，机器自动跑一遍「检查 → 测试 → 构建」，全过才允许合并。

**为什么需要**：没有 CI 的团队是靠人自觉的——

| 没有 CI | 有 CI |
| --- | --- |
| 有人忘记跑测试就推了 | 机器每次必跑，不依赖自觉 |
| 本地能跑、别人拉下来跑不了 | 固定环境执行，消除"我这儿是好的" |
| 合并后才发现主干挂了 | PR 阶段就拦住，主干永远是绿的 |
| 「上一个人改坏了什么」靠猜 | 精确指出是哪次提交、哪个用例挂了 |

**关键认知**：CI 的价值不在于"能自动跑测试"，而在于**把质量约束变成不可绕过的事实**。
测试写得再好，只要可以"这次先不跑"，它就形同虚设。

## 二、GitHub Actions 的核心概念

| 概念 | 含义 | 本项目 |
| --- | --- | --- |
| workflow | 一个 yml 文件 = 一条流水线 | `.github/workflows/ci.yml` |
| job | 流水线里的一个任务，**默认并行** | ruff / pytest / 前端 / 镜像构建 |
| step | job 里的一个步骤，**串行且共享环境** | checkout → setup → install → run |
| runner | 跑 job 的临时虚拟机 | `ubuntu-latest` |
| service | 给 job 配套的容器服务 | PostgreSQL（测试要用真库） |
| matrix | 一套配置批量展开成多个 job | 4 个镜像并行构建 |
| artifact | job 产出的文件，可下载 | 覆盖率报告 |

**几个容易踩的点**：

- **job 之间默认并行、彼此隔离**：前一个 job 装的依赖，后一个 job 里没有，
  所以每个 job 都要自己 checkout + setup
- **`needs` 控制依赖顺序**：镜像构建必须等测试全过才跑（`needs: [lint, test, frontend]`）
- **`concurrency` 取消旧运行**：连续推送时，前一次还没跑完的会被取消，
  避免"旧提交的检查把新提交的结果覆盖掉"

## 三、`pytest` 与 `python -m pytest` 的行为差异（面试可讲的坑）

这是本次 CI 排查中**最有价值的一个知识点**，因为它在本地几乎不可能复现。

```bash
# 本地习惯这么跑 —— 一直是绿的
python -m pytest

# CI 里这么跑 —— 直接挂
pytest
```

**差异根源在 `sys.path` 的第一个元素**：

| 调用方式 | `sys.path[0]` | 能否 `from app import ...` |
| --- | --- | --- |
| `python -m pytest` | **当前工作目录** | ✅ 能 |
| `pytest`（console script） | **脚本所在目录**（venv/Scripts 或 /usr/local/bin） | ❌ 不能 |

`python -m` 的本质是"把模块当作程序入口运行"，Python 会把 cwd 放进 `sys.path`；
而 console script 是一个独立的可执行入口，`sys.path[0]` 是那个可执行文件所在目录。

**为什么这个坑特别阴**：

1. 本地开发习惯用 `python -m pytest`（或 IDE 点什么按钮），永远复现不了
2. 一旦 conftest 导入失败，**pytest 的退出码是 4（usage error）**，
   报错信息看起来像"参数写错了"，而不是"模块找不到"
3. @ 排查方向会被误导到"是不是 --cov 参数不对 / 插件没装"

**修复**：`pytest.ini` 里声明 `pythonpath = .`，让 pytest 主动把项目根加进 `sys.path`。
这样**两种调用方式行为一致**，把差异彻底消除。

> **延伸**：同类问题也出现在 `celery`、`gunicorn`、`uvicorn` 等命令上——
> 凡是有 console script 入口的工具，都可能遇到"直接调命令找不到项目模块"。
> 通用解法是让项目**可安装**（`pip install -e .` + `pyproject.toml` 的 packages 配置），
> 或者显式声明 pythonpath。

## 四、lint 规则的取舍：会开也要会关

配 lint 最容易犯的错是"规则开到最满"，结果一次报出几百条无关痛痒的问题，
最后所有人都选择无视——**那还不如不配**。

本项目的取舍原则：

**开启**：能发现真 bug 的（`F` 未使用变量/未定义名、`B` 易错写法）
或能自动修的（`I` 导入排序、`UP` 新语法）。

**谨慎开启**：`BLE`（裸 `except Exception`）——本可以不开，
但因为项目里**已经写了 30+ 处 `# noqa: BLE001` 标注**，不开反而会让这些标注
被 `RUF100` 判为"无用"而**自动删掉**，丢掉"这里是有意捕获全部异常"的信息。

> 这是一个反直觉的教训：**启用一条规则的动机，可能不是为了检查新代码，
> 而是为了保护老代码里已有的意图标注。**

**明确关闭**：`ARG`（未使用参数）——在测试 fixture（参数常为副作用）和
FastAPI 路由函数（参数由框架按名字注入）里报了 70+ 条误报。

```toml
# ruff.toml 里的说明
# 说明：没有启用 ARG（未使用参数）。试过之后发现它在**测试文件**（fixture
# 注入常常只为副作用）和 **FastAPI 路由函数**（参数由框架按名字注入）里
# 会报出 70+ 条误报，属于"关掉比开着更有价值"的规则。
```

**另外**：自动修复（`--fix`）用之前要看清 `RUF100` 这类"删除型"规则会删什么，
**先补规则再修复**，否则容易把有意标注当垃圾清掉。

## 五、依赖版本：`>=` 与 `==` 该用哪个

| 场景 | 建议 | 理由 |
| --- | --- | --- |
| 运行依赖 | `>=` + 定期手动升级 | 保持能拿到安全补丁，灵活性优先 |
| 测试/构建工具 | `==` 精确锁定 | **CI 是否绿取决于它们的行为**，本地与 CI 必须一致 |

本项目的实测教训：`requirements-dev.txt` 原本写 `pytest>=8.0.0`，
本地 venv 是几天前装的，CI 每次装**当时的最新版**——两边版本可能不同，
导致"本地绿、CI 红"且无法复现。改为 `==` 锁定后，CI 与本地完全一致。

## 六、如何在拿不到日志的情况下排查 CI

**约束**：GitHub 的完整 Actions 日志需要登录才能查看，自动化环境没有账号。

**解法分三步**：

1. **最小化探针**：先推一个只做 `echo` + `checkout` 的 workflow。
   它过了 → 说明 runner 环境正常，问题在 job 配置里；它挂了 → 环境/权限层面问题。
   这一步能立刻砍掉一半的排查空间。

2. **让 CI 自己把错误搬到能看见的地方**：日志要登录，但**注解区不需要**。
   用 workflow command 把关键信息输出成注解：

   ```bash
   echo "::error::pytest 退出码 $code"
   head -12 pytest-output.log | while read -r l; do echo "::error::[头部] $l"; done
   tail -12 pytest-output.log | while read -r l; do echo "::error::[尾部] $l"; done
   echo "::notice::pytest $(pytest --version)"
   ```

   **取首尾两段**是有讲究的：用法错误的原因写在最前面，
   测试失败的信息在最后面，只取一头会漏。

3. **这个机制应该长期保留**，不只是临时调试——它把"读日志"这个动作
   从"需要登录"变成"打开页面就能看"。

---

# 30 · 前端测试体系：Vitest 单测 + Playwright E2E

## 一、测试金字塔在前后端的落位

```
        /\        Playwright E2E（22 个）—— 真实服务、真实 LLM，最慢最脆，只测核心路径
       /--\       Testing Library 组件测试（18 个）—— 渲染逻辑、交互状态
      /----\      Vitest 单元测试（23 个）—— 纯函数、拦截器，毫秒级
     /------\     pytest 后端测试（379 个）—— 单元 / 接口两层
```

前端测试的**分层原则**与后端一致，但边界更清楚：

- **纯函数/工具**：必须高覆盖（utils 100%），没有 DOM、没有 mock，测试就是文档
- **组件**：只测「渲染结果与状态流转」，不测内部实现（不查 useState 的值，查屏幕上出现了什么）
- **页面/布局**：**不在单测层测**——它们的价值在于路由、鉴权、接口联动的集成行为，
  单测层写只会得到一堆 mock 剧本（"当 API 返回 X 时显示 Y"），是**对实现的复述**而不是验证。
  交给 E2E 打真实服务更划算

> 收窄覆盖率口径不是作弊：把"由 E2E 覆盖更合适"的部分从单测统计里排除，
> 让 84% 这个数字真正反映「单测该守住的代码」的覆盖情况。

## 二、Testing Library 的核心心法：查"用户看得见的东西"

```tsx
// ❌ 测实现：组件内部状态、类名、props 透传
expect(component.state.loading).toBe(false)

// ✅ 测行为：用户视角——屏幕上有什么、点了之后有什么变化
await user.click(screen.getByText('近 30 天'))
expect(screen.getAllByText('实况').length).toBeGreaterThan(0)
```

`getByText` / `getByRole` / `getByPlaceholder` 都是以用户感知为准的查询；
`getByTestId` 是最后手段（用户看不见 testid）。

**两大陷阱**（本项目都踩了）：

1. **strict mode**：`getByText('我的行程')` 命中导航栏菜单项 + 页面标题两个元素
   会直接抛错（而不是返回第一个）——这是故意的，强迫你写精确的选择器
2. **假 Elements**：antd Segmented 渲染了一个**视觉隐藏**的 radio input，
   能被查询到但点不了（Playwright 对不可见元素拒绝 click，直到超时）

## 三、Playwright 选择器稳定性排序

```
getByRole('button', { name: '保 存' })     ← 最优先：语义 + 可访问名，重构 UI 不碎
getByPlaceholder('如：迪士尼一日游')        ← 次优：表单场景很稳
page.locator('.jp-serif', { hasText })     ← 兜底：语义拿不到时用稳定类名
getByText('登录')                          ← 危险：极易撞名（导航/标题/按钮）
page.locator('.ant-picker-cell-in-view')   ← 最危险：绑死 antd 内部类名，升级必碎
XPath                                      ← 基本不用
```

实战补充：antd 会给**两个字的按钮自动插空格**（「登录」→「登 录」），
精确文本匹配会莫名失败——用正则 `/登\s*录/` 才稳。
这也是「为什么同一个文件里注册用例能过、登录用例超时」这类诡异现象的答案。

## 四、E2E 的稳定性设计（反 flaky）

E2E 最大的成本不是写，而是**让它每次跑都绿**。本项目用了这几招：

| 手段 | 做法 |
| --- | --- |
| 数据隔离 | 每个用例 `uniqueName()` 注册全新账号，用完即弃——不猜上一条测试留下的状态 |
| 串行执行 | `workers: 1` + `fullyParallel: false`，避免并发写库互相干扰 |
| 宽松断言 | 只断结构不断数值：断「温度卡片出现了」，不断「温度等于 29.9」（天气在变） |
| 动态时间 | 所有日期相对 `new Date()` 计算，测试不随日历过期 |
| 时序留白 | AI 流式回复渲染完 ≠ 落库完成（流结束才写库），reload 前显式等待 |
| 失败留痕 | `screenshot/video/trace` 全部 `only-on-failure` / `retain-on-failure` |

**探活也要讲究**：E2E 开始前先确认服务在跑，并给出清晰报错
（"请先 docker compose up -d"），否则 22 个用例全部超时 90 秒，等 33 分钟才告诉你挂了。

## 五、Vitest vs pytest 速查对照

| 概念 | pytest | Vitest |
| --- | --- | --- |
| 断言 | `assert x == y` | `expect(x).toBe(y)` |
| mock 函数 | `mocker.patch()` | `vi.fn()` + `vi.mock('模块路径')` |
| mock 模块 | - | `vi.mock('./api/weather', () => ({...}))` |
| 时间冻结 | `freezegun` | `vi.useFakeTimers()` + `setSystemTime` |
| 用例参数 | fixtures | `beforeEach` / 工厂函数 |
| 覆盖率 | pytest-cov | `--coverage`（v8 provider） |
| DOM 测试 | - | jsdom + Testing Library |

本项目没让 vitest 注入全局（`globals: false`），
`describe/it/expect` 显式 import——和 ESLint 的 no-undef 规则天然兼容，
也省得改 tsconfig 的 types。

## 附：前端测试速查

```bash
cd frontend-user

npm test            # vitest 单测（跑一次）
npm run test:cov    # 单测 + 覆盖率（HTML 报告在 coverage/index.html）
npm run test:e2e    # Playwright E2E（需先 docker compose up -d）
npm run test:e2e:ui # Playwright 可视化调试模式

# 只跑一个 E2E 文件 / 用例（-g 按名字过滤）
npx playwright test e2e/auth.spec.ts
npx playwright test -g "新用户可注册"

# 看失败录像回放（HTML 报告）
npx playwright show-report
```

---

# 32 · CD 自动化部署

## 一、CI 与 CD 的关系：发布门禁

CI 解决「验证」，CD 解决「发布」。两者衔接的关键设计：

```
push 到 master
   → lint / test / 前端 全部通过（发布门禁，needs 硬性保证）
   → 才允许构建并推送镜像到镜像仓库
   → 部署机一条命令拉取上线（失败自动回滚）
```

**核心原则：测试没过的代码不配被发布。** 这条约束用 GitHub Actions 的
`needs` 字段硬性保证，而不是靠流程约定——机器强制的规则才不会被忘掉。

本项目把「构建验证」和「发布」合并成一个 job（PR 时只构建、push 时才推送），
避免同一批镜像构建两次（后端镜像 1.3GB，重复构建很贵）。

## 二、镜像标签策略 = 回滚能力

**回滚的本质是"让服务跑回旧版本的镜像"**，所以标签设计直接决定回滚好不好用：

| 标签 | 作用 | 为什么需要 |
| --- | --- | --- |
| `latest` | 始终指向主干最新 | 部署机的默认拉取目标 |
| `sha-4c3abe9` | 与某次提交一一对应 | 回滚时精确指定"回到哪一次提交" |
| `pg17`（数据库镜像固定） | 随 PG 版本走 | 数据库镜像不随应用发版变化，避免部署时连带重建 |

> 只打 `latest` 一个标签的项目，回滚时只能"回到上一个 latest"——
> 但你根本说不清那是哪次提交的代码。**`sha-` 标签让每次发布都可追溯、可复现。**

## 三、compose 的 build / image 并存模式

```yaml
build: {context: ./backend}                                # 本地开发：按源码构建
image: ghcr.io/ticnix/weather-backend:${IMAGE_TAG:-latest} # 部署：拉取镜像
```

两者并存时行为互补：

| 命令 | 行为 |
| --- | --- |
| `docker compose up -d --build` | 构建并打上 image 指定的标签（本地开发） |
| `IMAGE_TAG=sha-xxx docker compose pull` | 拉取指定版本（部署机） |
| `IMAGE_TAG=sha-xxx docker compose up -d` | 用指定版本启动 |

**回滚 = 换个旧 tag 把部署命令再跑一遍**。不需要单独的"回滚系统"，
回滚就是一次普通的部署，这大幅降低了回滚的维护成本和心智负担。

## 四、备份的时机：一致性比"做了备份"更重要

部署脚本的步骤顺序是刻意安排的：

```
停应用容器 → pg_dump 备份 → 拉镜像 → 启动 → 健康检查
     ↑            ↑
  数据不再变化   拿到的是一致性快照
```

如果先备份再停应用：备份期间应用还在写数据，`pg_dump` 拿到的可能是
"写到一半"的状态。**备份的正确性取决于时机，而不只是命令对不对。**

配套的两个防呆：

1. 备份完成后立即校验（非空 + `gzip -t` 完整性）——防止"备份了个寂寞"
2. 只保留最近 N 份并自动清理——防止备份目录悄悄吃满磁盘

## 五、部署脚本的失败处理：每个退出点都要想清楚

部署可能在**任何一步**失败，每一步的"正确反应"不一样：

| 失败点 | 正确反应 | 为什么 |
| --- | --- | --- |
| 备份失败 | **中止**，什么都不动 | 数据安全优先于上线 |
| 拉镜像失败 | **中止**（服务本来就停着，无副作用） | 大概率是网络/tag 不存在 |
| 启动失败 | 回滚 | 新镜像可能有问题 |
| 健康检查失败 | **自动回滚**，回滚后再真实探测一次 | 上线标准就是健康检查 |
| 回滚也失败 | 退出码 2，人工介入 | 这时数据库已有部署前备份 |

**退出码是给"下一个环节"看的**：`0` 成功 / `1` 已回滚 / `2` 需人工。
监控系统或 CI 只需要看退出码就知道要不要告警。

## 六、镜像仓库的可见性是个隐蔽坑

GHCR 的用户级包**默认私有**，匿名 `docker pull` 会 401。
而部署脚本通常是匿名拉取的——"CI 明明推送成功了，部署机却拉不到"。

解法：CI 推送后用内置 `GITHUB_TOKEN` 调 packages API 把包改为公开
（本项目镜像不含敏感层，公开无风险）；或手动去包页面改一次。
首次部署前建议先用 `docker pull` 匿名试拉一次，确认可见性没问题。

## 附：部署操作速查

```bash
# 日常部署（主干最新）
./deploy.sh

# 部署指定提交的版本
IMAGE_TAG=sha-4c3abe9 ./deploy.sh

# 回滚 = 部署旧版本（同一条命令）
IMAGE_TAG=sha-a1b2c3d ./deploy.sh

# 演练回滚流程（模拟新版本健康检查失败，验证自动回滚）
FORCE_HEALTHCHECK_FAIL=1 ./deploy.sh

# 指定环境配置
ENV_FILE=./backend/.env.prod ./deploy.sh

# 查看当前运行的是哪个版本
cat .last_deployed_tag
```
