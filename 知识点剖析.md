# 知识点剖析（面试向）

> 这份文档记录项目开发过程中**真正踩过的坑**和背后的原理。
> 每个主题都按「是什么 → 为什么 → 本项目怎么做 → 面试怎么答」组织，
> 目的是让你能不背稿地讲清楚自己的项目。

---

## 目录

| 主题 | 来源 | 一句话价值 |
| --- | --- | --- |
| [一、自动化测试体系](#一自动化测试体系) | Day 29 | 项目从"没测试"到 379 个用例、61% 覆盖率 |
| [二、异步测试的事件循环陷阱](#二异步测试的事件循环陷阱) | Day 29 | 最难查的一类 bug，面试能讲出深度 |
| [三、测试数据隔离两种方案](#三测试数据隔离两种方案) | Day 29 | 为什么选独立库而不是事务回滚 |
| [四、Mock 外部依赖](#四mock-外部依赖) | Day 29 | 断网也能跑全量测试 |
| [五、覆盖率的正确用法](#五覆盖率的正确用法) | Day 29 | 别拿它当 KPI，也别为了数字造假 |

---

## 一、自动化测试体系

### 是什么

**测试 = 一段替你做检查的代码**。改完功能后跑一遍，它会告诉你"原来能用的功能还被没被改坏"。

### 为什么需要

想象一个场景：项目有 65 个接口，你改了一个"天气规则"文件。怎么知道没影响别的地方？

- **没测试**：手动点一遍全部页面，半小时过去了，还不一定点全
- **有测试**：`pytest` 跑一下，2 分钟告诉你"379 个检查点全部通过"或"第 47 个坏了"

### 测试金字塔

```
        /\        端到端测试（E2E）   少而慢，验证完整流程
       /  \       —— 如 Playwright 点页面
      /____\
     /      \     集成/接口测试        中等，验证多个模块协作
    /        \    —— 如「调接口 → 落库 → 查回来」
   /__________\
  /            \  单元测试              多而快，验证单个函数
 /______________\ —— 如「23.7℃ 应命中凉爽档」
```

比例建议：**单元 70% / 集成 20% / E2E 10%**。
本项目 Day 29 完成的是下面两层（单元 + 接口），E2E 安排在 Day 30。

### 本项目怎么做

```
backend/tests/
├── conftest.py                  # 全局 fixture（测试库、客户端、鉴权头）
├── unit/                        # 14 个文件：纯逻辑与解析层
│   ├── test_amap_client.py
│   ├── test_outfit_engine.py
│   └── ...
└── api/                         # 5 个文件：接口 + 鉴权 + 数据隔离
    ├── test_auth.py
    ├── test_itinerary.py
    └── ...
```

### 面试怎么答

> "项目原本没有测试，我补了一套：19 个文件、379 个用例，覆盖率 61%。
> 单元测试覆盖规则引擎、解析层、评分逻辑；接口测试覆盖鉴权边界和多租户隔离。
> 外部依赖（天气、地图、LLM）全部用 respx 打桩，所以**断网也能跑全量**。
> 测试还帮我抓出一个真 bug：天气规则遍历时 break 了，导致『雷阵雨』拿不到防雷提示。"

---

## 二、异步测试的事件循环陷阱

> ⭐ 这是本项目 Day 29 最难查的问题，**面试讲这个很加分**。

### 现象

单独跑某个测试文件 → **全绿**。
和别的文件一起跑 → **大面积 ERROR**：

```
RuntimeError: Event loop is closed
self = <ProactorEventLoop running=False closed=True debug=False>
```

### 原理：什么是事件循环

异步代码（`async/await`）需要一个"调度员"来安排任务，这个调度员就是**事件循环（event loop）**。

打比方：事件循环是**餐厅里的一个服务员**。
- 你点菜（发起 await）→ 服务员去后厨下单 → 顺便服务别的桌 → 菜好了端给你
- 这个服务员只服务"自己负责的区域"

### 坑在哪

`pytest-asyncio` 的默认行为是：**每个测试用例开一个新的服务员（新 event loop）**。

而数据库连接池（asyncpg）的连接是**绑定在具体服务员身上的**：

```
用例 1：服务员 A 建了连接 ──→ 用完放回池子（连接仍记着"我归 A 管"）
用例 1 结束：服务员 A 下班了 ❌
用例 2：服务员 B 从池子里拿连接 ──→ 连接说"我归 A 管" ──→ 💥 报错
```

### 为什么难查

1. **单独跑不复现**——只有一个用例时，连接和 loop 是一对一的，没问题
2. **错误信息误导**——报的是 `Event loop is closed`，看起来像"代码里手动关了 loop"，实际是连接池的锅
3. **修一半还会剩一半**——我第一反应是给"测试自己的 engine"加 NullPool，但接口测试走的是**应用自己的 engine**（`app.db.session.engine`），它仍是默认连接池

### 解决方案

```python
# app/db/session.py
_use_null_pool = os.getenv("DB_USE_NULLPOOL", "").lower() in ("1", "true", "yes")

engine = create_async_engine(
    settings.DB_URL,
    pool_pre_ping=True,
    **({"poolclass": NullPool} if _use_null_pool else {}),
)
```

**NullPool = 不用连接池**：每次用完就关掉，下次重新建。
这样连接永远属于"当前这个服务员"，不会跨 loop 复用。

```
生产环境：用连接池（建连接有开销，复用更划算）
测试环境：用 NullPool（用例频繁切换 loop，复用反而会出事）
```

### 同源问题

项目在 Celery 侧**早就踩过同一个坑**——Celery 任务里反复 `asyncio.run()` 也会创建新 loop，
当时的解法也是"模块级复用一个 loop + NullPool 独立引擎"。

> **这就是"同一个原理在不同场景反复出现"的典型**，所以我说它是同类坑。

### 面试怎么答

> "多文件跑测试时遇到 `Event loop is closed`。根因是 pytest-asyncio 每个用例新建事件循环，
> 而 asyncpg 的连接池把连接绑定在创建它的 loop 上，跨 loop 复用就报错。
> 有意思的是，单独跑不复现——因为只有一个 loop。
> 解法是让 engine 用 NullPool，每用完即关、不跨 loop 复用。
> 这个坑项目在 Celery 里也踩过，本质是同一个问题。"

---

## 三、测试数据隔离两种方案

### 问题

测试要往库里写数据，怎么保证**不污染开发数据**、又**不互相干扰**？

### 方案对比

| 方案 | 做法 | 优点 | 缺点 |
| --- | --- | --- | --- |
| A. 同库 + 事务回滚 | 每个用例在事务里跑，结束回滚 | 快、无额外库 | **挡不住"自建 session 并 commit"的代码** |
| B. 独立测试库 | 建单独的库，用例结束 TRUNCATE | 彻底隔离、真实 | 需建库、稍慢 |

### 为什么本项目选 B

项目里有些 Service 在未传 `db` 时会**自己建 session**：

```python
# chat_history_service.add_message
owns_db = db is None
session = db or AsyncSessionLocal()      # ← 自建 session
...
await s.commit()                          # ← 真实提交，事务回滚拦不住
```

方案 A 依赖"所有写操作都走同一个被回滚的事务"，
但只要有一处自建 session 并 commit，数据就**真写进开发库了**。

所以选 B：`weather_db_test` 独立库 + 每用例 `TRUNCATE ... RESTART IDENTITY CASCADE`。

```python
@pytest_asyncio.fixture(autouse=True)
async def _clean_tables():
    yield
    async with _engine.begin() as conn:
        names = ", ".join(f'"{t.name}"' for t in reversed(Base.metadata.sorted_tables))
        await conn.execute(text(f"TRUNCATE TABLE {names} RESTART IDENTITY CASCADE"))
```

> 注意 `RESTART IDENTITY`：让自增 ID 归零，避免用例之间产生"ID 顺序"的隐性依赖。

### 面试怎么答

> "我用独立测试库 + 每用例清表。本来想用事务回滚（更轻量），
> 但项目里有 Service 会自建 session 并 commit，回滚挡不住这类写入，
> 会真写进开发库，所以换成独立库物理隔离。"

---

## 四、Mock 外部依赖

### 是什么

**Mock = 假的替身**。测试时把"真实的外部服务"换成"你控制的假响应"。

打比方：演员试镜时不会真的去拍外景——用一个绿幕代替。
**绿幕 = Mock**，试镜 = 测试。

### 为什么必须 Mock

| 如果不 Mock | 后果 |
| --- | --- |
| 依赖网络 | 断网/服务抖动 → 测试随机失败（"flaky test"） |
| 依赖 API Key | CI 上没配 Key → 全挂 |
| 依赖真实数据 | 天气天天变 → 断言写不了 |
| 消耗额度 | 和风/高德/Tavily 都有限额，跑测试就把额度烧光 |

### 本项目怎么做

用 `respx` 拦截 httpx 请求：

```python
async def test_解析搜索结果(self, respx_mock):
    respx_mock.post("https://api.tavily.com/search").mock(
        return_value=httpx.Response(200, json={"results": [{"title": "广州天气", ...}]})
    )
    results = await ws._search_tavily("广州天气", 5)
    assert results[0]["title"] == "广州天气"
```

对 Service 层的函数依赖，用 `monkeypatch`：

```python
monkeypatch.setattr(home_service, "fetch_weather", fake_fetch_weather)
```

### 一个巧妙的设计

`respx` 有个默认行为：**没被 mock 的外部请求会直接报错**。

这等于免费获得一道防线——如果某个用例"偷偷"访问了真实网络，
测试会立刻失败并告诉你"有个请求没打桩"。
所以我没加"兜底 mock"，反而**故意保留这个报错行为**。

### 面试怎么答

> "外部依赖全部打桩，保证测试不联网、不需要 Key、不烧额度。
> 而且我特意依赖 respx『未匹配就报错』的特性——
> 这样一旦有用例偷偷发真实请求，测试会立刻失败，等于多一道防线。"

---

## 五、覆盖率的正确用法

### 是什么

**覆盖率 = 被测试执行到的代码行数 ÷ 总代码行数**。

### 怎么理解它

覆盖率**只能说明"没测什么"，不能说明"测得好不好"**。

```python
def test_什么也没验证(self):
    await client.get("/api/v1/notes")     # 执行了，覆盖率 +1
    # 但没有任何 assert —— 接口返回 500 也照样通过
```

这个用例让覆盖率上涨，却毫无价值。**高覆盖率 ≠ 高质量**。

### 本项目的做法

**1. 设置红线但不迷信**

```ini
[coverage:report]
fail_under = 60      # 低于 60% 直接让 CI 失败
```

**2. 排除口径要诚实、并写明理由**

```ini
# 注意用 */ 前缀匹配：Windows 下实际路径是 app\tasks\xxx.py（反斜杠），
# 只写 app/tasks/* 会因为分隔符差异匹配不上
omit =
    */app/scripts/*     # 一次性入口脚本
    */app/tasks/*       # Celery 调度胶水，业务逻辑在 services 层（已覆盖）
    */__init__.py
```

> ⚠️ **不要为了数字好看去排除业务代码**——那是自欺欺人。

**3. 关注"哪些关键路径没覆盖"而不是"百分比"**

看这份清单比看总分有用得多：

```
app/services/agent.py        24%   ← Agent 编排未覆盖（需 mock LLM）
app/services/weather_news_service.py  16%  ← 资讯采集流程未覆盖
```

### 面试怎么答

> "我设了 60% 的红线，但我不会拿它当 KPI。
> 覆盖率只说明『没测什么』，不说明『测得对不对』——
> 一个没有断言的用例也能刷高覆盖率。
> 我更关注的是：核心业务路径有没有覆盖、异常分支有没有覆盖。
> 比如排除项我明确写了理由：Celery 任务只是调度包装，逻辑在 service 层已覆盖。"

---

## 附：本项目测试相关速查

```bash
# 跑全部测试 + 覆盖率
cd backend && python -m pytest

# 只跑单元测试（快）
python -m pytest tests/unit -q

# 只跑接口测试
python -m pytest tests/api -q

# 跑单个文件 / 单个用例
python -m pytest tests/unit/test_amap_client.py
python -m pytest "tests/api/test_notes.py::TestCrud::test_创建笔记"

# 失败时看详细堆栈（默认已开 -v）
python -m pytest -x --tb=long      # -x 遇到第一个失败即停

# 覆盖率 HTML 报告（生成到 htmlcov/index.html，可点进去看哪行没覆盖）
python -m pytest --cov-report=html
```

| 配置项 | 作用 |
| --- | --- |
| `asyncio_mode = auto` | async 测试函数不用手写 `@pytest.mark.asyncio` |
| `DB_USE_NULLPOOL=1` | 测试环境启用 NullPool，避免跨事件循环报错 |
| `respx_mock` fixture | 拦截 httpx 请求（respx 提供） |
| `client` fixture | ASGI 直连客户端，不启动真实服务器 |
| `auth_headers` / `admin_headers` | 已登录的请求头（普通用户 / 管理员） |
