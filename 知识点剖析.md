# 知识点剖析（面试向）

> 这份文档记录项目开发过程中**真正踩过的坑**和背后的原理。
> 每个主题都按「是什么 → 为什么 → 本项目怎么做 → 面试怎么答」组织，
> 目的是让你能不背稿地讲清楚自己的项目。
>
> 📖 **没接触过自动化测试、觉得「测试到底在干嘛」？**
> 先读 [`测试入门指南.md`](./测试入门指南.md)——那份是零基础向的，
> 用大白话 + 本项目真实 Bug 案例讲清测试是什么、442 个用例分别在守什么。
> 本文档则偏技术深度，讲「为什么这么设计」。

---

## 目录

| 主题 | 来源 | 一句话价值 |
| --- | --- | --- |
| [一、自动化测试体系](#一自动化测试体系) | Day 29 | 项目从"没测试"到 379 个用例、61% 覆盖率 |
| [二、异步测试的事件循环陷阱](#二异步测试的事件循环陷阱) | Day 29 | 最难查的一类 bug，面试能讲出深度 |
| [三、测试数据隔离两种方案](#三测试数据隔离两种方案) | Day 29 | 为什么选独立库而不是事务回滚 |
| [四、Mock 外部依赖](#四mock-外部依赖) | Day 29 | 断网也能跑全量测试 |
| [五、覆盖率的正确用法](#五覆盖率的正确用法) | Day 29 | 别拿它当 KPI，也别为了数字造假 |
| [六、前端测试体系](#30--前端测试体系vitest-单测--playwright-e2e) | Day 30 | Vitest 与 Playwright 的分工、选择器稳定性排序 |
| [七、CI 持续集成](#31--ci-持续集成) | Day 31 | `pytest` 与 `python -m pytest` 的路径差异、lint 规则的取舍 |
| [八、CD 自动化部署](#32--cd-自动化部署) | Day 32 | 镜像标签即回滚能力、备份的一致性时机、部署脚本的失败处理 |
| [九、可观测性](#33--可观测性错误边界健康检查与指标) | Day 33 | 错误边界为什么必须 class、健康检查的 503 语义、吞异常的隐蔽性 |
| [十、通知基础设施](#34--通知基础设施) | Day 34 | AsyncSession 并发挂起、通道降级语义（skipped≠failed）、Web Push 失效清理 |
| [十一、每日智能早报](#35--每日智能早报) | Day 35 | 主动推送的成本意识、定时任务排期的两种思路、注入 session factory |
| [十二、天气预警实时推送](#36--天气预警实时推送) | Day 36 | 去重键设计、跨进程通信、SSE vs WebSocket、心跳与代理缓冲 |
| [十三、生活指数接入](#37--生活指数接入) | Day 37 | 先验权限再动手、排序 vs 筛选、文档与实现不一致、提权漏洞 |
| [十四、PWA 与 Service Worker](#38--pwa-service-worker-的两种职责与一个坑) | Day 38 | 一个 scope 一个 SW、已注册≠已接管、缓存分级、act 包裹 |
| [十五、推送偏好与订阅管理](#39--推送偏好假开关收口与幂等窗口) | Day 39 | 假开关识别、偏好收口、幂等窗口替代状态、本地装了≠部署有 |

---

## 一、自动化测试体系

### 是什么

**测试 = 一段替你做检查的代码**。改完功能后跑一遍，它会告诉你"原来能用的功能还被没被改坏"。

### 为什么需要

想象一个场景：项目有 65 个接口，你改了一个"天气规则"文件。怎么知道没影响别的地方？

- **没测试**：手动点一遍全部页面，半小时过去了，还不一定点全
- **有测试**：`pytest` 跑一下，2 分钟告诉你"379 个检查点全部通过"或"第 47 个坏了"

### 测试金字塔

```
        /\        端到端测试（E2E）   少而慢，验证完整流程
       /  \       —— 如 Playwright 点页面
      /____\
     /      \     集成/接口测试        中等，验证多个模块协作
    /        \    —— 如「调接口 → 落库 → 查回来」
   /__________\
  /            \  单元测试              多而快，验证单个函数
 /______________\ —— 如「23.7℃ 应命中凉爽档」
```

比例建议：**单元 70% / 集成 20% / E2E 10%**。
本项目 Day 29 完成的是下面两层（单元 + 接口），E2E 安排在 Day 30。

### 本项目怎么做

```
backend/tests/
├── conftest.py                  # 全局 fixture（测试库、客户端、鉴权头）
├── unit/                        # 14 个文件：纯逻辑与解析层
│   ├── test_amap_client.py
│   ├── test_outfit_engine.py
│   └── ...
└── api/                         # 5 个文件：接口 + 鉴权 + 数据隔离
    ├── test_auth.py
    ├── test_itinerary.py
    └── ...
```

### 面试怎么答

> "项目原本没有测试，我补了一套：19 个文件、379 个用例，覆盖率 61%。
> 单元测试覆盖规则引擎、解析层、评分逻辑；接口测试覆盖鉴权边界和多租户隔离。
> 外部依赖（天气、地图、LLM）全部用 respx 打桩，所以**断网也能跑全量**。
> 测试还帮我抓出一个真 bug：天气规则遍历时 break 了，导致『雷阵雨』拿不到防雷提示。"

---

## 二、异步测试的事件循环陷阱

> ⭐ 这是本项目 Day 29 最难查的问题，**面试讲这个很加分**。

### 现象

单独跑某个测试文件 → **全绿**。
和别的文件一起跑 → **大面积 ERROR**：

```
RuntimeError: Event loop is closed
self = <ProactorEventLoop running=False closed=True debug=False>
```

### 原理：什么是事件循环

异步代码（`async/await`）需要一个"调度员"来安排任务，这个调度员就是**事件循环（event loop）**。

打比方：事件循环是**餐厅里的一个服务员**。
- 你点菜（发起 await）→ 服务员去后厨下单 → 顺便服务别的桌 → 菜好了端给你
- 这个服务员只服务"自己负责的区域"

### 坑在哪

`pytest-asyncio` 的默认行为是：**每个测试用例开一个新的服务员（新 event loop）**。

而数据库连接池（asyncpg）的连接是**绑定在具体服务员身上的**：

```
用例 1：服务员 A 建了连接 ──→ 用完放回池子（连接仍记着"我归 A 管"）
用例 1 结束：服务员 A 下班了 ❌
用例 2：服务员 B 从池子里拿连接 ──→ 连接说"我归 A 管" ──→ 💥 报错
```

### 为什么难查

1. **单独跑不复现**——只有一个用例时，连接和 loop 是一对一的，没问题
2. **错误信息误导**——报的是 `Event loop is closed`，看起来像"代码里手动关了 loop"，实际是连接池的锅
3. **修一半还会剩一半**——我第一反应是给"测试自己的 engine"加 NullPool，但接口测试走的是**应用自己的 engine**（`app.db.session.engine`），它仍是默认连接池

### 解决方案

```python
# app/db/session.py
_use_null_pool = os.getenv("DB_USE_NULLPOOL", "").lower() in ("1", "true", "yes")

engine = create_async_engine(
    settings.DB_URL,
    pool_pre_ping=True,
    **({"poolclass": NullPool} if _use_null_pool else {}),
)
```

**NullPool = 不用连接池**：每次用完就关掉，下次重新建。
这样连接永远属于"当前这个服务员"，不会跨 loop 复用。

```
生产环境：用连接池（建连接有开销，复用更划算）
测试环境：用 NullPool（用例频繁切换 loop，复用反而会出事）
```

### 同源问题

项目在 Celery 侧**早就踩过同一个坑**——Celery 任务里反复 `asyncio.run()` 也会创建新 loop，
当时的解法也是"模块级复用一个 loop + NullPool 独立引擎"。

> **这就是"同一个原理在不同场景反复出现"的典型**，所以我说它是同类坑。

### 面试怎么答

> "多文件跑测试时遇到 `Event loop is closed`。根因是 pytest-asyncio 每个用例新建事件循环，
> 而 asyncpg 的连接池把连接绑定在创建它的 loop 上，跨 loop 复用就报错。
> 有意思的是，单独跑不复现——因为只有一个 loop。
> 解法是让 engine 用 NullPool，每用完即关、不跨 loop 复用。
> 这个坑项目在 Celery 里也踩过，本质是同一个问题。"

---

## 三、测试数据隔离两种方案

### 问题

测试要往库里写数据，怎么保证**不污染开发数据**、又**不互相干扰**？

### 方案对比

| 方案 | 做法 | 优点 | 缺点 |
| --- | --- | --- | --- |
| A. 同库 + 事务回滚 | 每个用例在事务里跑，结束回滚 | 快、无额外库 | **挡不住"自建 session 并 commit"的代码** |
| B. 独立测试库 | 建单独的库，用例结束 TRUNCATE | 彻底隔离、真实 | 需建库、稍慢 |

### 为什么本项目选 B

项目里有些 Service 在未传 `db` 时会**自己建 session**：

```python
# chat_history_service.add_message
owns_db = db is None
session = db or AsyncSessionLocal()      # ← 自建 session
...
await s.commit()                          # ← 真实提交，事务回滚拦不住
```

方案 A 依赖"所有写操作都走同一个被回滚的事务"，
但只要有一处自建 session 并 commit，数据就**真写进开发库了**。

所以选 B：`weather_db_test` 独立库 + 每用例 `TRUNCATE ... RESTART IDENTITY CASCADE`。

```python
@pytest_asyncio.fixture(autouse=True)
async def _clean_tables():
    yield
    async with _engine.begin() as conn:
        names = ", ".join(f'"{t.name}"' for t in reversed(Base.metadata.sorted_tables))
        await conn.execute(text(f"TRUNCATE TABLE {names} RESTART IDENTITY CASCADE"))
```

> 注意 `RESTART IDENTITY`：让自增 ID 归零，避免用例之间产生"ID 顺序"的隐性依赖。

### 面试怎么答

> "我用独立测试库 + 每用例清表。本来想用事务回滚（更轻量），
> 但项目里有 Service 会自建 session 并 commit，回滚挡不住这类写入，
> 会真写进开发库，所以换成独立库物理隔离。"

---

## 四、Mock 外部依赖

### 是什么

**Mock = 假的替身**。测试时把"真实的外部服务"换成"你控制的假响应"。

打比方：演员试镜时不会真的去拍外景——用一个绿幕代替。
**绿幕 = Mock**，试镜 = 测试。

### 为什么必须 Mock

| 如果不 Mock | 后果 |
| --- | --- |
| 依赖网络 | 断网/服务抖动 → 测试随机失败（"flaky test"） |
| 依赖 API Key | CI 上没配 Key → 全挂 |
| 依赖真实数据 | 天气天天变 → 断言写不了 |
| 消耗额度 | 和风/高德/Tavily 都有限额，跑测试就把额度烧光 |

### 本项目怎么做

用 `respx` 拦截 httpx 请求：

```python
async def test_解析搜索结果(self, respx_mock):
    respx_mock.post("https://api.tavily.com/search").mock(
        return_value=httpx.Response(200, json={"results": [{"title": "广州天气", ...}]})
    )
    results = await ws._search_tavily("广州天气", 5)
    assert results[0]["title"] == "广州天气"
```

对 Service 层的函数依赖，用 `monkeypatch`：

```python
monkeypatch.setattr(home_service, "fetch_weather", fake_fetch_weather)
```

### 一个巧妙的设计

`respx` 有个默认行为：**没被 mock 的外部请求会直接报错**。

这等于免费获得一道防线——如果某个用例"偷偷"访问了真实网络，
测试会立刻失败并告诉你"有个请求没打桩"。
所以我没加"兜底 mock"，反而**故意保留这个报错行为**。

### 面试怎么答

> "外部依赖全部打桩，保证测试不联网、不需要 Key、不烧额度。
> 而且我特意依赖 respx『未匹配就报错』的特性——
> 这样一旦有用例偷偷发真实请求，测试会立刻失败，等于多一道防线。"

---

## 五、覆盖率的正确用法

### 是什么

**覆盖率 = 被测试执行到的代码行数 ÷ 总代码行数**。

### 怎么理解它

覆盖率**只能说明"没测什么"，不能说明"测得好不好"**。

```python
def test_什么也没验证(self):
    await client.get("/api/v1/notes")     # 执行了，覆盖率 +1
    # 但没有任何 assert —— 接口返回 500 也照样通过
```

这个用例让覆盖率上涨，却毫无价值。**高覆盖率 ≠ 高质量**。

### 本项目的做法

**1. 设置红线但不迷信**

```ini
[coverage:report]
fail_under = 60      # 低于 60% 直接让 CI 失败
```

**2. 排除口径要诚实、并写明理由**

```ini
# 注意用 */ 前缀匹配：Windows 下实际路径是 app\tasks\xxx.py（反斜杠），
# 只写 app/tasks/* 会因为分隔符差异匹配不上
omit =
    */app/scripts/*     # 一次性入口脚本
    */app/tasks/*       # Celery 调度胶水，业务逻辑在 services 层（已覆盖）
    */__init__.py
```

> ⚠️ **不要为了数字好看去排除业务代码**——那是自欺欺人。

**3. 关注"哪些关键路径没覆盖"而不是"百分比"**

看这份清单比看总分有用得多：

```
app/services/agent.py        24%   ← Agent 编排未覆盖（需 mock LLM）
app/services/weather_news_service.py  16%  ← 资讯采集流程未覆盖
```

### 面试怎么答

> "我设了 60% 的红线，但我不会拿它当 KPI。
> 覆盖率只说明『没测什么』，不说明『测得对不对』——
> 一个没有断言的用例也能刷高覆盖率。
> 我更关注的是：核心业务路径有没有覆盖、异常分支有没有覆盖。
> 比如排除项我明确写了理由：Celery 任务只是调度包装，逻辑在 service 层已覆盖。"

---

## 附：本项目测试相关速查

```bash
# 跑全部测试 + 覆盖率
cd backend && python -m pytest

# 只跑单元测试（快）
python -m pytest tests/unit -q

# 只跑接口测试
python -m pytest tests/api -q

# 跑单个文件 / 单个用例
python -m pytest tests/unit/test_amap_client.py
python -m pytest "tests/api/test_notes.py::TestCrud::test_创建笔记"

# 失败时看详细堆栈（默认已开 -v）
python -m pytest -x --tb=long      # -x 遇到第一个失败即停

# 覆盖率 HTML 报告（生成到 htmlcov/index.html，可点进去看哪行没覆盖）
python -m pytest --cov-report=html
```

| 配置项 | 作用 |
| --- | --- |
| `asyncio_mode = auto` | async 测试函数不用手写 `@pytest.mark.asyncio` |
| `DB_USE_NULLPOOL=1` | 测试环境启用 NullPool，避免跨事件循环报错 |
| `respx_mock` fixture | 拦截 httpx 请求（respx 提供） |
| `client` fixture | ASGI 直连客户端，不启动真实服务器 |
| `auth_headers` / `admin_headers` | 已登录的请求头（普通用户 / 管理员） |

---

# 31 · CI 持续集成

## 一、CI 到底解决什么问题

**是什么**：每次 push 代码，机器自动跑一遍「检查 → 测试 → 构建」，全过才允许合并。

**为什么需要**：没有 CI 的团队是靠人自觉的——

| 没有 CI | 有 CI |
| --- | --- |
| 有人忘记跑测试就推了 | 机器每次必跑，不依赖自觉 |
| 本地能跑、别人拉下来跑不了 | 固定环境执行，消除"我这儿是好的" |
| 合并后才发现主干挂了 | PR 阶段就拦住，主干永远是绿的 |
| 「上一个人改坏了什么」靠猜 | 精确指出是哪次提交、哪个用例挂了 |

**关键认知**：CI 的价值不在于"能自动跑测试"，而在于**把质量约束变成不可绕过的事实**。
测试写得再好，只要可以"这次先不跑"，它就形同虚设。

## 二、GitHub Actions 的核心概念

| 概念 | 含义 | 本项目 |
| --- | --- | --- |
| workflow | 一个 yml 文件 = 一条流水线 | `.github/workflows/ci.yml` |
| job | 流水线里的一个任务，**默认并行** | ruff / pytest / 前端 / 镜像构建 |
| step | job 里的一个步骤，**串行且共享环境** | checkout → setup → install → run |
| runner | 跑 job 的临时虚拟机 | `ubuntu-latest` |
| service | 给 job 配套的容器服务 | PostgreSQL（测试要用真库） |
| matrix | 一套配置批量展开成多个 job | 4 个镜像并行构建 |
| artifact | job 产出的文件，可下载 | 覆盖率报告 |

**几个容易踩的点**：

- **job 之间默认并行、彼此隔离**：前一个 job 装的依赖，后一个 job 里没有，
  所以每个 job 都要自己 checkout + setup
- **`needs` 控制依赖顺序**：镜像构建必须等测试全过才跑（`needs: [lint, test, frontend]`）
- **`concurrency` 取消旧运行**：连续推送时，前一次还没跑完的会被取消，
  避免"旧提交的检查把新提交的结果覆盖掉"

## 三、`pytest` 与 `python -m pytest` 的行为差异（面试可讲的坑）

这是本次 CI 排查中**最有价值的一个知识点**，因为它在本地几乎不可能复现。

```bash
# 本地习惯这么跑 —— 一直是绿的
python -m pytest

# CI 里这么跑 —— 直接挂
pytest
```

**差异根源在 `sys.path` 的第一个元素**：

| 调用方式 | `sys.path[0]` | 能否 `from app import ...` |
| --- | --- | --- |
| `python -m pytest` | **当前工作目录** | ✅ 能 |
| `pytest`（console script） | **脚本所在目录**（venv/Scripts 或 /usr/local/bin） | ❌ 不能 |

`python -m` 的本质是"把模块当作程序入口运行"，Python 会把 cwd 放进 `sys.path`；
而 console script 是一个独立的可执行入口，`sys.path[0]` 是那个可执行文件所在目录。

**为什么这个坑特别阴**：

1. 本地开发习惯用 `python -m pytest`（或 IDE 点什么按钮），永远复现不了
2. 一旦 conftest 导入失败，**pytest 的退出码是 4（usage error）**，
   报错信息看起来像"参数写错了"，而不是"模块找不到"
3. @ 排查方向会被误导到"是不是 --cov 参数不对 / 插件没装"

**修复**：`pytest.ini` 里声明 `pythonpath = .`，让 pytest 主动把项目根加进 `sys.path`。
这样**两种调用方式行为一致**，把差异彻底消除。

> **延伸**：同类问题也出现在 `celery`、`gunicorn`、`uvicorn` 等命令上——
> 凡是有 console script 入口的工具，都可能遇到"直接调命令找不到项目模块"。
> 通用解法是让项目**可安装**（`pip install -e .` + `pyproject.toml` 的 packages 配置），
> 或者显式声明 pythonpath。

## 四、lint 规则的取舍：会开也要会关

配 lint 最容易犯的错是"规则开到最满"，结果一次报出几百条无关痛痒的问题，
最后所有人都选择无视——**那还不如不配**。

本项目的取舍原则：

**开启**：能发现真 bug 的（`F` 未使用变量/未定义名、`B` 易错写法）
或能自动修的（`I` 导入排序、`UP` 新语法）。

**谨慎开启**：`BLE`（裸 `except Exception`）——本可以不开，
但因为项目里**已经写了 30+ 处 `# noqa: BLE001` 标注**，不开反而会让这些标注
被 `RUF100` 判为"无用"而**自动删掉**，丢掉"这里是有意捕获全部异常"的信息。

> 这是一个反直觉的教训：**启用一条规则的动机，可能不是为了检查新代码，
> 而是为了保护老代码里已有的意图标注。**

**明确关闭**：`ARG`（未使用参数）——在测试 fixture（参数常为副作用）和
FastAPI 路由函数（参数由框架按名字注入）里报了 70+ 条误报。

```toml
# ruff.toml 里的说明
# 说明：没有启用 ARG（未使用参数）。试过之后发现它在**测试文件**（fixture
# 注入常常只为副作用）和 **FastAPI 路由函数**（参数由框架按名字注入）里
# 会报出 70+ 条误报，属于"关掉比开着更有价值"的规则。
```

**另外**：自动修复（`--fix`）用之前要看清 `RUF100` 这类"删除型"规则会删什么，
**先补规则再修复**，否则容易把有意标注当垃圾清掉。

## 五、依赖版本：`>=` 与 `==` 该用哪个

| 场景 | 建议 | 理由 |
| --- | --- | --- |
| 运行依赖 | `>=` + 定期手动升级 | 保持能拿到安全补丁，灵活性优先 |
| 测试/构建工具 | `==` 精确锁定 | **CI 是否绿取决于它们的行为**，本地与 CI 必须一致 |

本项目的实测教训：`requirements-dev.txt` 原本写 `pytest>=8.0.0`，
本地 venv 是几天前装的，CI 每次装**当时的最新版**——两边版本可能不同，
导致"本地绿、CI 红"且无法复现。改为 `==` 锁定后，CI 与本地完全一致。

## 六、如何在拿不到日志的情况下排查 CI

**约束**：GitHub 的完整 Actions 日志需要登录才能查看，自动化环境没有账号。

**解法分三步**：

1. **最小化探针**：先推一个只做 `echo` + `checkout` 的 workflow。
   它过了 → 说明 runner 环境正常，问题在 job 配置里；它挂了 → 环境/权限层面问题。
   这一步能立刻砍掉一半的排查空间。

2. **让 CI 自己把错误搬到能看见的地方**：日志要登录，但**注解区不需要**。
   用 workflow command 把关键信息输出成注解：

   ```bash
   echo "::error::pytest 退出码 $code"
   head -12 pytest-output.log | while read -r l; do echo "::error::[头部] $l"; done
   tail -12 pytest-output.log | while read -r l; do echo "::error::[尾部] $l"; done
   echo "::notice::pytest $(pytest --version)"
   ```

   **取首尾两段**是有讲究的：用法错误的原因写在最前面，
   测试失败的信息在最后面，只取一头会漏。

3. **这个机制应该长期保留**，不只是临时调试——它把"读日志"这个动作
   从"需要登录"变成"打开页面就能看"。

---

# 30 · 前端测试体系：Vitest 单测 + Playwright E2E

## 一、测试金字塔在前后端的落位

```
        /\        Playwright E2E（22 个）—— 真实服务、真实 LLM，最慢最脆，只测核心路径
       /--\       Testing Library 组件测试（18 个）—— 渲染逻辑、交互状态
      /----\      Vitest 单元测试（23 个）—— 纯函数、拦截器，毫秒级
     /------\     pytest 后端测试（379 个）—— 单元 / 接口两层
```

前端测试的**分层原则**与后端一致，但边界更清楚：

- **纯函数/工具**：必须高覆盖（utils 100%），没有 DOM、没有 mock，测试就是文档
- **组件**：只测「渲染结果与状态流转」，不测内部实现（不查 useState 的值，查屏幕上出现了什么）
- **页面/布局**：**不在单测层测**——它们的价值在于路由、鉴权、接口联动的集成行为，
  单测层写只会得到一堆 mock 剧本（"当 API 返回 X 时显示 Y"），是**对实现的复述**而不是验证。
  交给 E2E 打真实服务更划算

> 收窄覆盖率口径不是作弊：把"由 E2E 覆盖更合适"的部分从单测统计里排除，
> 让 84% 这个数字真正反映「单测该守住的代码」的覆盖情况。

## 二、Testing Library 的核心心法：查"用户看得见的东西"

```tsx
// ❌ 测实现：组件内部状态、类名、props 透传
expect(component.state.loading).toBe(false)

// ✅ 测行为：用户视角——屏幕上有什么、点了之后有什么变化
await user.click(screen.getByText('近 30 天'))
expect(screen.getAllByText('实况').length).toBeGreaterThan(0)
```

`getByText` / `getByRole` / `getByPlaceholder` 都是以用户感知为准的查询；
`getByTestId` 是最后手段（用户看不见 testid）。

**两大陷阱**（本项目都踩了）：

1. **strict mode**：`getByText('我的行程')` 命中导航栏菜单项 + 页面标题两个元素
   会直接抛错（而不是返回第一个）——这是故意的，强迫你写精确的选择器
2. **假 Elements**：antd Segmented 渲染了一个**视觉隐藏**的 radio input，
   能被查询到但点不了（Playwright 对不可见元素拒绝 click，直到超时）

## 三、Playwright 选择器稳定性排序

```
getByRole('button', { name: '保 存' })     ← 最优先：语义 + 可访问名，重构 UI 不碎
getByPlaceholder('如：迪士尼一日游')        ← 次优：表单场景很稳
page.locator('.jp-serif', { hasText })     ← 兜底：语义拿不到时用稳定类名
getByText('登录')                          ← 危险：极易撞名（导航/标题/按钮）
page.locator('.ant-picker-cell-in-view')   ← 最危险：绑死 antd 内部类名，升级必碎
XPath                                      ← 基本不用
```

实战补充：antd 会给**两个字的按钮自动插空格**（「登录」→「登 录」），
精确文本匹配会莫名失败——用正则 `/登\s*录/` 才稳。
这也是「为什么同一个文件里注册用例能过、登录用例超时」这类诡异现象的答案。

## 四、E2E 的稳定性设计（反 flaky）

E2E 最大的成本不是写，而是**让它每次跑都绿**。本项目用了这几招：

| 手段 | 做法 |
| --- | --- |
| 数据隔离 | 每个用例 `uniqueName()` 注册全新账号，用完即弃——不猜上一条测试留下的状态 |
| 串行执行 | `workers: 1` + `fullyParallel: false`，避免并发写库互相干扰 |
| 宽松断言 | 只断结构不断数值：断「温度卡片出现了」，不断「温度等于 29.9」（天气在变） |
| 动态时间 | 所有日期相对 `new Date()` 计算，测试不随日历过期 |
| 时序留白 | AI 流式回复渲染完 ≠ 落库完成（流结束才写库），reload 前显式等待 |
| 失败留痕 | `screenshot/video/trace` 全部 `only-on-failure` / `retain-on-failure` |

**探活也要讲究**：E2E 开始前先确认服务在跑，并给出清晰报错
（"请先 docker compose up -d"），否则 22 个用例全部超时 90 秒，等 33 分钟才告诉你挂了。

## 五、Vitest vs pytest 速查对照

| 概念 | pytest | Vitest |
| --- | --- | --- |
| 断言 | `assert x == y` | `expect(x).toBe(y)` |
| mock 函数 | `mocker.patch()` | `vi.fn()` + `vi.mock('模块路径')` |
| mock 模块 | - | `vi.mock('./api/weather', () => ({...}))` |
| 时间冻结 | `freezegun` | `vi.useFakeTimers()` + `setSystemTime` |
| 用例参数 | fixtures | `beforeEach` / 工厂函数 |
| 覆盖率 | pytest-cov | `--coverage`（v8 provider） |
| DOM 测试 | - | jsdom + Testing Library |

本项目没让 vitest 注入全局（`globals: false`），
`describe/it/expect` 显式 import——和 ESLint 的 no-undef 规则天然兼容，
也省得改 tsconfig 的 types。

## 附：前端测试速查

```bash
cd frontend-user

npm test            # vitest 单测（跑一次）
npm run test:cov    # 单测 + 覆盖率（HTML 报告在 coverage/index.html）
npm run test:e2e    # Playwright E2E（需先 docker compose up -d）
npm run test:e2e:ui # Playwright 可视化调试模式

# 只跑一个 E2E 文件 / 用例（-g 按名字过滤）
npx playwright test e2e/auth.spec.ts
npx playwright test -g "新用户可注册"

# 看失败录像回放（HTML 报告）
npx playwright show-report
```

---

# 32 · CD 自动化部署

## 一、CI 与 CD 的关系：发布门禁

CI 解决「验证」，CD 解决「发布」。两者衔接的关键设计：

```
push 到 master
   → lint / test / 前端 全部通过（发布门禁，needs 硬性保证）
   → 才允许构建并推送镜像到镜像仓库
   → 部署机一条命令拉取上线（失败自动回滚）
```

**核心原则：测试没过的代码不配被发布。** 这条约束用 GitHub Actions 的
`needs` 字段硬性保证，而不是靠流程约定——机器强制的规则才不会被忘掉。

本项目把「构建验证」和「发布」合并成一个 job（PR 时只构建、push 时才推送），
避免同一批镜像构建两次（后端镜像 1.3GB，重复构建很贵）。

## 二、镜像标签策略 = 回滚能力

**回滚的本质是"让服务跑回旧版本的镜像"**，所以标签设计直接决定回滚好不好用：

| 标签 | 作用 | 为什么需要 |
| --- | --- | --- |
| `latest` | 始终指向主干最新 | 部署机的默认拉取目标 |
| `sha-4c3abe9` | 与某次提交一一对应 | 回滚时精确指定"回到哪一次提交" |
| `pg17`（数据库镜像固定） | 随 PG 版本走 | 数据库镜像不随应用发版变化，避免部署时连带重建 |

> 只打 `latest` 一个标签的项目，回滚时只能"回到上一个 latest"——
> 但你根本说不清那是哪次提交的代码。**`sha-` 标签让每次发布都可追溯、可复现。**

## 三、compose 的 build / image 并存模式

```yaml
build: {context: ./backend}                                # 本地开发：按源码构建
image: ghcr.io/ticnix/weather-backend:${IMAGE_TAG:-latest} # 部署：拉取镜像
```

两者并存时行为互补：

| 命令 | 行为 |
| --- | --- |
| `docker compose up -d --build` | 构建并打上 image 指定的标签（本地开发） |
| `IMAGE_TAG=sha-xxx docker compose pull` | 拉取指定版本（部署机） |
| `IMAGE_TAG=sha-xxx docker compose up -d` | 用指定版本启动 |

**回滚 = 换个旧 tag 把部署命令再跑一遍**。不需要单独的"回滚系统"，
回滚就是一次普通的部署，这大幅降低了回滚的维护成本和心智负担。

## 四、备份的时机：一致性比"做了备份"更重要

部署脚本的步骤顺序是刻意安排的：

```
停应用容器 → pg_dump 备份 → 拉镜像 → 启动 → 健康检查
     ↑            ↑
  数据不再变化   拿到的是一致性快照
```

如果先备份再停应用：备份期间应用还在写数据，`pg_dump` 拿到的可能是
"写到一半"的状态。**备份的正确性取决于时机，而不只是命令对不对。**

配套的两个防呆：

1. 备份完成后立即校验（非空 + `gzip -t` 完整性）——防止"备份了个寂寞"
2. 只保留最近 N 份并自动清理——防止备份目录悄悄吃满磁盘

## 五、部署脚本的失败处理：每个退出点都要想清楚

部署可能在**任何一步**失败，每一步的"正确反应"不一样：

| 失败点 | 正确反应 | 为什么 |
| --- | --- | --- |
| 备份失败 | **中止**，什么都不动 | 数据安全优先于上线 |
| 拉镜像失败 | **中止**（服务本来就停着，无副作用） | 大概率是网络/tag 不存在 |
| 启动失败 | 回滚 | 新镜像可能有问题 |
| 健康检查失败 | **自动回滚**，回滚后再真实探测一次 | 上线标准就是健康检查 |
| 回滚也失败 | 退出码 2，人工介入 | 这时数据库已有部署前备份 |

**退出码是给"下一个环节"看的**：`0` 成功 / `1` 已回滚 / `2` 需人工。
监控系统或 CI 只需要看退出码就知道要不要告警。

## 六、镜像仓库的可见性是个隐蔽坑

GHCR 的用户级包**默认私有**，匿名 `docker pull` 会 401。
而部署脚本通常是匿名拉取的——"CI 明明推送成功了，部署机却拉不到"。

解法：CI 推送后用内置 `GITHUB_TOKEN` 调 packages API 把包改为公开
（本项目镜像不含敏感层，公开无风险）；或手动去包页面改一次。
首次部署前建议先用 `docker pull` 匿名试拉一次，确认可见性没问题。

## 附：部署操作速查

```bash
# 日常部署（主干最新）
./deploy.sh

# 部署指定提交的版本
IMAGE_TAG=sha-4c3abe9 ./deploy.sh

# 回滚 = 部署旧版本（同一条命令）
IMAGE_TAG=sha-a1b2c3d ./deploy.sh

# 演练回滚流程（模拟新版本健康检查失败，验证自动回滚）
FORCE_HEALTHCHECK_FAIL=1 ./deploy.sh

# 指定环境配置
ENV_FILE=./backend/.env.prod ./deploy.sh

# 查看当前运行的是哪个版本
cat .last_deployed_tag
```

---

# 33 · 可观测性：错误边界、健康检查与指标

## 一、错误边界：为什么 React 至今只有 class 实现

没有错误边界时，**任何一个组件的渲染错误都会卸载整棵组件树**——
一个列表接口返回了意外结构（`data.items` 为 null），`.map` 一抛，
用户看到的就是整站白屏，连导航都没有。

两级兜底的分工：

| 层级 | 挂载点 | 价值 |
| --- | --- | --- |
| 布局内 | Content 包住 `<Outlet/>` | 单页崩溃只换内容区，**导航可用**——用户能自己走掉 |
| 全局 | App 外层 | 最后防线，兜底页带重试与回首页 |

**为什么必须 class**：`getDerivedStateFromError` / `componentDidCatch`
没有 hooks 等价物。这是 React 少数几个"hooks 覆盖不了"的场景。

「重试」的实现值得注意：**不是刷新页面，而是清空边界内的 error 状态**，
让 React 重新渲染子树——瞬时错误（一次接口抖动）即可原地自愈。

## 二、健康检查的语义：503 是给机器看的

健康检查有**两类读者**，需求完全不同：

| 读者 | 要什么 | 
| --- | --- |
| 人（开发调试） | 失败原因明细 |
| 部署脚本/负载均衡（机器） | **一个能判断"能不能服务"的状态码** |

所以本项目 `/health` 的失败路径返回 **503 + 明细**：状态码给机器
（deploy.sh 的 `curl -f` 只认 5xx 为失败，依赖挂了还返回 200 会让回滚机制失明），
明细给人。

**「不查什么」与「查什么」同样重要**：

- 查：DB / Redis（自己说了算的核心依赖，各 2s 超时）
- 不查：和风/高德等外部 API——第三方抖动是常态且业务层有降级，
  纳入健康检查会让**部署回滚被第三方故障误触发**

另一个细节：Redis 检查用独立短连接而非复用 `cache.py` 的客户端——
后者是「一次失败永久短路」的降级设计，短路后**无法反映实时状态**，
而健康检查的职责恰恰是反映实时状态。同一个 Redis，两处连接，两种语义，不冲突。

## 三、吞异常比不处理更隐蔽

改造前所有 Celery 任务都这样写：

```python
try:
    ...干活...
except Exception as e:
    logger.exception("失败")
    return {"ok": False, "error": str(e)}
```

看起来"稳"——worker 不会崩。但代价是：

1. Celery 把任务标记为 **SUCCESS**（返回值不算失败）
2. `autoretry_for` **永远不会触发**（没有异常可捕获）
3. 失败统计、告警、重试全部失效
4. 定时任务可以**每天悄悄失败一年**而无人知晓

**正确姿势**：任务体只管干活，异常向上抛——Celery 自己会把异常变成
FAILURE 状态 + 触发重试 + 触发 `task_failure` 信号。日志里 `logger.exception`
保留上下文即可，不需要捕获后"假装成功"。

配套的告警用**信号**而非每个任务里写一遍：

```python
@task_failure.connect
def _alert_on_task_failure(sender=None, task_id=None, exception=None, retries=0, **_):
    alert_logger.error("Celery 任务最终失败",
        extra={"task": getattr(sender, "name", str(sender)), "task_id": task_id, "retries": retries},
        exc_info=exception)
```

一处代码覆盖所有任务，未来新增任务自动被覆盖。

## 四、可测试性的两个坑（都在本次测试中踩到）

**① async 测试里不能调用"内部起循环"的同步代码**

Celery 任务体用 `_run()`（`run_until_complete`）执行异步逻辑。在
pytest-asyncio 的 async 测试里直接调任务会报
`Cannot run the event loop while another loop is running`——
当前线程已有运行中的循环，任何循环都无法再 `run_until_complete`。

这是**测试环境特有**的：生产的 worker 线程里没有运行中的循环。
解法是把这类测试写成同步测试。

**② patch 局部导入必须 patch 源模块**

```python
def _job():
    from app.services.weather_news_service import collect_all  # 函数内导入
```

patch `app.tasks.news_tasks.collect_all` 是**静默无效**的——任务模块上
根本没有这个名字。必须 patch `app.services.weather_news_service.collect_all`
（`from X import Y` 在函数执行时才从 X 取 Y 的当前值）。

## 五、结构化日志的白名单

logging 的 `extra` 会把字段挂到 LogRecord 上，但 record 自带
`funcName/lineno/threadName/processName...` 几十个属性——
Formatter 若无脑遍历输出，每行 JSON 会膨胀到不可读。
白名单（`method/path/route/status/duration_ms/task/retries`）
在「可控字段数」与「可检索性」之间取平衡。

另有一个 Windows 特有的坑：stdout 重定向到文件时编码是本地编码（GBK），
`ensure_ascii=False` 的中文全变问号——`sys.stdout.reconfigure(encoding="utf-8")`
修复（Linux 容器原生 UTF-8，此操作无副作用）。

---

# 34 · 通知基础设施

## 一、AsyncSession 不支持并发——gather 会让它挂起

Day 34 最大的坑。初版统一发送入口想当然地并行化：

```python
async def notify_user(db, ...):
    web_push, email = await asyncio.gather(
        _send_web_push(db, ...),   # ┐
        _send_email(db, ...),      # ┘ 两个协程共用同一个 db
    )
```

**实测直接挂起**（pytest 超时，无任何报错输出）。

原因：`AsyncSession` 底层持有一个数据库连接，**不支持并发使用**——
两个协程同时 `execute` 会争抢同一个连接的事务状态，SQLAlchemy 2.x
在并发访问时会挂起或抛 `InvalidRequestError`。

规则：**看到 asyncio.gather 先问一句"协程们共享了什么状态"**——
session、连接池连接、文件句柄这类非线程安全/非并发安全的资源，
要么各用各的实例，要么老老实实顺序执行。

本项目最终选择顺序执行：通知发送除真实外发（推送/SMTP，已在 to_thread 里）
外都是毫秒级本地查询，并行化没有收益。

顺带：这种坑的可怕之处在于**挂起而非报错**——不抛异常、不留日志，
测试只会"卡住超时"。排查时先怀疑共享状态，而不是到处找 try/except。

## 二、通道降级语义：skipped ≠ failed

| 状态 | 含义 | 例子 |
| --- | --- | --- |
| `sent` | 发送成功 | |
| `failed` | 发了但失败 | SMTP 连不上、推送被拒 |
| `skipped` | **通道未启用**，没发 | SMTP 未配置、用户没订阅、没绑定邮箱 |

把 skipped 记成 failed 的危害：邮件通道本来就没启用，
错误率却是 100%——**告警会天天响，直到没人再看告警**。
产品状态（功能没开）与故障状态（发了失败）必须分开。

## 三、Web Push 的失效订阅必须清理

推送服务（FCM/Mozilla autopush）对已失效订阅返回 **404/410**：
用户清了浏览器数据、卸载了站点授权、换了设备。

失效订阅留着的问题：每次群发都要对它做一次注定失败的加密+请求，
还会被推送服务按无效请求**限流**，拖累其他用户的送达率。

处理：捕获 `WebPushException` 时读 `response.status_code`，
命中 404/410 就把订阅从库里删掉。这是「发送服务」的自我维护能力，
不清理的推送系统上线几周后送达率会持续劣化。

## 四、浏览器授权的两个约束

1. **requestPermission 必须由用户手势触发**（点击按钮）——页面加载就弹授权
   会被浏览器直接拒绝，且用户体验极差
2. **`userVisibleOnly: true` 是规范要求的**：不允许"静默推送"，
   每条推送必须展示可见通知——这也是为什么服务端 payload 必须带 title/body

权限被拒绝（denied）后的正确行为是**友好降级**：告知用户去浏览器
地址栏锁形图标里改权限，而不是反复调 requestPermission（永远 denied）。

## 五、路由前缀的项目约定

本项目各 router 的 prefix 写的是**完整路径**（`/api/v1/users`）而不是
`/users` + 全局挂载时补前缀。新增路由忘记带 `/api/v1` 会导致
「测试 404 但代码看起来完全正确」——一致性约定本身就是防呆。

---

# 35 · 每日智能早报

## 一、主动推送的成本意识

查询是「用户来问」，推送是「你去找用户」——后者的**打扰成本**是真实存在的。
所以早报设计处处在收敛打扰：

| 收敛点 | 做法 |
| --- | --- |
| 次数 | 每天最多一次，且可整体关闭 |
| 时间 | 用户自选小时（免打扰） |
| 内容 | 无行程时给通用提示，**不让推送为空**；天气源全挂时退化但不静默 |

> 「宁可不发，也不发一条废话」比「每天准时打卡」更难，也更值得。

## 二、定时任务的两种排期思路

需求是「每个用户可自选推送时间」，有两种实现：

| 思路 | 问题 |
| --- | --- |
| 为每个用户注册一个定时任务 | 用户数一多，beat 里就是成千上万个排期，改配置要动态增删 |
| **每小时统一分发一次，按用户设定的小时过滤** | 时间粒度只能到小时，但实现简单、人多了也不变形 |

选了后者：**粒度损失（小时级）换来的是复杂度恒定**。
这也是「免打扰粒度为什么是小时」的真正原因——是设计取舍，不是偷懒。

## 三、让服务可测试：注入 session factory

`dispatch_to_users(hour, factory)` 不在内部创建数据库引擎，而是由调用方注入。
好处：
- Celery 任务传自己的 NullPool 引擎（与 Web 进程隔离）
- 测试传测试库的 factory，**不必 mock 引擎**就能跑通完整逻辑

比起「服务内部自建引擎 + 测试里 patch 掉」，注入版让测试关注业务而非基础设施。

---

# 36 · 天气预警实时推送

## 一、去重键为什么用「业务字段哈希」而不是上游 ID

`fingerprint = sha1(city|level|title)`：

- **上游没有稳定 ID**：和风的预警接口返回的是「当前生效的预警列表」，
  每次调用顺序、字段都可能变，也没有可长期引用的 ID
- **业务字段反而更稳**：同一城市同一级别的同一标题，就是同一条预警；
  上游文案小改（多一个标点）会被当成新预警——这是可接受的误差，
  相比「漏推」它更不容易造成严重后果

设计去重键时要想清楚：**误判成新（多推一次）和误判成旧（漏推）哪个更糟**。

## 二、跨进程才是在线推送的真难点

```
Celery worker 进程           Web 进程
  发现新预警         →        某个用户的 SSE 长连接
      └──── 进程内队列在这里是无效的 ────┘
```

进程内的 `asyncio.Queue` 只能在同一进程内传递。要跨进程，
必须有**外部中介**：Redis Pub/Sub（本项目）、MQ、或者 HTTP 回调。

选 Redis 的理由：项目本来就依赖它，零新组件；Pub/Sub 是「广播」语义，
正合「一条预警所有在线连接都该知道」。代价是**没有持久化**——
订阅者掉线期间的消息会丢，所以它只能做实时通道，
**主通道必须是能离线送达的 Web Push**。

> 这也是为什么 Day 36 做的是「双通道」而不是「SSE 一把梭」：
> 只有实时通道的系统，用户关掉页面就收不到预警了。

## 三、SSE 与 WebSocket 怎么选

| | SSE | WebSocket |
| --- | --- | --- |
| 方向 | 服务端 → 客户端（单向） | 双向 |
| 协议 | 就是 HTTP | 需要升级握手 |
| 断线重连 | 浏览器内置（EventSource） | 自己实现 |
| 代理/网关友好度 | 好（普通 HTTP 响应） | 常需额外配置 |
| 适用 | 通知、进度、日志流 | 聊天、协同编辑 |

预警推送是**纯服务端推送**，SSE 语义正好，且不引入新协议栈。
聊天室那种双向交互才值得上 WebSocket。
（本项目的 AI 对话用的是 SSE，也正是这个道理。）

## 四、一个容易忽略的鉴权限制

`EventSource` **不能自定义请求头**，只能把凭证放 URL——
token 出现在 URL 里会进 Nginx access log、浏览器历史、Referer。
所以改用 `fetch` + `ReadableStream` 读 SSE 响应：

- 能带 `Authorization` 头（安全）
- 代价：SSE 的分行解析、重连退避、事件类型过滤都得自己写

**事件类型过滤**是这次真实踩到的 bug：服务端会定期发心跳
`{"type":"ping"}` 保活，客户端若不按 `event === 'weather_alert'` 过滤，
就会把心跳当成预警弹出来（「undefined天气预警」）。
这类 bug 单测发现不了——它出在两端交互上，必须真连起来跑。

## 五、心跳与代理缓冲

两件必须做的事，否则"实时"会失效：

| 措施 | 不加会怎样 |
| --- | --- |
| 服务端定期发心跳（15s） | 空闲长连接被 Nginx / 浏览器按 60s 超时掐断 |
| 响应头 `X-Accel-Buffering: no` | Nginx 把 SSE 攒成一次性输出，彻底失去实时性 |

## 六、付费 API 的现实约束要老老实实兜底

实测发现：和风的 `/warning/now` **需要付费订阅**，免费 Key 返回 403。
如果不处理，「配置为 qweather」时预警列表恒为空——**功能等于从未启用**，
而且日志里只有一句 warning，很容易被忽略。

`fetch_alerts_with_fallback()` 的处理：
1. 先用主源，有结果就用（付费源更权威）
2. 主源没有 → 用免费的 Open-Meteo 阈值规则兜底
3. 主源本身就是免费源 → 不重复调用

> 教训：**接第三方能力时，先确认自己的账号权限够不够**，
> 否则会写出「代码正确、测试通过、线上永远不触发」的功能。

---

# 37 · 生活指数接入

## 一、接第三方能力：先验证权限，再动手

Day 36 的教训是「代码写对了，但账号没权限，功能等于从未启用」。
所以接入任何第三方接口前，先花一分钟实测：

```bash
curl "https://<host>/v7/indices/1d?type=0&location=101280101&key=..."
# -> 200，返回 16 类指数
```

**这一步能省掉一整天的白干**。接口文档写"支持"不等于你的套餐支持。

## 二、排序，而不是筛选

给用户展示个性化内容有两种做法：

| 做法 | 问题 |
| --- | --- |
| 筛选：只给他"相关的"几项 | 替用户做了决定，他偶尔想看的就没入口了 |
| **排序：全给，最相关的排前面** | 需要一套合理的权重，但没有信息损失 |

选了排序。权重的来源必须**有据可依**，不能凭空猜：

```
① 基础权重     — 穿衣/紫外线/运动这类人人都会看
② 体质偏好     — 用户自己填的（怕冷/怕热）
③ 行程活动     — 用户自己排的行程（爬山 → 运动指数重要）
④ 指数自身结论 — 穿衣指数已经算出「炎热」，据此给防晒加权
```

第 ④ 条是最省钱的：**指数本身就是天气的结论**，
不需要为排序再多打一次天气接口。

**排序必须稳定**：同权重时按编码排序，否则同样的数据每次渲染顺序不同，
界面会"跳"，用户会以为出了 bug。

## 三、时序表：唯一约束 + upsert

指数接口只返回"当天"，不保留历史。要支持「这几天紫外线在变强吗」，
必须自己按天存：

```sql
UNIQUE (city, date, type_code)
```

写入时先查同格再决定 insert / update——一天内拉多次（Celery 轮询）
不会撑出重复行，而指数随天气变化时（转晴后洗车指数从"不宜"变"适宜"）
又能正确覆盖。

> 什么时候用 upsert 表：数据有**天然的业务主键**（城市+日期+类型），
> 且会被反复刷新。没有这个键就只能靠去重键哈希（如 Day 36 的预警）。

## 四、文档与实现不一致时，以实测为准

实测发现和风的指数编码与文档不符：

| 编码 | 文档 | 接口实际返回 |
| --- | --- | --- |
| 7 | 花粉过敏指数 | 过敏指数 |
| 10 | 空调开启指数 | **空气污染扩散条件指数** |
| 11 | 空气污染扩散指数 | **空调开启指数** |

后果极其隐蔽：给「怕热」加权空调指数时写 `"10"`，
结果把**空气污染扩散**排到了前面——界面上只是"顺序有点怪"，
不盯着真实数据对比根本发现不了。

**防御手段是结构性测试**，而不是"下次小心点"：

```python
def test_偏好引用的编码都已定义(self):
    for pref, codes in PREFERENCE_HINTS.items():
        assert not (set(codes) - set(TYPE_NAMES))
```

这类测试断言的是「引用完整性」而不是业务结果，
专治「写错不报错、只是行为怪」的问题。

## 五、测试库的 schema 漂移

模型加了字段（`users.body_preference`）后，本地遗留的测试库报 `UndefinedColumn`。
现象很有迷惑性：**代码没错、开发库迁移也跑了，就是测试库"老了"**。

原因：`Base.metadata.create_all` 只创建**缺失的表**，不会给已存在的表补列。

| 方案 | 取舍 |
| --- | --- |
| 每次会话 `drop_all` + `create_all` | 简单彻底；测试库本就是一次性的 |
| 在测试库跑 alembic | 更贴近生产，但慢，且迁移出错会污染测试结论 |

选了前者，并加护栏：**库名必须以 `_test` 结尾才允许重建**，避免误伤真实库。

## 六、顺手发现的提权漏洞

给前端找「改资料」接口时发现 `PUT /api/v1/users/{user_id}` **没有权限校验**，
而 `UserUpdate` 里带着 `role`——任何登录用户都能把自己改成管理员。

```python
# 修复思路：默认拒绝，按角色放行字段
if not is_admin:
    allowed = {"nickname", "email", "avatar", "body_preference"}
    if set(fields) - allowed:
        raise HTTPException(403, "无权修改字段：...")
```

> 教训：**写"用户可自助修改"的接口时，先问一句"他能改哪些字段"**。
> 把整个 Update schema 直接透给普通用户，往往就是提权入口。

---

# 38 · PWA：Service Worker 的两种职责与一个坑

## 一、一个 scope 只能有一个 Service Worker

这是 PWA 改造时最容易踩的坑：项目里已经有一个 SW 处理 Web Push，
而离线缓存也需要 SW。浏览器对同一 scope **只认一个**，
各注册一个的结果是后注册的顶掉先注册的——**推送或离线缓存必然坏一个**，
而且往往不会报错，只是"某天发现推送不来了"。

解法是合并成一个 SW：

```js
// 生成的 sw.js 内部
importScripts("/push-sw.js")   // 推送逻辑
precacheAndRoute([...])        // 离线缓存逻辑
```

> 判断依据：**同一类资源（同 origin + 同 scope）的"总管"只能有一个**。
> 类似的还有：一个页面只能有一个 `document.title`、一个端口只能被一个进程监听。
> 遇到这类约束，正解是"合并职责"而不是"想办法绕开"。

## 二、「已注册」不等于「已接管」

Service Worker 有几个状态，混起来会写出很迷惑的测试：

```
注册（registered）→ 安装（installing）→ 激活（activated）→ 接管页面（controlled）
                                        ↑ 只有到这里才开始拦截请求
```

两个关键点：

1. **断言要等 `active`，不是等 getRegistration() 有返回值**
   刚注册完的 SW 还在 `installing`，此时读 `reg.active` 是 null
2. **首次加载时页面通常不被 SW 接管**
   于是那一次发出的接口请求**绕过 SW**，缓存里什么都没有。
   想验证"断网可用"，必须先在**已被接管**的状态下在线加载一次

> 这一条解释了那个最反直觉的现象：明明"SW 注册成功了"，
> 断网后却依然只有空态——因为要缓存的数据从来没经过 SW。

## 三、缓存策略：按"数据变化速度"分级

| 数据 | 策略 | 理由 |
| --- | --- | --- |
| 静态资源（JS/CSS/图标） | 预缓存 | 构建产物带 hash，一变就是新文件 |
| 天气/首页数据 | StaleWhileRevalidate | 变化快但不能白屏，**旧数据也比没有强** |
| 其他 GET 接口 | NetworkFirst | 新鲜度优先，断网才回退 |
| 写操作 | 不缓存 | 缓存 POST/PUT 是灾难 |

三个必须显式处理的细节：

1. **判 `method === 'GET'`**：不判的话登录、提交反馈这类请求也会被卷进缓存
2. **导航回退 `index.html`**：否则断网刷新 `/itinerary` 这类子路由直接 404
3. **`navigateFallbackDenylist: [/^\/api\//]`**：别让接口请求也回退到 HTML，
   否则前端拿到的是一段 HTML 而不是 JSON，报错会非常莫名其妙

## 四、离线体验：应用内提示 > 静态离线页

传统做法是准备一个 `offline.html`，断网时展示它。但对 SPA 来说这不是最优解：
**应用外壳本来就在缓存里、能正常打开**，此时把用户丢到一个静态页反而更糟。

更好的做法是在应用内明确告知：「当前处于离线状态，展示的是最近缓存的数据」。
用户需要的是**解释**（为什么数据是旧的），而不是一个中断流程的页面。

## 五、安装引导必须区分平台

```js
// Chromium
window.addEventListener('beforeinstallprompt', (e) => {
  e.preventDefault()        // 拦掉浏览器自带的迷你条
  deferredEvent = e         // 存起来，等用户点我们的按钮再 prompt()
})
// prompt() 必须由用户手势触发，不能自动弹
```

iOS Safari **没有这个事件**，只能给「分享 → 添加到主屏幕」的文字指引。
不做平台判断的话，iOS 用户（占比不小）永远看不到任何安装入口。

另外要判断 `display-mode: standalone`——已经从桌面图标启动的，
再弹"添加到桌面"就很好笑了。

## 六、测试：状态更新在 React 事件系统之外

单测里手动 `window.dispatchEvent(new Event('offline'))` 后断言不到 DOM 变化，
因为 **setState 发生在 React 的事件系统之外**，React 不会同步 flush。
解法是 `act()` 包住派发：

```ts
const goOffline = () => act(() => window.dispatchEvent(new Event('offline')))
```

> 凡是"手动触发的事件"（网络状态、storage、matchMedia、自定义事件），
> 测试里都要考虑 act 包裹。这是 React 测试的通用规律，不限于 PWA。

## 七、关于 Lighthouse

Lighthouse 的 PWA 审计项其实就是几条可编程的检查，可以直接用浏览器 API 核对：

```ts
navigator.serviceWorker.controller                    // 是否接管
document.querySelector('link[rel="manifest"]')        // manifest 是否存在
manifest.icons 含 192x192 与 512x512 且能 200 拿到     // 图标是否合格
context.setOffline(true) 后 reload 仍有内容            // 离线是否可用
```

用 E2E 固化这几条，比"某人某天手动跑一次 Lighthouse 截图存档"更有价值——
**它会跟着 CI 一直跑**。

---

# 39 · 推送偏好：假开关、收口与幂等窗口

## 一、做设置项之前，先问「它背后有发送方吗」

Day 39 要给「行程提醒」加开关，排查后发现：**这个类型根本没有发送方**。
于是开关做出来也是死的——关掉它什么都不会变，打开它什么也不会来。

这类东西有个共同特征：**界面看起来完成了，功能其实不存在**。
用户不会报 bug（"我关了还是没收到"听起来像正常的），但产品的可信度在流失。

判断方法很简单：**为每个设置项写出「打开时会发生什么」和「关闭时会阻止什么」**。
写不出来，就说明缺的不是设置，而是功能本身。

## 二、偏好校验要收口在唯一发送入口

```python
async def notify_user(db, user_id, title, body=None, url=None, category="system"):
    allowed, reason = await category_allowed(db, user_id, category)
    if not allowed:
        return await _log_suppressed(...)   # 仍然留痕
```

把校验放在各调用方（早报、预警、行程提醒各自判一次）看起来更"解耦"，
但那是**把正确性押在调用者的记性上**。新增一个发送方时的默认行为
会变成"绕过所有开关"——而这类 bug 用户只会说「我不是关了吗」。

收口之后，新调用方唯一要做的事就是声明 `category="alert"`，
不声明也有默认值（`system`，不受开关约束，适合诊断类通知）。

**被拦下时仍然写 skip 记录**：用户在历史里能看到「这条被我的设置拦下了」，
答案在用户手里，不在"是不是坏了"的猜疑里。

## 三、定时任务的幂等：用时间窗口代替状态标记

「出发前 30 分钟提醒一次」听起来需要一个 `reminded_at` 字段。
换个角度：**任务每 10 分钟跑一次，窗口取「距出发 25~35 分钟」**——
一个出发时刻必然只落在一个 10 分钟窗口里，于是天然只提醒一次。

好处不只是少一个字段：

| 方案 | 风险 |
| --- | --- |
| 记 `reminded_at` | 状态与事实可能不一致（写入失败、重试、并发） |
| 时间窗口 | 无状态，重跑多少次结果都一样（幂等） |

**设计定时任务时先想「能不能让重复执行本身无害」**，
而不是先想「怎么记住已经执行过」。

## 四、时区：别用服务器本地时间算「今天」

Celery 容器默认 UTC，而用户在东八区。用 `date.today()` 算"今天的行程"，
在 UTC 16:00 之后就会算错一天——傍晚出发的行程会被漏掉。

所以显式用 `ZoneInfo("Asia/Shanghai")`，并且**在函数签名上留出 `now` 参数**，
测试可以直接构造任意时刻（不用 freeze time 全局状态）。

## 五、部分更新 vs 全量提交

前端每个开关是**独立保存**的。如果接口要求全量提交：

```
用户点开 A（提交 {a:true, b:true}）→ 立刻点关 B（提交 {a:true, b:false}）
若两次请求乱序到达：A 的结果可能把 B 覆盖回 true
```

单机手点很难复现，但它会在弱网/多端同时操作时出现。
做法是 `PUT /prefs` **只改传进来的字段**（schema 全部可选），
并且专门写一条用例：改一个字段后，其他字段必须保持原样。

> 凡是"部分更新"的接口，都应该有这条用例守着。

## 六、本地装了 ≠ 部署有

这次最值得记的坑：后端容器启动即崩 `ModuleNotFoundError: pywebpush`。

原因：Day 34 引入 Web Push 时，`pywebpush` 装进了本地 venv，
但**没写进 `requirements.txt`**。本地测试全绿，容器里直接起不来。

它顺带暴露了第二个问题：**后端容器自 Day 34 起就没重建过**，
所以那段时间的 E2E 一直在打旧代码——新增的接口在容器里根本不存在。

两条经验：

1. **凡是代码里直接 `import` 的库，必须在依赖清单里显式声明**，
   不能依赖别人的传递依赖（httpx 这次是靠 langchain 带进来的，同样补上了）
2. **改了后端就要重建后端容器**，否则联调时会出现
   「前端改了后端没部署」的乌龙——表现是接口 404 或参数不生效

## 七、测试超时要看环境，不只是调大数字

前端单测出现「单跑必过、全量偶挂」，连既有的 ErrorBoundary 用例也挂。
原因不是代码：**本机同时跑着 6 个 docker 容器**，antd 组件渲染明显变慢，
默认 5s 用例超时 / 1s 异步查询等待不够用。

处理方式是统一放宽（`testTimeout: 20s` + `configure({ asyncUtilTimeout: 5000 })`），
并且在注释里写清"为什么放宽"——否则下一个人看到 20s 会以为是在掩盖真实问题。

> 判断标准：**如果失败与并发/负载相关、且单跑稳定通过，那是环境问题；
> 如果单跑也挂，那才是代码问题。** 前者调超时，后者查代码，别混。
