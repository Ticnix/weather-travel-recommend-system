
fastapi>=0.115.0
uvicorn[standard]>=0.31.0
pydantic-settings>=2.6.0
python-dotenv
python-multipart>=0.0.9
sse-starlette
pyjwt
asyncpg>=0.30.0
pandas>=2.0.0
celery>=5.4.0
redis>=5.2.0
sqlalchemy[asyncio]>=2.0.0
pgvector>=0.3.0
geoalchemy2>=0.15.0
alembic>=1.13.0
langchain-openai>=0.3.0
langgraph>=0.2.60
langchain-core>=0.3.0
mcp>=1.0.0
langchain-mcp-adapters>=0.1.0
# Web Push 发送（Day 34 引入）。之前只装在本地 venv 里没写进来，
# 结果是「本地测试全过、容器启动就 ModuleNotFoundError」——
# 凡是代码里直接 import 的第三方库，必须在这里显式声明，
# 不能依赖别人的传递依赖。
pywebpush>=2.0.0
httpx>=0.27.0
