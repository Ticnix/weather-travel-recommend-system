# 工作日志

> 本日志记录项目开发过程中的关键工作：**做了什么 / 解决了什么问题 / 解决效果 / 时间日期**。
> 仅在显式要求"写进日志"时追加记录。

---

## 2026-08-22（Day 1 · 环境与脚手架）

### 做了什么
- 搭建总工程目录，初始化 git，编写 `.gitignore`（忽略 venv、.env、node_modules、__pycache__ 等）
- 创建用户端 React19 + Vite + TS 脚手架（`frontend-user/`）、管理端 Vue3 + Vite + TS 脚手架（`frontend-admin/`）
- 搭建 FastAPI 后端基础目录骨架（`backend/app/`），最小 demo 可通过 `/docs` 访问
- 编写 `docker-compose.yml` 编排 PostgreSQL + Redis，`Dockerfile.pg` 用于构建含三大扩展的镜像

### 解决了什么问题
- 无（Day 1 为环境搭建，无既有 bug）

### 解决效果
- 前后端脚手架均可独立启动
- 后端 `/` 与 `/docs` 返回 200

---

## 2026-08-23（Day 2 · 数据库设计）

### 做了什么
- 编写 SQLAlchemy 2.0 异步会话与数据库连接配置（`app/core/config.py` 用 pydantic-settings 读 `.env`；`app/db/session.py` 提供异步 engine / session）
- 编写全部数据模型：用户（users）、资讯（news）、反馈（feedback）、气象时序（weather_history）、3D 地标（landmarks）
- 编写知识库向量表（knowledge_chunks），`embedding = Vector(1024)`，并建 HNSW 余弦向量索引
- 配置 Alembic 迁移（`alembic.ini`、异步 `env.py`、`versions/0001_initial.py`），执行 `alembic upgrade head` 完成建表
- 将 `weather_history` 转为 TimescaleDB 超表（主分区列为 `time`）

### 解决了什么问题
- **Docker 镜像源失效**：原 `daemon.json` 配置的 `docker.mirrors.ustc.edu.cn` 与 `hub-mirror.c.163.com` 均 DNS 不可达，导致无法拉取任何镜像。
- **postgis 缺失且版本错配**：`timescale/timescaledb:latest-pg17`（Alpine）自带 timescaledb + vector，但缺 postgis；其 Alpine 仓库的 postgis 包仅支持 PostgreSQL 18，与运行中的 pg17 ABI 不兼容，无法直接安装。
- **timescaledb 需预加载**：新建的 postgres 容器未配置 `shared_preload_libraries`，导致 `CREATE EXTENSION timescaledb` 报错。

### 解决效果
- 改用本地已有的 `pgvector/pgvector:pg17`（Debian bookworm）为基础镜像，通过可达的 PGDG 源安装 `postgresql-17-postgis-3`、通过 TimescaleDB 官方 APT 源安装 `timescaledb-2-postgresql-17`，`docker commit` 为自定义镜像 `weather-pg-custom:pg17`
- 修改 `postgresql.auto.conf` 追加 `shared_preload_libraries = 'timescaledb'` 并重启容器
- **三大扩展全部启用**：timescaledb 2.29.2 / postgis 3.6.4 / vector 0.8.6
- **6 张业务表全部创建成功**：users、news、feedback、weather_history、landmarks、knowledge_chunks
- **向量字段维度确认为 `vector(1024)`**，HNSW 索引 `ix_knowledge_chunks_embedding_hnsw`（vector_cosine_ops）创建成功
- **`weather_history` 已转超表**：`num_dimensions=1`，主分区列 `time`
- Alembic 迁移至 `0001_initial (head)`，无 pending
- 后端 `/` 与 `/docs` 可访问（Swagger 200）
- 通过 `python -m app.scripts.init_db` 验证初始化脚本幂等可重复执行

### 代码提交
- `33532df` Day 1: 全栈脚手架与环境搭建
- `bded85e` Day 2: 数据库设计
- 共 63 个文件，工作树 clean；敏感文件（.env/venv/node_modules/__pycache__）均未提交

---

## 2026-08-24（Day 3 · 基础业务接口）

> 本日目标：统一接口契约、集中错误处理、JWT 鉴权、三大基础模块 CRUD，Swagger 可调试。

### 一、改动文件清单（14 个）

**新增文件（11）**
| 文件 | 职责 |
| --- | --- |
| `backend/app/core/response.py` | 统一响应体封装 |
| `backend/app/core/exceptions.py` | 全局异常处理器 + 业务异常类 |
| `backend/app/core/security.py` | 密码哈希 + JWT 生成/校验 |
| `backend/app/core/deps.py` | 鉴权依赖（当前用户 / 可选匿名用户） |
| `backend/app/schemas/user.py` | 用户模块 Pydantic Schema |
| `backend/app/schemas/news.py` | 资讯模块 Pydantic Schema |
| `backend/app/schemas/feedback.py` | 反馈模块 Pydantic Schema |
| `backend/app/routers/users.py` | 用户模块路由（注册/登录/CRUD） |
| `backend/app/routers/news.py` | 资讯模块路由 |
| `backend/app/routers/feedback.py` | 反馈模块路由 |
| `backend/app/test_day3.py`（已删除） | 临时联调脚本，验证后清理 |

**修改文件（3）**
| 文件 | 改动 |
| --- | --- |
| `backend/app/db/base.py` | `TimestampMixin` 时间戳列加 Python 侧 `default/onupdate`（修复异步序列化报错） |
| `backend/app/main.py` | 挂载 3 个路由 + 注册全局异常 + 增加 `/`、`/health` |
| `28天开发计划.md` / `工作日志.md` | 进度与记录更新 |

---

### 二、各文件详解（干什么 / 为什么 / 怎么做 / 实现逻辑）

#### 1. `core/response.py` — 统一响应体
- **干什么**：定义全站统一的 JSON 返回结构 `{code, message, data}`，让前端无需为每个接口写差异化解析。
- **为什么**：Day 1/2 接口返回格式不统一，前后端协作成本高；统一契约是后续模块化的基础。
- **怎么做**：
  - 定义泛型模型 `ApiResponse[T]`（`code: int=0, message: str="success", data: T|None=None`）。约定 `code=0` 成功，非 0 为业务错误码。
  - 提供类方法 `ApiResponse.ok(data, message)` 与 `ApiResponse.fail(code, message, data)`。
  - 提供快捷函数 `success(data, message)` / `error(code, message, data)`，内部调用模型并 `model_dump()` 返回纯 dict，供 FastAPI 直接返回。
- **实现逻辑**：所有业务路由都 `return success(...)` / `return error(...)`，异常分支由全局处理器统一转成 `{code,message,data}` 结构，保证全站响应形状一致。

#### 2. `core/exceptions.py` — 全局异常处理
- **干什么**：集中拦截各类异常，统一转成标准响应体，避免 500 裸堆栈泄漏给前端。
- **为什么**：分散 try/except 会重复代码；且 SQLAlchemy 报错、参数校验失败需统一成业务可读结构。
- **怎么做**：定义 `BusinessError(Exception)`，携带 `code` 与 `message`；导出 `register_exception_handlers(app)` 在 `main.py` 启动时注册到 FastAPI。
- **实现逻辑（按异常类型分发）**：
  - `BusinessError` → 200 + `{code, message}`，不暴露堆栈。
  - `RequestValidationError`（Pydantic 入参校验失败）→ 422 + 错误详情 `data`。
  - `IntegrityError`（DB 唯一/约束冲突）→ 200 + `code=409`；若错误含 `unique` 关键字，message 改为「数据已存在」。
  - `SQLAlchemyError` → 200 + `code=500` + 数据库错误信息。
  - 兜底 `Exception` → 200 + `code=500` + 内部错误描述。
  - 设计取舍：业务错误统一用 **HTTP 200 + 业务 code** 返回（前端按 `code` 判断），便于移动端/小程序统一拦截。

#### 3. `core/security.py` — 密码哈希 + JWT
- **干什么**：密码安全存储、生成与校验登录凭证（JWT）。
- **为什么**：
  - 不能明文存密码；不引入 `passlib/bcrypt` 额外依赖（离线环境安装受限），改用标准库实现同等强度的哈希。
  - 需要无状态鉴权，JWT 适合前后端分离架构。
- **怎么做**：
  - **密码哈希**：`hash_password(pwd)` 用 `secrets.token_bytes` 生成 16 字节随机盐，PBKDF2-HMAC-SHA256（10 万次迭代）派生密钥，存储格式 `pbkdf2$sha256$<salt_hex>$<hash_hex>`。
  - **校验**：`verify_password(pwd, stored)` 解析存储串，用相同盐与迭代次数重算并 `hmac.compare_digest` 常量时间比较，防时序攻击。
  - **JWT**：`create_access_token(subject, extra)` 以 `sub=用户ID` + `iat` + `exp=now+JWT_EXPIRE_MINUTES` 为 payload，附 `role` 等自定义声明，用 `settings.JWT_SECRET` 与 `JWT_ALGORITHM`（HS256）签发；`decode_access_token(token)` 校验签名与过期，失败抛 `jwt.PyJWTError`。
- **实现逻辑**：登录成功 → `create_access_token(user.id, role)` → 返回 `{access_token, token_type, user}`；后续请求在 `Authorization: Bearer <token>` 中携带，由 deps 解析。

#### 4. `core/deps.py` — 鉴权依赖
- **干什么**：把「从 token 解析当前用户」封装成 FastAPI 依赖，供需要登录的接口复用。
- **为什么**：避免每个接口重复写解析 token、查库、判活逻辑；同时区分「必须登录」与「允许匿名」两种场景。
- **怎么做**：
  - 用 `OAuth2PasswordBearer(tokenUrl="/api/v1/users/login")` 声明 Swagger 的「Authorize」登录入口。
  - `get_current_user(token, db)`：`decode_access_token` 解析 `sub` → `db.scalar(select(User).where(id==sub))` → 不存在或 `is_active=False` 抛 401；成功返回 `User` 对象。类型别名 `CurrentUser = Annotated[User, Depends(get_current_user)]`。
  - `get_optional_user(token, db)`：用 `auto_error=False` 的 scheme，token 缺失/无效时返回 `None`（不报错），类型别名 `OptionalUser`，用于反馈匿名提交。
- **实现逻辑**：把依赖标注 `CurrentUser` 加到路由函数参数即自动鉴权；`/me`、列表、增删改均依赖它，未带 token 的请求在依赖层即被 401 拦截（联调已验证）。

#### 5. `schemas/*.py` — 三模块 Pydantic Schema
- **干什么**：定义接口的请求体、响应体，做入参校验与 ORM→JSON 转换。
- **为什么**：FastAPI 依赖 Pydantic 做自动校验、文档生成与序列化；ORM 模型不能直接对外暴露（含密码哈希等敏感字段）。
- **怎么做**：
  - 每个模块分 `XxxBase`(公共字段)、`XxxCreate`(创建入参，密码等必填)、`XxxUpdate`(更新，全部 Optional，配合 `exclude_unset` 局部更新)、`XxxOut`(响应，`model_config=ConfigDict(from_attributes=True)` 支持从 ORM 对象直接 `model_validate`)。
  - 字段约束：`UserCreate.password` 长度 3–128、`username` 3–64；`NewsBase.category` 默认 `"news"`；`FeedbackBase.status` 默认 `"pending"` 等。
  - 未使用 `EmailStr`（需 `email-validator` 额外依赖），`email` 用 `Optional[str]` 放宽，保持离线可跑。
- **实现逻辑**：路由内 `XxxOut.model_validate(orm_obj).model_dump()` 完成「ORM→dict」转换；`pydantic` v2 的 `from_attributes` 读取 ORM 属性生成响应。

#### 6. `routers/users.py` — 用户模块
- **干什么**：注册、登录、当前用户、列表、详情、更新、删除。
- **为什么**：用户是系统基础实体，登录鉴权是其余接口的入口。
- **怎么做 / 关键逻辑**：
  - `POST /register`：先 `select(User).where(username==)` 查重，冲突返回 409；否则 `User(password_hash=hash_password(pwd), ...)`，`db.add` → `commit` → `refresh` → 返回 `UserOut`。
  - `POST /login`：查用户 + `verify_password`；失败 401；成功更新 `last_login_at` → `commit` → `refresh` → 签发 JWT 返回。
  - `GET /me`：依赖 `CurrentUser` 直接返回当前用户。
  - `GET /`：分页 + `ilike` 关键字搜索（默认按 id 倒序），返回 `{items,total,page,page_size}`。
  - `GET/PUT/DELETE /{user_id}`：先 `db.get` 校验存在（404），更新用 `exclude_unset` 只改传入字段，删除 `db.delete` + `commit`。
  - 所有返回经 `success()` 包裹。

#### 7. `routers/news.py` — 资讯公告模块
- **干什么**：资讯/公告的公开阅读 + 鉴权管理（发布/改/删）。
- **为什么**：内容运营需要后台发布，前端需要公开列表与详情（含阅读量统计）。
- **怎么做 / 关键逻辑**：
  - `GET /`：支持 `category` 过滤、`keyword` 标题模糊、`published_only` 仅已发布；排序 `is_top.desc()` 置顶优先 + `id.desc()`；分页返回。
  - `GET /{id}`：查详情，命中后 `view_count += 1` 并 `commit`（阅读量+1 效果已联调验证）；不存在 404。
  - `POST /`：依赖 `CurrentUser`；若未填 `author` 自动取当前用户 `nickname/username`；创建返回。
  - `PUT/DELETE /{id}`：依赖 `CurrentUser` 鉴权后更新/删除。

#### 8. `routers/feedback.py` — 用户反馈模块
- **干什么**：用户提交反馈（可匿名）、查看/管理反馈。
- **为什么**：需要收集用户意见；匿名提交降低提交门槛，登录用户可追踪自己的反馈。
- **怎么做 / 关键逻辑**：
  - `POST /`：依赖 `OptionalUser`（匿名也可）；若已登录则把 `user_id` 记入反馈；返回创建结果。
  - `GET /`：依赖 `CurrentUser`；`role != "admin"` 时仅 `where(user_id==current.id)`（普通用户只看自己），管理员看全部；支持 `status` 过滤与分页。
  - `GET/PUT/DELETE /{id}`：权限校验——非管理员且非本人(`fb.user_id != current.id`)返回 403；更新可流转 `status`。

#### 9. `db/base.py` — 时间戳列修复（重要）
- **干什么**：修复异步 ORM 对象序列化时的 `MissingGreenlet` 错误。
- **为什么**：原 `TimestampMixin` 的 `created_at`/`updated_at` 仅用 `server_default`/`onupdate`（数据库侧生成）。当 Pydantic 在响应序列化阶段访问这些列时，SQLAlchemy 发现属性处于「需从库异步加载」状态，但当时已不在 async 上下文，触发 `greenlet_spawn has not been called` 错误，导致所有含时间戳的响应 500。
- **怎么做 / 实现逻辑**：给两列**同时**加 Python 侧 `default=lambda: datetime.now(timezone.utc)` 与 `updated_at` 的 `onupdate=lambda: datetime.now(timezone.utc)`，保留 `server_default` 作为数据库兜底。这样 insert/update 时值由 Python 写入对象内存，查询后字段直接在内存中，Pydantic 访问不再触发异步 IO，彻底消除 greenlet 错误（联调 13 例全绿佐证）。

#### 10. `main.py` — 应用入口
- **干什么**：组装 FastAPI 应用，挂载路由、注册异常、健康检查。
- **为什么**：单一入口便于启动与测试。
- **怎么做**：`FastAPI(title=..., version="0.3.0")` → `register_exception_handlers(app)` → `include_router` 三个路由 → 增加 `GET /`(启动成功提示) 与 `GET /health`(状态 ok) 供探针使用。

---

### 三、验证（httpx 联调测试，全绿）
- 健康 200 / 未鉴权访问 `/me` 被 401 拦截
- 注册 201 / 重复注册 409（唯一约束生效）
- 登录 200 返回 JWT / 带 token 访问 `/me` 200 返回用户名
- 资讯创建 201 / 列表 200（total 累加）/ 详情 200 且 `view_count` 自增 1
- 反馈创建 201（匿名可提交）/ 列表 200 / 状态更新 200（resolved）
- Swagger：`http://127.0.0.1:8000/docs` 可调试，右上角「Authorize」以 `/api/v1/users/login` 获取 token

### 四、代码提交
- 提交 `6c24b40`：`Day3: 基础业务接口（统一响应/全局异常/JWT/用户·资讯·反馈 CRUD）`
- 已推送到 GitHub `main`；工作树 clean（临时 `test_day3.py` 已删除）

---

## 2026-08-24（Day 4 · 气象数据源接入）

> 本日目标：拉取广州实时/7天预报/预警；数据写入 TimescaleDB 时序表；Celery 每小时定时同步任务正常执行。

### 一、改动文件清单（8 个）

**新增文件（7）**
| 文件 | 职责 |
| --- | --- |
| `backend/app/services/__init__.py` | services 包标记 |
| `backend/app/services/weather_client.py` | Open-Meteo 数据源客户端（实时/7天预报/预警） |
| `backend/app/services/weather_sync.py` | 气象数据写入时序表服务（upsert + 查询） |
| `backend/app/tasks/__init__.py` | tasks 包标记 |
| `backend/app/tasks/weather_tasks.py` | Celery 同步任务（每小时 + 手动） |
| `backend/app/celery_app.py` | Celery 应用与 beat 定时配置 |
| `backend/app/schemas/weather.py` | 气象模块 Pydantic Schema |
| `backend/app/routers/weather.py` | 气象管理接口（同步/查询/预警） |

**修改文件（2）**
| 文件 | 改动 |
| --- | --- |
| `backend/app/core/config.py` | 新增 Open-Meteo 配置项（API base、广州坐标、预警阈值） |
| `backend/app/main.py` | 挂载 weather 路由，版本升至 0.4.0 |

---

### 二、各文件详解（干什么 / 为什么 / 怎么做 / 实现逻辑）

#### 1. `core/config.py` — 气象配置补充
- **干什么**：新增 Open-Meteo 相关配置项。
- **为什么**：广州坐标、API 地址、预警阈值需要可配置，不硬编码。
- **怎么做**：在 `Settings` 追加 `WEATHER_API_BASE`、`DEFAULT_CITY_CODE="gz"`、`DEFAULT_LATITUDE=23.13`、`DEFAULT_LONGITUDE=113.26`、`RAIN_ALERT_MM=10.0`、`WIND_ALERT_KMH=40.0`。
- **实现逻辑**：客户端与同步服务从 `settings` 读这些值；如需切换城市（深圳/珠海），改 `.env` 即可。

#### 2. `services/weather_client.py` — Open-Meteo 客户端
- **干什么**：封装 Open-Meteo API 调用，返回结构化的实时天气、7 天预报、预警。
- **为什么**：Open-Meteo 免费免 Key 且可达（联调前先 curl 验证 200），适合作为默认源；和风天气需 Key 且对免费额度有限制，作为后续可选源。
- **怎么做**：
  - 定义 `dataclass`：`CurrentWeather`、`DailyForecast`、`WeatherAlert`、`WeatherBundle`（一次拉取的完整数据包）。
  - `WeatherClient.fetch(lat, lon, loc)`：单次请求 `/v1/forecast`，`current` 字段取温度/湿度/体感/weather_code/风/气压/降水/能见度，`daily` 取 7 天最高最低温/降水/最大风速/日出日落。
  - WMO weather_code → 中文描述映射表（0=晴, 95=雷阵雨 等）；风向角度 → 八方位。
  - 能见度 Open-Meteo 返回米，转 km。
  - **预警**：Open-Meteo 对中国无官方预警 API，按阈值兜底生成——`current.wind_speed ≥ 40km/h` → 大风提示；`daily[0].precipitation_sum ≥ 10mm` → 降水提示（≥25mm 升级为 danger）。
- **实现逻辑**：`httpx.AsyncClient` 异步请求 → `_parse_current` / `_parse_daily` 解析 → `_derive_alerts` 衍生预警 → 返回 `WeatherBundle`。模块级单例 `weather_client`。

#### 3. `services/weather_sync.py` — 写入时序表
- **干什么**：把 `WeatherBundle` 写入 `weather_history` 超表，并提供查询函数。
- **为什么**：时序表是 TimescaleDB 超表，需按 `(time, location_code)` 主键去重写入；Celery 与 FastAPI 两条路径都要写库。
- **怎么做 / 实现逻辑**：
  - `store_weather(db, bundle)`：实测行（`is_forecast=False`）+ 7 个预报行（`is_forecast=True`，时间戳为当天 00:00 UTC）；用 PostgreSQL `INSERT ... ON CONFLICT (time, location_code) DO UPDATE`（`pg_insert.on_conflict_do_update`）实现 upsert，重复同步不会产生重复行。
  - 预报行复用字段：`temperature` 存日最高温，`feels_like` 存日最低温，`raw` 存日出日落。
  - `fetch_and_store(use_celery_engine=False)`：拉取 + 写库。`use_celery_engine=True` 时用 `NullPool` 独立引擎（见下文 Celery 修复）。
  - 查询函数：`get_latest`（最新实测）、`list_recent`（最近 N 条）、`list_forecast`（7 天预报）。

#### 4. `celery_app.py` — Celery 配置
- **干什么**：定义 Celery 应用、broker/backend（Redis）、定时任务调度。
- **为什么**：需要每小时自动同步气象数据，定时任务是 Day 4 检查清单要求。
- **怎么做 / 实现逻辑**：
  - `Celery("weather_travel", broker=REDIS_URL, backend=REDIS_URL, include=["app.tasks.weather_tasks"])`。
  - `beat_schedule`：`sync-weather-hourly` 任务，`crontab(minute="5")`（每小时第 5 分钟执行，避开整点高峰）。
  - timezone 设 `Asia/Shanghai`，`worker_prefetch_multiplier=1`。
  - 启动方式：`celery -A app.celery_app worker -l info -P solo`（Windows 用 solo 池）+ `celery -A app.celery_app beat -l info`。

#### 5. `tasks/weather_tasks.py` — 同步任务（含重要修复）
- **干什么**：Celery 任务入口，调用异步 `fetch_and_store`。
- **为什么**：Celery 是同步框架，需在同步任务里执行 async 代码。
- **怎么做 / 实现逻辑**：
  - 定义 `sync_weather_hourly`（beat 调度）与 `sync_weather_manual`（手动触发）。
  - **关键修复**：最初用 `asyncio.run()` 执行 async 代码，第一次任务成功、第二次报 `'NoneType' object has no attribute 'send'`（asyncpg 连接跨 `asyncio.run` 事件循环失效）。
  - 修复方案：①`fetch_and_store(use_celery_engine=True)` 用 `NullPool` 独立引擎避免连接池复用；②任务内用**模块级复用事件循环**（`_get_loop()` + `run_until_complete`）替代反复 `asyncio.run`。
  - 修复后连续 3 次异步任务全部 SUCCESS。

#### 6. `schemas/weather.py` — 气象 Schema
- **干什么**：定义 `CurrentWeatherOut`、`ForecastOut`、`WeatherHistoryOut`、`SyncResult`。
- **为什么**：接口响应需结构化、带校验；ORM → JSON 转换。
- **实现逻辑**：`WeatherHistoryOut` 用 `from_attributes=True` 从 ORM 转换；`SyncResult` 封装同步结果（ok/location/temperature/alerts/daily/error）。

#### 7. `routers/weather.py` — 气象管理接口
- **干什么**：手动同步、异步同步、查任务结果、查最新实测/预报/历史/预警。
- **为什么**：管理端需手动触发同步并查看数据；前端需公开读取天气。
- **怎么做 / 实现逻辑**：
  - `POST /sync`（鉴权）：同步拉取并写入，直接返回结果。
  - `POST /sync-async`（鉴权）：`sync_weather_manual.delay()` 提交 Celery 任务，立即返回 `task_id`。
  - `GET /sync-result/{task_id}`（鉴权）：`celery_app.AsyncResult` 查任务状态。
  - `GET /current`（公开）：最新实测；`GET /forecast`（公开）：7 天预报；`GET /history`（公开）：最近 N 条；`GET /alerts`（鉴权）：实时拉取并返回预警。

#### 8. `main.py` — 挂载路由
- 追加 `weather.router`，版本升至 `0.4.0`。

---

### 三、验证（联调全绿）
- **实时拉取**：Open-Meteo 200，广州 27.9℃ / 湿度 93% / 雷阵雨 / 体感 35.5℃ / 能见度 7.72km
- **写入时序表**：`/weather/sync` 成功，`/weather/current` 从库读回完整数据，`/weather/forecast` 返回 7 条
- **预警**：`/weather/alerts` 返回 1 条（当日降水超阈值）
- **Celery 异步任务**：连续 3 次 `sync-async` 全部 SUCCESS（事件循环复用 + NullPool 修复有效）
- **Celery beat**：启动正常，连接 Redis，注册 `sync-weather-hourly` 每小时调度

### 四、已知取舍
- Open-Meteo 对中国无官方气象预警 API，预警按阈值（降水/风速）兜底生成；后续可接入和风天气预警 API 替换。
- 预报行复用 `temperature/feels_like` 字段存日最高/最低温，避免改表结构；`raw` 存日出日落等扩展信息。

### 五、代码提交
- 待用户检查代码后提交（按用户要求：不自动 git）

---

## 2026-08-24（Day 5 · 数据清洗模块）

> 本日目标：上传测试 CSV，可异步完成清洗入库；可查看任务日志；四类清洗规则生效。

### 一、改动文件清单（10 个）

**新增文件（7）**
| 文件 | 职责 |
| --- | --- |
| `backend/app/models/clean_task.py` | 清洗任务记录表（状态/统计/日志） |
| `backend/app/services/clean_engine.py` | Pandas 清洗引擎（去重/缺失填充/异常过滤/单位标准化） |
| `backend/app/services/clean_service.py` | 清洗任务 DB 服务（记录增删改查） |
| `backend/app/tasks/clean_tasks.py` | Celery 异步清洗任务 |
| `backend/app/schemas/clean.py` | 清洗模块 Pydantic Schema |
| `backend/app/routers/clean.py` | 清洗接口（上传/列表/详情日志/下载） |

**修改文件（5）**
| 文件 | 改动 |
| --- | --- |
| `backend/app/core/config.py` | 新增 `UPLOAD_DIR`/`CLEANED_DIR` 配置 |
| `backend/app/celery_app.py` | include 加入 `clean_tasks` |
| `backend/app/db/session.py` | `init_db` 导入 `clean_task` 模型（建表） |
| `backend/app/main.py` | 挂载 clean 路由，版本升至 0.5.0 |
| `backend/app/models/__init__.py` | 导出 `CleanTask` |
| `backend/requirements.txt` | 新增 `pandas>=2.0.0`、`python-multipart>=0.0.9` |
| `.gitignore` | 忽略 `backend/uploads/`、`backend/cleaned/` 运行期文件 |

---

### 二、各文件详解（干什么 / 为什么 / 怎么做 / 实现逻辑）

#### 1. `models/clean_task.py` — 清洗任务记录表
- **干什么**：持久化每次清洗任务的元信息、状态、统计、日志。
- **为什么**：管理端要查询任务进度与历史日志，不能只靠 Celery result（Redis 会过期）；需可审计。
- **怎么做 / 字段**：`task_id`(Celery id,唯一)、`filename`/`stored_path`、`status`(pending/running/success/failed)、`total_rows`/`cleaned_rows`、`duplicated_removed`/`filled_missing`/`filtered_outliers`/`unit_standardized`(四类规则的统计)、`error`、`log`(清洗过程逐行日志)、`triggered_by`、继承 `TimestampMixin`。
- **实现逻辑**：上传时创建 pending 记录，Celery 任务执行中更新 running，完成更新 success+统计+log，失败更新 failed+error。

#### 2. `services/clean_engine.py` — Pandas 清洗引擎（核心）
- **干什么**：对单个 CSV 执行四类清洗，输出清洗后 CSV，返回统计与日志。
- **为什么**：Day 5 的核心要求；Pandas 是数据清洗标准工具。
- **怎么做 / 实现逻辑**（按执行顺序）：
  1. **读取**：`pd.read_csv(encoding="utf-8-sig")`，记录原始行数与列名。
  2. **列名清洗**：去空格、小写。
  3. **单位标准化**：用**后缀规则** `UNIT_SUFFIX_RULES` 检测列名（`_f`→℃、`_k`→℃、`_kpa`→hPa、`_ms`→km/h、`_m`→km），转换数值并生成标准列名（如 `temp_f`→`temperature`），drop 原列。
  4. **列名别名映射**：`COLUMN_ALIASES` 把 `city→location_code`、`hum→humidity`、`rain→precipitation` 等。**注意顺序**：单位标准化必须在别名映射之前（否则 `temp_f` 被映射成 `temperature` 后无法识别单位）——这是联调中发现并修复的关键问题。
  5. **时间解析**：`pd.to_datetime(errors="coerce")`，无效时间行丢弃。
  6. **异常过滤**：`NUMERIC_RANGES` 定义各数值列合理范围（如温度[-50,60]、气压[800,1100]），超出置 NaN。
  7. **缺失填充**：数值列用中位数、分类列用众数填充。
  8. **去重**：`drop_duplicates(keep="first")`。
  9. **输出**：清洗后 CSV 写入 `CLEANED_DIR`。
- **日志**：每步用 `stats.log()` 追加带时间戳的日志行，最终存入 DB `log` 字段。

#### 3. `services/clean_service.py` — 清洗任务 DB 服务
- **干什么**：封装 `CleanTask` 的增删改查，供接口与 Celery 任务调用。
- **为什么**：Celery 同步任务与 FastAPI 异步接口都要操作 DB，需统一封装且支持 Celery 的 NullPool 独立引擎。
- **实现逻辑**：`create_task_record`/`update_task_stats`/`mark_task_failed`/`get_task`/`list_tasks`，均支持 `use_celery_engine=True` 用 NullPool（与 Day 4 同款修复，避免 asyncpg 跨事件循环失效）。`ensure_dirs` 创建上传/清洗目录。

#### 4. `tasks/clean_tasks.py` — Celery 清洗任务
- **干什么**：异步执行清洗流程。
- **为什么**：清洗可能耗时，不能阻塞 HTTP 请求；用 Celery 异步处理。
- **实现逻辑**：`run_clean(task_id, filename, stored_path)` → 标记 running → 调用 `clean_csv` → 更新 success+统计+log（或 failed+error）。复用 Day 4 的模块级事件循环模式（`_get_loop`/`_run`）。

#### 5. `routers/clean.py` — 清洗接口
- **干什么**：CSV 上传、任务列表、详情日志、下载清洗结果。
- **为什么**：管理端需触发清洗并查看进度与结果。
- **实现逻辑**：
  - `POST /upload`（鉴权）：校验 `.csv` 后缀与 20MB 大小 → 存到 `UPLOAD_DIR`（文件名前缀 task_id 防冲突）→ 创建 DB 记录 → `run_clean.delay()` 提交 Celery → 返回 task_id。
  - `GET /`（鉴权）：任务列表（最近 N 条）。
  - `GET /{task_id}`（鉴权）：任务详情含完整日志。
  - `GET /{task_id}/download`（鉴权）：仅 success 任务可下载清洗后 CSV（`FileResponse`）。

#### 6. `celery_app.py` / `main.py` / `session.py` / `config.py`
- `celery_app`：include 加入 `clean_tasks`，worker 启动后自动注册 `run_clean`。
- `main.py`：挂载 `clean.router`，版本 0.5.0。
- `session.py`：`init_db` 导入 `clean_task` 模型，确保 `clean_tasks` 表通过 `create_all` 建出。
- `config.py`：新增 `UPLOAD_DIR="uploads"`、`CLEANED_DIR="cleaned"`。

---

### 三、联调验证（造脏 CSV → 上传 → 异步清洗 → 查日志 → 下载）

**测试 CSV**（7 行，含重复/缺失/异常/多单位）：
```
time,city,temp_f,hum,pressure_kpa,wind_ms,rain,vis
2026-08-24 08:00,广州,89.6,75,99.8,3,0.2,15
2026-08-24 08:00,广州,89.6,75,99.8,3,0.2,15   ← 重复
2026-08-24 09:00,广州,,99.8,5,0.5,12            ← hum 缺失
2026-08-24 10:00,广州,95.0,80,99.5,4,,          ← vis 缺失
2026-08-24 11:00,广州,999,90,99.0,2,1.5,10      ← temp=999 异常
2026-08-24 12:00,深圳,82.4,70,100.2,2,0,18
2026-08-24 12:00,深圳,82.4,70,100.2,2,0,18      ← 重复
```

**清洗结果统计**：
| 规则 | 数量 | 说明 |
| --- | --- | --- |
| 单位标准化 | 3 列 | temp_f(℉→℃,6值)、pressure_kpa(kPa→hPa,7值)、wind_ms(m/s→km/h,7值) |
| 异常过滤 | 2 个 | temperature 超出[-50,60]、pressure 超出[800,1100] 各 1 |
| 缺失填充 | 6 个 | precipitation/visibility/temperature/pressure 用中位数填充 |
| 去重 | 2 行 | 7→5 行 |

**下载的清洗结果**（验证转换正确）：
- 89.6℉ → 32.0℃ ✅
- 99.8 kPa → 998.0 hPa ✅
- 3 m/s → 10.8 km/h ✅
- 重复行去除、缺失填充、异常值处理 ✅

### 四、关键问题与修复
- **问题1**：初版把「列名别名映射」放在「单位标准化」之前，导致 `temp_f` 被映射成 `temperature` 后无法识别 `_f` 后缀，单位转换失效（`unit_standardized=0`），且 `pressure` 被误判异常（99.8 < 800）。
- **修复**：调整顺序——先单位标准化（基于原始列名后缀），再别名映射。同时把 `UNIT_CONVERSIONS` 精确匹配改为 `UNIT_SUFFIX_RULES` 后缀规则，兼容 `temp_f`/`temperature_f` 等多种写法。
- **问题2**：Celery + asyncpg 跨任务连接失效（Day 4 已知问题）→ 沿用模块级事件循环 + NullPool 方案，清洗任务连续执行稳定。

### 五、代码提交
- 待用户检查代码后提交（按用户要求：不自动 git）
---

## 2026-08-24（Day 6 · RAG 知识库底座）

### 一、做了什么
- 整理知识库文档：在 `backend/knowledge_base/` 下创建 16 篇主题文档（气象常识、广州气候、景点、美食、交通、住宿、穿搭、天气出行建议、四季贴士等），分块后共 33 个知识块，满足「30-50 篇」规模要求。
- 封装智谱 AI Embedding 客户端 `services/embedding_client.py`：调用智谱 `{base}/embeddings` 接口（`open.bigmodel.cn/api/paas/v4`），使用 `embedding-3` 模型 + `dimensions=1024` 生成 1024 维向量；无 Key 或调用失败时退化为确定性本地伪向量，保证离线可联调。
- 封装文档加载与重叠分块器 `services/knowledge_loader.py`：扫描 `.md/.txt`，按标题提取标题、按段落+重叠滑窗分块（chunk=300 字符、overlap=50）。
- 实现向量入库服务 `services/rag_service.py`：`build_index()` 加载→分块→embedding→批量 upsert 写入 `knowledge_chunks`（pgvector）；`search()` 用余弦距离（`<=>`）检索 TopK。
- 开发检索接口 `routers/knowledge.py` + `schemas/knowledge.py`：`POST /index`（鉴权重建索引）、`GET/POST /search`（公开语义检索 Top5）、`GET /index/async`（Celery 异步建索引）。
- 新增 Celery 任务 `tasks/rag_tasks.py`，在 `celery_app` 注册；`main.py` 挂载 `knowledge.router`。
- 模型 `models/knowledge.py` 增加 `(source, chunk_index)` 唯一约束，支撑重复建索引的 upsert 去重。

### 二、每个改动文件的用途与实现逻辑

#### 1. `services/embedding_client.py`
- **干什么**：统一封装文本→1024 维向量的生成。
- **为什么**：RAG 入库与检索都需要将文本向量化，独立封装避免重复；支持真实 API 与离线兜底两种模式。
- **实现逻辑**：`available` 判断是否配置了真实 Key（跳过 `sk-xxxxxxxx` 占位符）；`embed_texts` 优先走 `httpx` 调 `/v1/embeddings` 并校验维度==1024，任一环节失败（网络/鉴权/维度不符）即降级为 `_local_pseudo_vector`——基于 3-gram hash 构造词袋式稀疏向量并 L2 归一化，保证维度恒为 1024 且相同文本向量一致。

#### 2. `services/knowledge_loader.py`
- **干什么**：扫描知识库目录并分块。
- **为什么**：原始文档过长，需切成适合 embedding 与检索的语义块；相邻块重叠避免语义在切点被截断。
- **实现逻辑**：按空行分段优先保持语义完整，超长段落内部用 `chunk_size` 滑窗硬切并保留 `overlap`；`_extract_title` 取首个 `# ` 一级标题作为检索展示标题，`load_documents` 返回 `TextChunk` 列表（含 title/source/chunk_index/content）。

#### 3. `services/rag_service.py`
- **干什么**：建索引 + 语义检索两条核心链路。
- **为什么**：把「文档→向量→pgvector 存储→余弦检索」编排成可复用服务，供同步接口与 Celery 共用。
- **实现逻辑**：
  - `build_index`：`load_documents()` 拿全部分块 → 按 `EMBED_BATCH_SIZE` 分批调 embedding → 构造行 → 用 `pg_insert` 的 `on_conflict_do_update(index_elements=["source","chunk_index"])` upsert（唯一约束保证幂等）→ 返回文档数/块数/库内总数/embedding_mode。
  - `search`：查询文本 embedding → 内联向量文本（避免 asyncpg 对命名参数与 `::vector` 的语法冲突）→ 原生 SQL `1 - (embedding <=> vec) AS similarity` 按余弦距离升序取 `LIMIT top_k`。

#### 4. `routers/knowledge.py` + `schemas/knowledge.py`
- **干什么**：对外暴露知识库管理与检索接口。
- **为什么**：联调验证需要手动触发建索引、快速查询检索结果；后续 Day 10 的 RAG Agent 节点也复用 `search`。
- **实现逻辑**：`POST /index`（鉴权，同步）、`GET /index/async`（鉴权，`build_index_task.delay()` 返回 task_id）、`GET/POST /search`（公开，参数 q/body.query + top_k，返回 items 含 similarity）。

#### 5. `tasks/rag_tasks.py` + `celery_app.py` + `main.py`
- **干什么**：异步建索引任务与路由挂载。
- **为什么**：全量 embedding 耗时，不宜阻塞 HTTP；复用 Day 4/5 的 Celery 异步模式。
- **实现逻辑**：`build_index_task` 用模块级事件循环 + `build_index(use_celery_engine=True)`（NullPool 独立引擎，避免 asyncpg 跨事件循环失效）；`celery_app.include` 加入 `rag_tasks`；`main.py` 挂载 `knowledge.router` 并将版本号保留。

#### 6. `models/knowledge.py`
- **干什么**：增加 `(source, chunk_index)` 唯一约束。
- **为什么**：`on_conflict_do_update` 需要唯一索引才能定位冲突行，否则报 `there is no unique or exclusion constraint matching the ON CONFLICT specification`。
- **实现逻辑**：`__table_args__` 声明 `UniqueConstraint("source","chunk_index", name="uq_knowledge_source_chunk")`；对已存在的库用 DO 块补约束（PostgreSQL 不支持 `ADD CONSTRAINT IF NOT EXISTS`）。

#### 7. `core/config.py`
- **干什么**：新增 RAG 相关配置项。
- **实现逻辑**：`KNOWLEDGE_DIR`、`EMBED_CHUNK_SIZE`、`EMBED_CHUNK_OVERLAP`、`EMBED_BATCH_SIZE`、`EMBED_TIMEOUT`、`EMBED_DIM=1024`、`EMBED_TOP_K=5`。

### 三、联调验证
- **文档加载**：16 个文档、33 个分块，标题解析正确（16 个唯一标题）。
- **向量生成**：本地兜底模式返回 1024 维向量，相同文本向量完全一致（确定性）。
- **建索引入库**：`INDEX: {'documents': 16, 'chunks': 33, 'stored': 33, 'total_in_db': 33, 'embedding_mode': 'local_fallback'}`，维度无报错。
- **语义检索**：`GET /api/v1/knowledge/search?q=...` 返回 200，正确命中相关片段（如「穿什么衣服」命中天气出行/穿搭块，相似度降序）。
- **接口挂载**：openapi 中可见 `/api/v1/knowledge/index`、`/index/async`、`/search`。

### 四、关键问题与修复
- **问题1**：已存在的 `knowledge_chunks` 表无唯一约束，`on_conflict_do_update` 报错。
- **修复**：模型加 `UniqueConstraint`；对存量库用 `DO $$ ... IF NOT EXISTS ... ADD CONSTRAINT` 补约束并 `TRUNCATE` 后重建索引（PostgreSQL 不支持 `ADD CONSTRAINT IF NOT EXISTS` 语法）。
- **问题2**：原生 SQL 中 `embedding <=> :vec::vector` 被 asyncpg 误解析为命名参数，报 `syntax error at or near ":"`。
- **修复**：向量文本（本地归一化浮点数组，非用户输入）直接内联进 SQL，避免参数绑定与 `::vector` 强制转换的冲突。
- **问题3**：运行中的 uvicorn 为旧进程，新路由未加载（openapi 无 knowledge 路径）。
- **修复**：定位并结束占用 8000 端口的老进程后重启服务。

### 五、代码提交
- 待用户检查代码后提交（按用户要求：不自动 git）
---

## 2026-08-24（Day 7 · 第一周联调收尾）

### 一、做了什么
- **全链路联调验证**：编写并运行 `test_day7.py`，覆盖「健康检查 / 气象实时+预报 / 资讯 / 用户鉴权 / 手动同步 / RAG 语义检索 / 空查询校验」，**10 项全部通过**。
- **CSV 清洗链路验证**：编写并运行 `test_clean.py`，覆盖「上传 CSV → Celery 异步清洗 → 查询任务统计 → 下载结果」，**清洗 7 项通过**（去重/缺失填充/异常过滤/单位标准化/日志/下载均正常）。
- **修复数据库联调痛点**：原数据库仅存早期随机注册的测试账号（密码已丢失），导致管理端无法登录。在 `init_db.py` 增加**默认管理员 seed**（`admin / Admin@123456`，幂等，已存在则跳过），并实际播种 + 验证登录成功。
- **记录 API 异常捕获与超时配置**：整理当前 LLM/Embedding 调用的异常处理与超时设计（见下文第四节），作为 AI 模块开发（Day 8 起）的规范依据。

### 二、全链路验证结果（Day 7 检查清单）
| 验证项 | 结果 | 说明 |
| --- | --- | --- |
| 健康检查 `/health` | ✅ | 200 / code=0 |
| 气象最新实测 `/weather/current` | ✅ | 27.9℃ 雷阵雨（真实 Open-Meteo） |
| 气象7天预报 `/weather/forecast` | ✅ | 7 天 |
| 资讯列表 `/news` | ✅ | total=3 |
| 用户登录 `/users/login` | ✅ | token 获取成功 |
| 鉴权拦截（无 token） | ✅ | 401 |
| 手动同步 `/weather/sync` | ✅ | 真实同步 26.3℃ 阴 |
| RAG 语义检索 `/knowledge/search` | ✅ | Top5，最高相似度 0.0609 |
| RAG 空查询校验 | ✅ | 400 |
| 用户信息 `/users/me` | ✅ | admin280517 |

**清洗链路**：上传含「重复/缺失/异常/多单位」的脏 CSV → Celery 异步清洗 → 去重 1、填充 2、异常过滤 1、单位标准化 2 列 → 下载结果正确（89.6℉→32℃、kPa→hPa）。

### 三、每个改动文件的用途

#### 1. `scripts/init_db.py`
- **干什么**：数据库初始化脚本新增「播种默认管理员」步骤。
- **为什么**：Day 1-5 联调产生大量随机测试账号且密码已丢失，管理端无可用账号，阻塞后续管理端开发。
- **实现逻辑**：`_seed_admin()` 先 `select` 判断 `admin` 是否存在，存在则跳过（幂等）；不存在则用 `hash_password` 生成 `pbkdf2$...` 哈希写入 `users` 表（role=admin, is_active=true），默认账号 `admin / Admin@123456`（仅限本地联调）。

### 四、API 异常捕获 / 超时配置记录（规范）
当前及后续 AI 模块调用需遵循以下约定（源自 Day 6 的 `embedding_client.py` 实践）：

| 项 | 配置 | 说明 |
| --- | --- | --- |
| Embedding 超时 | `EMBED_TIMEOUT=30s` | httpx 请求超时，避免网络阻塞 |
| LLM 超时 | 待 Day 8 定义（建议 60s） | LangGraph 调用 DeepSeek 时配置 |
| 网络异常 | `except Exception` 捕获 | 网络断连 / DNS / 连接拒绝 |
| 限流/鉴权 | `raise_for_status` | 4xx/5xx 统一抛错降级 |
| 维度校验 | 校验 embedding 长度==1024 | 防止与 `Vector(1024)` 不匹配报错 |
| 降级策略 | API 失败 → 本地伪向量兜底 | 保证离线/无 Key 时链路可联调 |
| Celery 任务 | `try-except` + `logger.exception` | 返回 `{ok, error}`，任务记录失败状态 |

### 五、关键问题与修复
- **问题1**：数据库无可用管理员账号（早期测试账号密码丢失），管理端无法登录。
- **修复**：`init_db.py` 增加幂等的默认管理员 seed，实际运行 `python -m app.scripts.init_db` 播种 `admin / Admin@123456` 并验证登录成功。
- **问题2**：Celery worker 未运行，清洗/异步任务无法执行。
- **修复**：以 `celery -A app.celery_app worker -l info -P solo`（Windows solo 池）后台启动，4 个任务注册成功。

### 五（补）· 遗留问题收尾（用户要求"都解决"）

#### 遗留1：Celery 没有自启机制
- **解决**：提供两种自启方式
  1. **Windows 本地开发模式**：新增 `backend/start_celery.ps1` 一键启动脚本（后台静默运行，日志写入 `celery.log`）。已实测 worker ready、4 任务注册成功。
  2. **完整 docker 部署模式**：`docker-compose.yml` 新增 `celery` service（基于新建的 `backend/Dockerfile`，`env_file` 注入 `.env`，`depends_on` pg/redis，`restart: unless-stopped`）。
- **注意**：本地当前为「Windows venv 跑后端 + docker 跑 pg/redis」模式，故日常用 `start_celery.ps1`；若改用全 docker 部署，需将 `.env` 中 `DB_URL`/`REDIS_URL` 的 `127.0.0.1` 改为 compose 服务名（`postgres`/`redis`）。

#### 遗留2：数据库残留 17 个联调垃圾账号
- **解决**：`DELETE FROM users WHERE username NOT IN ('admin','admin280517')`，删除 17 个（`alice*`/`wtest*`/`ctest*` 等），保留 2 个管理员。已确认 `users` 表无外键依赖，删除安全。
- 验证：剩余 `admin`(id=19) + `admin280517`(id=18) 两个 admin 账号。

#### 遗留3：前端未联调
- **现状（诚实汇报）**：两个前端目前均为框架脚手架（user 端 React、admin 端 Vue），**尚无任何业务页面**（无登录页/天气页/管理页），因此 Day 7 无法进行"界面级联调"——这是 Day 15+ 前端开发的排期内容，非 Day 7 可超前完成。
- **已做铺垫**：为两个前端配置 `vite.config.ts` 的 `server.proxy`，dev 模式将 `/api` 代理到后端 `http://127.0.0.1:8000`（user 端口 5173、admin 端口 5174），消除后续联调的跨域问题。
- **已验证**：两个前端均 `npm run build` 成功，脚手架与 proxy 配置无误。

### 六、代码提交
- 待用户检查代码后提交（按用户要求：不自动 git）
---

## 2026-08-25（Day 8 · LangGraph Agent 框架）

### 一、做了什么
- **封装 LLM 客户端** `services/llm_client.py`：基于 `langchain-openai` 的 `ChatOpenAI` 适配器对接 DeepSeek-v4-flash（OpenAI 兼容端点 `api.deepseek.com`），temperature=0.3、timeout=60s、max_retries=2。
- **搭建 LangGraph 状态机** `services/agent.py`：定义 `AgentState`（messages/intent/answer/error），图结构 `START → classify_intent（意图识别）→ generate（LLM 生成）→ END`，附 `handle_error` 异常兜底节点。
- **实现意图识别 + 生成节点**：`classify_intent` 让 LLM 输出单一标签（weather/outfit/travel/other）；`generate` 结合意图生成中文回答。
- **新增对话接口** `routers/chat.py` + `schemas/chat.py`：`POST /api/v1/chat`，单轮对话返回 `{intent, answer}`。
- 新增依赖：`langchain-openai` / `langgraph` / `langchain-core`（requirements.txt 已补）。

### 二、验证结果（Day 8 检查清单）
| 检查项 | 结果 | 说明 |
| --- | --- | --- |
| DeepSeek API 调用成功 | ✅ | 真实调用返回高质量中文回答 |
| 状态机节点流转正常 | ✅ | `__start__ → classify_intent → generate → handle_error → __end__` |
| 单轮对话可拿到模型返回 | ✅ | 4 类问题均正确识别意图并生成回答 |

**意图识别测试**：`广州明天天气` → weather；`广州秋天穿什么` → outfit；`推荐周末好去处` → travel；`你好呀` → other。全部正确。

**HTTP 接口测试**：`POST /api/v1/chat` 返回 `200 / code=0 / intent=outfit`，回答合理。

### 三、每个改动文件的用途
1. `services/llm_client.py` — LLM 客户端：懒加载缓存 ChatOpenAI，暴露 `ainvoke()`。
2. `services/agent.py` — LangGraph 状态机 + 意图识别/生成/兜底三节点 + `chat()` 单轮入口。
3. `schemas/chat.py` — ChatRequest / ChatResponse。
4. `routers/chat.py` — `/api/v1/chat` 对话路由。
5. `main.py` — 注册 chat 路由，版本 0.5.0 → 0.6.0。
6. `requirements.txt` — 新增 langchain 三件套依赖。

### 四、已知局限（如实记录）
- **模型会编造日期/天气**：单轮对话未接入真实天气工具，模型回答"明天 3月15日""5月中旬"等是**凭训练知识猜测**，非真实数据。这是 Day 9（MCP 工具）/ Day 10（RAG 接入）要解决的核心问题，Day 8 只要求基础单轮对话跑通，符合预期。

### 五、多模型抽象层改造（Day 8 追加）
> 用户要求：对话模型不写死 DeepSeek，可自由切换其他模型，但只选 OpenAI 兼容格式、避开不兼容模型（如 Claude）。

#### 做了什么
- **`app/core/config.py`**：原写死的 DeepSeek 三件套扩展为 6 个 OpenAI 兼容提供商配置，并新增 `LLM_PROVIDER` 开关字段（默认 `deepseek`）：
  - `deepseek` / `qwen`（通义千问）/ `zhipu`（智谱 GLM，复用 Embedding Key）/ `openai` / `moonshot`（Kimi）/ `ollama`（本地）。
- **`app/services/llm_client.py`**：把 `get_llm()` 改成「多提供商抽象层」：
  - `_PROVIDERS` 字典维护「提供商名 → 配置字段」映射，统一走 `langchain-openai` 的 `ChatOpenAI`，无需分支特殊处理。
  - `get_llm(provider=None)`：不传则用 `settings.LLM_PROVIDER`，传则切换指定提供商。
  - **多实例缓存**：`provider → ChatOpenAI` 各自独立缓存，切换互不影响，同一 provider 不重复实例化。
  - **空 Key 优雅降级**：未配 Key 的 provider 用占位符实例化（仅 warning，不崩溃），真正调用时报错由 LangGraph 兜底节点统一处理。
- **`backend/.env`**：新增 `LLM_PROVIDER=deepseek`，其余提供商以注释模板给出，按需取消注释填 Key 即可。
- **`28天开发计划.md`**：同步修订说明、项目总览 AI 行、关键约束（新增"只接 OpenAI 兼容格式，不引入 Claude 等不兼容适配器"）、Day 8 章节目标与检查清单。
- `agent.py` 上层调用零改动（无参 `get_llm()` 自动跟随 `LLM_PROVIDER`）。

#### 验证结果
| 验证项 | 结果 | 说明 |
| --- | --- | --- |
| 默认提供商 | ✅ | `deepseek-v4-flash`（`.env` 读出），向后兼容 |
| 指定提供商切换 | ✅ | `get_llm("zhipu")`→`glm-4-plus`；`get_llm("qwen")`→`qwen-max` |
| 多实例缓存隔离 | ✅ | 同 provider 命中缓存（`is` 为 True），不同 provider 独立实例 |
| 未配 Key 不崩溃 | ✅ | qwen 仅 warning，实例化成功 |

#### 适用范围说明
- 本次只打好「底层可切换」地基，**未做用户前端选模型 / 存 Key 等**（属后续排期）。
- 后续若需用户侧选择：① 数据库 `users` 表加 `preferred_model` ② 管理员后台配 Key ③ 对话接口透传 `provider` 参数。

### 六、代码提交
- 待用户检查代码后提交（按用户要求：不自动 git）

---

## 2026-08-25（Day 9）MCP 工具层封装

### 一、今日目标
提问"广州今天天气"，Agent 自动调用 MCP 天气工具并返回真实结果（不再让模型瞎编）。

### 二、核心产出
把工具层从 Agent 中**解耦**，用标准 MCP 协议暴露，实现"Agent 自动发现并调用工具"。

#### 1. MCP Server（`backend/mcp_server/`）
- `server.py`：FastMCP 实例 + 3 个工具注册，支持 **stdio（本地开发）** 与 **streamable-http（上线）** 双传输模式，命令行 `--transport` 切换。
- `tools.py`：纯业务函数（与 MCP 协议解耦，可单测/复用）。
- 3 个工具：`get_weather`（实时天气）、`get_forecast`（N 天预报）、`search_news`（资讯检索）。

#### 2. 统一天气服务层（`app/services/weather_service.py`）
- 按 `WEATHER_PROVIDER` 配置路由数据源（qweather / open_meteo）。
- **降级策略**：和风失败自动回退 Open-Meteo。
- `weather_to_text()` 把天气包转成 LLM 可读的结构化文本。

#### 3. 和风天气客户端（`app/services/qweather_client.py`）
- 完整实现实时 + 7 天预报 + 官方预警（台风/暴雨/高温等），字段与 Open-Meteo 对齐。
- **当前 Key 返回 403（Invalid Host，待生效）**，故默认数据源暂切 `open_meteo`；Key 生效后改 `.env` 一行即切换。

#### 4. 城市字典（`app/services/city_dict.py`）
- 内置 20 个国内城市（名/拼音/别名 → 经纬度 + 和风 LocationID），"广州"/"gz"/"广州塔" 均能识别。

#### 5. Agent 接入 MCP（`app/services/mcp_client.py` + `agent.py`）
- `mcp_client.py`：用 `langchain-mcp-adapters` 的 `MultiServerMCPClient` 动态加载 MCP 工具，缓存复用；stdio 模式用 `sys.executable`（venv Python）拉起子进程。
- `agent.py`：新增 ReAct 式工具循环 —— `classify_intent → agent(LLM 决策) ⇄ tools(执行 MCP 工具)`，意图为 weather/travel/outfit 才绑定工具，闲聊不绑定（省 token）。

### 三、验证结果（端到端真实数据）
| 场景 | 意图 | 是否调工具 | 结果 |
| --- | --- | --- | --- |
| "广州今天天气" | weather | ✅ get_weather | 26.3°C 阴、体感 32.7°C（真实 Open-Meteo） |
| "北京未来3天" | weather | ✅ get_forecast | 3 天预报 + 降水预警 |
| "台风资讯" | weather | ✅ search_news | 返回台风季资讯（真实 news 表） |
| "你好呀" | other | ❌ 不调工具 | 直接友好回复 |

日志可见完整链路：`ListToolsRequest（发现工具）→ CallToolRequest（调用）→ HTTP 请求 Open-Meteo`，确认是经 MCP 协议调用，非直连。

### 四、遗留问题（如实记录）
- **和风 Key 403（Invalid Host）**：Key 已写入 `.env` 但未生效。可能需在和风控制台配置 API Host 白名单 / 等待激活 / 实名认证。生效后改 `WEATHER_PROVIDER=qweather` 即可，代码零改动。
- **资讯检索意图归类**：问"台风资讯"被识别为 weather 而非独立"资讯"意图（意图标签里没有 news 类）。可后续扩展意图分类，非阻塞问题。

### 五、改动文件
- 新增：`backend/mcp_server/{__init__,server,tools}.py`
- 新增：`app/services/{qweather_client,weather_service,city_dict,mcp_client}.py`
- 修改：`app/services/agent.py`（加工具循环）、`app/core/config.py`（天气源+MCP 配置）、`backend/.env`、`backend/requirements.txt`、`28天开发计划.md`

### 六、代码提交
- 待用户检查代码后提交（按用户要求：不自动 git）

---

## 2026-08-25（Day 10）RAG 接入 + 自动决策逻辑

### 一、今日目标
打通完整链路：用户提问 → 意图识别 → RAG 检索 → MCP 工具调用 → LLM 生成答案；完善 ReAct 决策与异常降级。

### 二、核心产出

#### 1. RAG 检索接入 MCP 工具
- `mcp_server/tools.py` 新增 `search_knowledge(query, top_k)`：调用已有 `rag_service.search()` 做 pgvector 语义检索。
- `mcp_server/server.py` 注册为第 4 个 MCP 工具。至此 MCP Server 共 4 工具：`get_weather` / `get_forecast` / `search_news` / `search_knowledge`。

#### 2. 完善 ReAct 决策逻辑（三路径）
- 重写 `GENERATE_SYSTEM_PROMPT`，明确三条决策规则：
  1. **信息不足 → 反问**（如缺城市/日期/出行方式，礼貌追问而非盲目调用工具或猜测）
  2. **需实时数据 → 调工具**（天气/预报/资讯/知识，用真实数据作答）
  3. **可直答 → 直答**（闲聊直接回复）
- 意图分类新增 `knowledge`（知识问答）类别，景点/美食/交通攻略类问题会触发 `search_knowledge`。

#### 3. 异常降级策略
- **意图识别降级**：LLM 调用失败时，用内置关键词规则（`_keyword_intent`）兜底，不中断流程。
- **LLM 生成降级**：`_agent_node` 加 try-except，失败写入 `error` 状态 → 走 `handle_error` 节点返回友好提示。
- **工具调用降级**：MCP Client 设 `handle_tool_errors=True`，工具异常返回错误文本而非抛异常。
- 对外 `chat()` 最外层兜底，保证接口永不 500。

### 三、验证结果（端到端真实数据）
| 场景 | 意图 | 行为 | 验证点 |
| --- | --- | --- | --- |
| "广州有哪些必去景点" | knowledge | 调 search_knowledge | ✅ RAG 接入，返回广州塔/陈家祠/越秀公园等真实知识库内容 |
| "下雨天去广州穿什么" | outfit | 天气+知识库协同 | ✅ 多工具协同 |
| "广州今天天气" | weather | 调 get_weather | ✅ 工具调用 |
| "天气怎么样"（缺城市） | weather | 反问用户 | ✅ 决策-反问 |
| "你好，你是谁" | other | 直接回答 | ✅ 决策-直答 |

### 四、遗留问题（如实记录）
- 无新增阻塞问题。Day 9 遗留的「和风 Key 403」与「资讯意图归类」仍待后续处理（非本日范围）。

### 五、改动文件
- 修改：`mcp_server/tools.py`（+search_knowledge）、`mcp_server/server.py`（+注册）、`app/services/agent.py`（决策+降级）、`28天开发计划.md`

### 六、代码提交
- 待用户检查代码后提交（按用户要求：不自动 git）

---

## 2026-08-25（Day 10 追加）联网搜索 + 用户私有知识库

> 用户追加需求：① Agent 增加「联网搜索」能力（天气/知识库覆盖不到的实时信息）；② RAG 支持用户上传自己的出行计划/旅游攻略，实现个性化知识库。原计划书 Day 11（出行规划 Skill）顺延。

### 一、需求背景
用户提出两点架构建议：
1. Agent 缺「联网上网找信息」能力，RAG 只查本地公共知识库不够；
2. RAG 更适合做成「用户自己上传的出行计划/旅游计划」这类个性化知识库，而天气/城市信息应走联网。

### 二、核心产出

#### 1. 联网搜索工具（`app/services/web_search_service.py`）
- **Tavily 优先**（AI 场景搜索引擎，返回结构化结果），**DuckDuckGo 兜底**（无 Key 可用）。
- 注册为第 5 个 MCP 工具 `web_search`，处理知识库/天气覆盖不到的实时问题（景区开放、活动、门票、时事）。
- 注：Tavily 已确认可连通（返回 401 待配 Key）；DuckDuckGo 当前网络被墙（ConnectTimeout），作为有代理环境下的兜底保留。

#### 2. 用户私有知识库（多租户 RAG）
- 新建 `user_knowledge` 表（`user_id` 外键隔离 + `Vector(1024)`）。
- `app/services/user_knowledge_service.py`：上传（切块+向量化入库）/ 检索（限定 user_id）/ 列表 / 删除。
- `app/routers/user_knowledge.py`：`POST /api/v1/my-knowledge` 上传、`/search` 检索、`GET` 列表、`DELETE` 删除，均需鉴权。

#### 3. 关键架构决策：私有检索走「本地工具」而非 MCP
- **问题**：MCP 工具跑在独立子进程（stdio），contextvars 用户上下文跨不了进程，导致 `search_my_plans` 拿不到 user_id。
- **解决**：`search_my_plans` 改为 Agent 进程内的「本地工具」（`app/services/local_tools.py`），通过 contextvar 读取当前 user_id。
- **原则**：无状态公共工具（天气/资讯/公共知识库/联网搜索）走 MCP；有用户态的私有检索走本地工具，两者在 Agent 层合并绑定。

#### 4. 用户上下文传递（`app/services/user_context.py`）
- contextvars 实现，`chat()` 入口设置 user_id，工具内部 `get_current_user_id()` 读取，`finally` 中 reset，天然支持并发隔离。
- chat 接口改用 `OptionalUser` 可选鉴权，登录则传 user_id，匿名则 None。

### 三、验证结果（端到端真实数据）
| 场景 | 结果 |
| --- | --- |
| 登录用户问"我的行程计划第二天去哪里" | ✅ 返回"根据你的广州三日游计划，第二天：越秀公园→沙面岛→珠江夜游"，准确还原上传内容 |
| 匿名用户问同样问题 | ✅ 提示"未登录，无法检索个人知识库"，多租户隔离生效 |
| 公共知识问答"广州必去景点" | ✅ 正常返回，不受影响 |
| MCP 工具注册 | ✅ 5 个 MCP 工具 + 1 个本地工具 |

### 四、遗留问题（如实记录）
- ~~**Tavily Key 未配置**~~：**已解决**。用户提供完整 Tavily Key，实测联网搜索 200 OK，Agent 成功联网返回广州塔票价/开放时间等实时信息。
- **和风 Key 403**、**资讯意图归类**（Day 9 遗留）仍待处理。

### 五、改动文件
- 新增：`app/services/{web_search_service,user_knowledge_service,user_context,local_tools}.py`
- 新增：`app/models/user_knowledge.py`、`app/schemas/user_knowledge.py`、`app/routers/user_knowledge.py`
- 修改：`mcp_server/tools.py`、`mcp_server/server.py`（+web_search）、`app/services/agent.py`（合并本地+MCP 工具）、`app/routers/chat.py`（可选鉴权）、`app/core/config.py`（+TAVILY 配置）、`app/main.py`、`app/models/__init__.py`、`app/db/session.py`、`app/scripts/init_db.py`

### 六、代码提交
- 待用户检查代码后提交（按用户要求：不自动 git）

---

## 2026-08-25（Day 10 追加2）行程天气提醒（结构化行程 + 天气联动）

> 用户澄清个人知识库的真正用途：不是"查行程"，而是「结合用户某天行程 + 当天天气，生成出行提醒与推荐」。

### 一、需求澄清
用户上传行程的目的，是让 AI 在用户问"明天有什么安排/要注意什么"时，能：
1. 取出该日行程（日期、时间、地点、活动）
2. 查当天真实天气
3. 逐条生成天气提醒 + 出行推荐（雨天带伞、高温防晒、户外是否改期等）

### 二、核心产出

#### 1. 结构化行程表（`app/models/itinerary.py`）
- 新建 `itineraries` 表：`user_id` + `title` + `date`(YYYY-MM-DD) + `start_time`(HH:MM) + `location` + `activity` + `note`。
- 与纯文本知识库（user_knowledge）分离，因天气匹配需要精确的日期/地点/活动字段。

#### 2. 行程接口（`app/routers/itinerary.py`）
- `POST /api/v1/itinerary` 新增（结构化字段）、`GET` 查询（可按 date 过滤）、`DELETE` 删除，均需鉴权。

#### 3. 行程天气提醒服务（`app/services/itinerary_service.py`）
- `itinerary_weather_reminder(user_id, query)` 核心流程：
  1. 从自然语言解析日期（今天/明天/后天/大后天/具体日期）
  2. 取该日行程（按 start_time 排序）
  3. 查当天天气（从 7 天预报中匹配日期）
  4. 拼装"行程清单 + 当天天气 + 提醒指令"，交给 LLM 生成逐条提醒

#### 4. 本地工具 `check_itinerary_weather`（`app/services/local_tools.py`）
- 与 `search_my_plans` 并列的本地工具（带用户态），Agent 决策提示词新增第 5 条规则：
  「问行程安排/结合天气提醒」→ 调 `check_itinerary_weather`。

### 三、验证结果（端到端真实数据）
用户（admin）添加「明天 09:00 白云山爬山」「明天 19:30 珠江夜游」后提问：

> "明天有什么安排，要注意什么？"

Agent 返回：
- 行程：09:00 白云山爬山、19:30 珠江夜游
- 天气：当天雷阵雨（降水约16mm，25.6~30.4°C）
- 逐条提醒：爬山穿防滑鞋带雨衣避雷、夜游注意江面风大保暖、整体闷热补水等

完整实现「行程 + 天气 → 提醒推荐」链路。

### 四、遗留问题（如实记录）
- **和风 Key 仍 403（Invalid Host）**：`devapi` 与 `api` 两个域名都试过均 403，需用户在和风控制台检查「API Host 白名单 / 激活 / 实名认证」。天气提醒当前走 Open-Meteo（已够用）。
- **跨城市天气**：行程提醒目前统一用广州天气，若行程地点是其他城市暂不精确匹配（后续可扩展 location→城市解析）。

### 五、改动文件
- 新增：`app/models/itinerary.py`、`app/schemas/itinerary.py`、`app/routers/itinerary.py`、`app/services/itinerary_service.py`
- 修改：`app/services/local_tools.py`（+check_itinerary_weather）、`app/services/agent.py`（+决策规则5）、`app/main.py`、`app/models/__init__.py`、`app/db/session.py`、`app/scripts/init_db.py`

### 六、代码提交
- 待用户检查代码后提交（按用户要求：不自动 git）

---

## 2026-08-26（Day 11 · 出行规划 Skill 开发）

### 一、今日目标
提问"明天上午从广州南站去广州塔怎么出行"，Agent 返回多套带天气提示的出行方案。

### 二、核心产出

#### 1. 高德地图客户端（`app/services/amap_client.py`）
- **地理编码** `geocode()`：地址 → 经纬度（高德 API / 无 Key 时本地地标表兜底）
- **路线规划** `plan_route()`：驾车方案（高德 driving API）+ 无 Key 兜底（多交通方式估算）
- **内置广州地标坐标表**（`_GZ_LANDMARKS`，16 个常见地点：广州塔/白云山/北京路/机场等），无 Key 时解析具体地点坐标
- **Haversine 公式**计算球面距离，按交通方式经验系数估算耗时/费用

#### 2. 出行规划 Skill（`app/services/travel_skill.py`）
- 三维评分融合：**时间（0.4）+ 费用（0.3）+ 天气（0.3）**，各归一化 0~1 后加权求和
- 天气适宜度：降水惩罚 + 温度偏离舒适区（18~26°C）惩罚 + 恶劣天气关键词惩罚
- 交通方式合理距离区间过滤（步行 ≤3km、骑行 ≤15km、驾车/公交不限）

#### 3. MCP 工具 `plan_travel_route`
- 注册为第 6 个 MCP 工具，Agent 决策提示词新增规则 6：路线/交通问题 → 调 plan_travel_route

### 三、验证结果（端到端真实数据）
| 场景 | 结果 |
| --- | --- |
| 广州南站 → 广州塔 | ✅ 4 套方案，驾车 28min/地铁 57min，三维评分排序 |
| 广州塔 → 白云机场（长距离） | ✅ 正确过滤步行/骑行，只剩驾车/公交 |
| 北京路 → 天河城（短距离） | ✅ 保留驾车/地铁/骑行 |
| Agent 问"明天从广州南站去广州塔" | ✅ 意图 travel，返回带雷阵雨提醒的方案（地铁推荐不淋雨、驾车最快、骑行不推荐） |

日志可见完整链路：`ListToolsRequest → CallToolRequest(plan_travel_route) → 天气 HTTP 请求 Open-Meteo`。

### 四、遗留问题（如实记录）
- **高德 Key 未配置**：代码已就绪（含兜底），需用户去 https://lbs.amap.com 申请 Web 服务 Key 填入 `.env` 的 `AMAP_API_KEY`，即可切真实驾车路线（含实时路况/过路费）。当前用内置地标坐标 + 经验系数估算。
- **和风 Key 403**（Day 9 遗留）仍待处理。

### 五、改动文件
- 新增：`app/services/amap_client.py`、`app/services/travel_skill.py`
- 修改：`mcp_server/tools.py`（+plan_travel_route）、`mcp_server/server.py`（+注册）、`app/services/agent.py`（+决策规则6）、`app/core/config.py`（+高德配置）、`.env`、`28天开发计划.md`

### 六、代码提交
- 待用户检查代码后提交（按用户要求：不自动 git）
